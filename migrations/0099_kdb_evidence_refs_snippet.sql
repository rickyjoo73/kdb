-- 0099: 근거 대장에 스니펫 추가 — URL 만으로는 판정을 재현할 수 없다.
--
-- 문제(2026-08-02 실측): 0098 이 URL+제목을 남겨 "되짚기"는 가능해졌으나, 적합성
-- 게이트(filterRelevantHits)와 gemma 판정은 **제목이 아니라 `제목 — 설명` 한 줄
-- (evHit.Line)** 을 입력으로 쓴다. 대장에 제목만 있으니 저장된 것으로 게이트 결정을
-- 재현할 수 없다.
--
-- ★실제로 오판을 만들었다. 이번 세션에서 대장 제목만 보고 '어머니 전'·'풍향중'·
-- '인터넷 걸'·'SNL 코리아 시즌8' 을 무관근거 승급으로 의심했는데, 복원된 gemma 근거는
-- 각각 "EBS 방영, 장윤주 출연 show" / "유튜브 '뜬뜬' 웹예능, 유재석·이성민 출연" /
-- "캣츠아이(KATSEYE)의 노래" 로 전부 타당했다. 이름이 설명에만 나오고 제목엔 없는 건이
-- 흔한데(적합률 제목기준 11.6%), 제목만 보면 전부 무관해 보인다.
-- 41차 §5-4(어휘 부분매칭 오탐 27건)와 같은 계열의 실수다.
--
-- 0098 이 "근거 ≠ 앵커"를 분리했다면, 0099 는 "저장된 근거 = 판정에 쓴 입력"을 맞춘다.
-- 기존 행은 빈 값으로 남는다 — 소급 채우기는 재검색이 필요해 별건(핸드오프 §6-1).
ALTER TABLE kwave_kdb_evidence_refs
  ADD COLUMN IF NOT EXISTS snippet text NOT NULL DEFAULT '';

COMMENT ON COLUMN kwave_kdb_evidence_refs.snippet IS
  '판정에 실제로 넣은 한 줄(evHit.Line = "제목 — 설명"). 게이트/판정 재현용.';
