# 이어서 진행하기 — 2026-09-14 19:35 KST

**대화 압축 뒤에도 방향을 잃지 않기 위한 문서다.** 새 세션은 여기부터 읽는다.
단일 실행 원장은 [`docs/KDB_INTEGRATION_TODO.md`](KDB_INTEGRATION_TODO.md).

---

## 1. ★ 운영자가 준 규칙 (절대 잃으면 안 된다)

0. **★빈칸을 보내지 않는다 (2026-09-14 방침 변경 — 종전 규칙을 뒤집는다).**
   운영자 원문: *"구글번역기라도 사용해서 막아버리면 어떻게 제공해줄건데, 먼저 채우고,
   llm 인지 단순번역인지 등 값을 주어 받는곳에서 판단해서 사용해야지 … 빈값을 안보내고,
   그래도 우리가 직번역이든 머라도 해서 보내야지 보낼때 출처등 명확한 내용도 같이"*

   **무엇이 바뀌었나.** 2026-07-04 의 *"추측=빈칸"* 은 **끝났다.**
   값을 주고 `provenance` · `locale_source` 로 등급을 함께 알려 **받는 쪽이 판단**한다.
   더 좋은 출처가 나중에 나오면 그때 올려 덮는다(상위 소스 자동 승급은 이미 있다).

   **왜 바뀌었나 — 실측.** presslocale 이 일본어판에 **한글을 그대로 남겼다.**
   KDB 가 `사랑이 온다` 의 ja(`ラブ・イズ・カミング`, codex-fallback)를 빈칸으로 지웠고,
   번역 모델이 참고할 것이 없어 원문을 남긴 것이다. **빈칸이 틀린값보다 나은 게 아니라,
   빈칸이 한글 잔존을 만들었다.**

   **조치(완료).** `KDB_SERVE_HIDE_LLM_ONLY=0` — 운영 `.env` 에 적용·재기동 완료.
   숨겨져 있던 **5,684건**이 등급과 함께 나간다(ja 1,230 · zh 886 · zh_hant 858 ·
   en 621 · pt_br 568 · vi 559 · id 510 · es 452).


1. **한 사람/한 대상 = 하나의 ID.** 겸업으로 ID 를 쪼개지 않는다. ID 가 갈리는 유일한
   이유는 **다른 대상**이라는 것이다. → [식별 계약 §1.1](KDB_IDENTITY_CONTRACT.md)
2. **흡수할 때 ID·분류·구분값을 함께 부여한다.** 구분되지 않으면 **지어내지 않고 검수로**.
3. **wikidata QID 는 보조다.** 자체 ID 가 주 앵커다.
4. **관측이 먼저, 흡수가 나중.** 관측하지 않은 값을 지어내지 않는다(D-37).
5. **권리 판단은 운영자 몫.** 운영자가 "사용 가능"이라 한 것을 내가 게이트로 막지 않는다.
6. **AI 가 스스로 판단해 작동하는 시스템이다.** 없던 사람 승인 단계를 만들지 않는다.
7. **다른 사이트 컨테이너는 절대 건드리지 않는다.** (2026-09-14 명시 지시)

---

## 2. 지금 상태 (실측)

### ★운영 장비가 바뀌었다 — 2026-09-14 13:39 넘겨받기 완료

```
운영    aiin23  ssh aiin@atikar.com -p 38383   (i7-6700 8T · RAM 46G · 스왑 0)
        체크아웃 /home/aiin/kdb/repo   · TDB /home/aiin/tdb
        DB 는 / (Samsung 750 EVO SSD, 478 MB/s, 118G 여유) · 백업자리 /data (2TB, 1.7T)
옛 서버 114.203.210.38  — kdb-db·tdb-db 가 **낡은 사본**으로 남아 있다(되돌리기용)
        중계 두 개만 산다: kdb-shim(172.19.0.240, 별칭 kdb-app) · tdb-shim(172.19.1.0, 별칭 tdb-api)
```

**소비자·nginx 는 한 줄도 안 고쳤다.** 옛 서버의 중계가 그 IP·이름을 그대로 물려받아
SSH 터널로 23번에 넘긴다. 소비자 6개(edipresso×2·nbntv·trendbiz·mediafine·issuetalk)는
여전히 `http://kdb-app:9100` 을 부른다.

```
표기         2,686,762
대상         557,411    후보 414,590 · 범위 밖 130,134 · 활성 12,686
유형 미상    62,467     그중 편집 범위 안은 16,476
무결성 6종   전부 0     내용 지문 4fc0272591380a2ca6b318941a699080 (이전 전후 동일)
공통 공급     활성 흡수분 1건 (서울패션위크) — ja·zh-Hans ready. 그전까지 0건
git HEAD     5c16d5e
배포         main push → GitHub Actions → **aiin23** (비밀값 5개 교체 완료)
회귀         bash scripts/run-isolated-regression.sh (23번) — 전부 통과
```

**이전 효과 — 같은 경로·같은 트래픽 실측**

```
12:49 옛 서버        /v1/entities/match   2,106 · 2,100 · 2,066 ms
13:40 aiin23         /v1/entities/match     513 ·   624 ·   549 ms   ← 이전으로 4배
14:07 정규식 제거후  /v1/entities/match      34 ·    36 ·    35 ms   ← 다시 15배
                                            누적 60배
디스크 쓰기     106 MB/s → 478 MB/s (4.5배)
메모리          가용 3G·스왑 6G → 가용 32G·스왑 632KiB
```

**왜 22번이 아니라 23번인가.** 22번은 NVMe(1,900 MB/s)가 더 좋지만 **OS 디스크
(Seagate 1TB 회전)에 진행 중인 I/O 오류**가 있다(09-13·09-14 커널 로그). SMART 는
PASSED·재할당 0 이라 **SMART 만 보면 멀쩡해 보인다** — 커널 로그가 진실을 말한다.
SErr 핸드셰이크 오류 비트 + 재할당 0 조합이라 **SATA 케이블 불량일 가능성**이 있다.
케이블을 바꿔 오류가 멎으면 22번이 더 나은 선택이 된다(운영자 판단 대기).

**직전 완료: 5개 작업 전부 + 서버 이전.**

| # | 한 일 | 결과 |
|---|---|---|
| ① | VACUUM + 재측정 | 집계 2,427ms → 235ms. 부분 인덱스는 **안 붙였다**(표기의 100.00%가 verified) |
| ② | kdb-db 한도 적용 | 중단 5초 (옛 서버에서 한 일) |
| ③ | 체인 브랜드 추출 | **1,515개**. 삼자 일치, 중복 0, 지점수 하한 정확히 5 |
| ④ | 지점형 범위 밖 | **123,773건** rejected. 문화재 946·브랜드 1,515·체인 머리 12 보존 |
| ⑤ | 유형 판정 | **51,685건**. 미상 114,152 → 62,467(범위 안 16,476) |
| ⑥ | 서버 이전 | **146초** 중단. 데이터 지문 동일, 소비자 무설정변경 |
| ⑦ | match 정규식 제거 | **581ms → 5.3ms**. 동치를 본문 8개로 시험에 박음 |
| ⑧ | 격리 회귀 이전 | template 을 운영 전량 사본으로. **표기 1,249 → 2,686,762** |
| ⑨ | **P4.02** 여행 공급 | 연결할 여행 출처 **없음**을 실측. 막던 것은 대상 상태였다. 활성화 경로를 만들고 **1건** 개통 — 공통 원장이 값을 공급한 첫 사례 |
| ⑩ | **P4.03-a** 인물 공급 근거 감사 | 권리·정체성·기간·분야 중 **셋이 통과 못 함**. wikidata 근거 65,487건이 항목 미지목. 활성화 도구에 가드 추가(29,169건 차단) |
| ⑪ | **P4.01** 입력 계약 | 알려진 이름이 있으면 새 동명이 발견될 길이 없었다. `homonym_risk` 신호 추가(서빙 불변, 2.2ms/200건) |
| ⑫ | **P4.01-g** 관리자 UI | 공통 원장이 **메뉴에 없었다**. ⑥ 공통 원장 절 + 공급 개시 대기 + 동일인 판정 대기열 |
| ⑬ | **QID 회수** | 운영 434건 적용(외부ID 243→677). 전량 조회는 **4,000/15,092 에서 중단**(운영자 지시: 차단 위험) |
| ⑭ | **근거 교정** | 미지목 wikidata 주장 65,487건 → TDB 자체 관측으로 정정. **진행 중** |
| ⑮ | **유형 미상** | `tourapi_ko/12` 운영자 결정으로 map — 9,127건 분류 **대기**(교정 끝나면 자동) |
| ⑯ | **관리자 UI** | 통합 찾기 · 분류 현황 · 공급 개시 · 동일인 판정 (⑥ 공통 원장 절) |

## 3. ★ 바로 이어서 할 일

### 3.1 지금 서버에서 돌고 있는 것 (분리 실행, 세션과 무관)

```bash
ssh aiin@atikar.com -p 38383          # scratchpad 의 r3 래퍼
cat /tmp/ev_log.txt                   # 근거 교정 배치 (65,487건, 남은 수가 줄어든다)
cat /tmp/ev_done                       # 있으면 끝남. rc 확인
cat /tmp/unk_log.txt                   # 교정 끝나면 자동으로 유형 분류가 이어진다
```

끝나면 확인할 것:
- 남은 미지목 근거 **0** · 표기 총수 **2,686,762 유지**(값은 안 건드렸다)
- 차단 표기 0 · 근거 부적합 0
- 범위 안 유형 미상 16,476 → **약 7,349**
- `/admin/kentity/supply` 의 「출처 항목 미지목」 **29,169 → 0**

### 3.2 ★정정 — 보강기는 **있다. 굶고 있었다** (2026-09-14 재확인)

처음엔 "흡수분에 보강 어댑터가 0개"라고 적었다. **절반만 맞았다.**

맞는 부분: 출처 어댑터 22개(kofic·kmdb·tmdb·mdl·ott·musicbrainz·discogs·itunes·kopis·
kowiki·wikidata_person·wikidata_locale·zhwiki·localfill …)는 **전부 `kwave_*` 전용**이다.

틀린 부분: 공통 원장에는 **전용 보강기가 따로 있다** —
`internal/kdb/readiness/common_fill.go`, 정책 `common-anchored-fill-v1`.
검증된 위키데이터 앵커를 물고 빠진 로케일을 채운다. 트리거
`kentity_guard_automatic_name` 이 "자동 표기는 현재 검토된 정체성 의존을 가져야 한다"고
강제까지 한다. **설계도 구현도 다 돼 있었다.**

운영 실측:
```
KDB_COMMON_READINESS_ENABLED=1  KDB_COMMON_ENTITY_ENABLED=1  KDB_COMMON_FILL_ENABLED=1
자격 대상(native·active·검증앵커 1개)  =  0          ← 먹을 것이 없다
kentity_locale_fill_jobs              native-evidence-v1/no_evidence 24 뿐
```

`commonFillEligible` 이 요구하는 것: `Owner='native'` ✓ · `Status='active'` ✗(전부 candidate)
· 앵커 정확히 1개 ✗(검증된 QID 가 435개뿐) · 해당 로케일이 아직 없을 것.

**먹이는 순서**
1. 미검증 QID 17,234건을 항목 원문과 대조해 검증 → 앵커 1개가 생긴다
   (`scripts/wdverify-batch.js` + `docs/p4/verify_premapped_qids.sql`)
2. `activate_absorbed_supply.sql` 로 활성화 → `status='active'`
3. 보강 워커가 **알아서** 빠진 로케일을 채운다 — 새로 만들 것이 없다

**교훈: "데이터가 비었다"를 "기능이 없다"로 읽지 않는다.** 22개를 세고 멈추지 말고
그 원장 전용 경로가 따로 있는지 봐야 했다. 오판 22번(빈 것 ≠ 고장)과 같은 실수다.

### 3.3 en 보강 시도 — kowiki 수율 **1.0%** (표본 300)

```
NO_PAGE 221(74%) · NO_EN 40 · REDIRECT 18 · DISAMBIG 16 · OK 3
```
흡수분 상위가 restaurant 17,326 · accommodation 4,501 이라 **백과사전에 없다.**
기존 드레인이 10% 를 냈던 것은 대상이 달랐기 때문.
### ★층화 표본 결과 — kowiki 는 **en 도구가 아니라 QID 도구**다

유형별 각 100건:

| 유형 | 문서 있음 | `NO_EN`(영어판 없음) | en 회수 |
|---|---|---|---|
| **person** | **94%** | **75** | **0** |
| heritage | 62% | 34 | 2 |
| work | 68% | 33 | 1 |

혼합 표본의 `NO_PAGE 74%` 는 식당·숙박이 평균을 끌어내린 것이었다.
**인물은 94%가 ko.wikipedia 문서를 갖고 있다.** 다만 영어판이 없어 `en` 은 못 얻는다.

**그런데 문서가 있으면 QID 를 준다** — 인물 94% 중 동음이의 16 을 빼면 **약 78%에서
QID 회수 가능**. 그리고 QID 는 P4.03 선결 ⑴⑵(항목 식별·분야 판정)의 열쇠이고,
"인물 16,685명은 물어볼 QID 자체가 없다"던 막힘을 푸는 유일한 길이다.
QID 를 얻으면 위키데이터가 en·ja·zh 라벨과 직업·생년을 함께 준다.

→ **다음 작업: kowiki 로 인물 QID 회수.** `scripts/kowiki-en.js` 를 QID 회수용으로
   좁히면 된다(가드 셋·100건마다 중간저장은 그대로). 회수한 QID 는 **보조 앵커**로
   기록하고(I03), 공급 자격은 자체 ID 가 준다 — `docs/p4/recover_wikidata_item_identity.sql`
   이 이미 그 형식이다.

### 3.4 다음 후보 (실측이 가리키는 순서)

1. 층화 표본 결과로 en 보강 방향 확정
2. 유형별 공식 출처 연결 — 문화재 `heritage_khs` 영문 · 식당/숙박 `tourapi_en` 재조회 ·
   영화 `kofic`/`kmdb` · 공연 `kopis` · 음악 `musicbrainz`/`itunes`
3. P4.07 동일인 판정 (대기열 화면 있음) — P4.03~P4.06 이 여기 막혀 있다
4. **P6.07 공개 문서 `/v1/docs` 갱신** — 운영자가 "추후 개선, 지금은 그대로 유지"라고
   정했다(2026-09-14). 실측: 엔드포인트 **28개 구현 / 8개만 문서화**, 공통 원장
   (`/v1/kentity/*`)이 통째로 빠져 있고, type 표가 기존 원장 것만 있다.
   자세한 목록은 [P6.07](KDB_INTEGRATION_TODO.md) 에 있다. **지금 손대지 않는다.**
5. 마지막에 nginx/TLS

## 3.8 ★밤새 한 일과 아침에 볼 것 — 2026-09-14 23:05

### 운영 최종 상태 (전부 확인함)
```
서비스   https://kdb.aiinplanet.com/v1/health  200  ci-20260914-db26da1
         kdb-app healthy restarts=0 · kdb-db restarts=0
중계     dockers_backend OK · mediafine_default OK   (소비자와 같은 이름으로 확인)
공통원장 표기 2,699,388 · 차단 0 · 근거없는 검증표기 0
         검증 외부ID 17,475 · 근거없는 검증 외부ID 0 · 활성 흡수분 301
기존원장 active 12,735 · romanization 어긋남 11(고칠 수 없는 것만)
```

### 배포 4회, 전부 격리 회귀 통과
`115648b` → `84fda5c` → `0aadab6` → `827b2f7`
(`84fda5c` 는 **회귀 없이 밀었다** — 오판 25 반복. 사후 확인은 통과)

### ★아침에 **결정이 필요한 것** — `gtranslate-raw` 222칸

게이트가 흠 잡은 기계번역을 최하위 등급으로 채우게 했다(운영자 지시).
실제로 들어간 값을 22건 표본으로 보니 **약 60% 쓸 만하고 40% 틀린다**:
```
쓸 만함  네가 사는 그 집 → 你居住的房子      만원의 행복 → 10,000韩元带来的幸福
         치티치티 뱅뱅 → 飞天万能车(실제 중국어 제목)   0집 → 第0张专辑
틀림     수제천(국악) → 手工制作的("수제품")   원소주(브랜드) → 元素("원소")
         슬리피맞아요 → 它很困("졸리다")      홀로아리랑 → 全息阿里郎("홀로그램")
         슈퍼아일릿 → 超级厕所("슈퍼 화장실")  ← 아이돌 그룹이다
```
**ja 1,065건으로 확대하지 않았다.** 40% 오류를 밤새 5배로 늘리는 것은 지시의 취지가
아니라고 판단했다. 되돌리는 것은 한 줄이다:
```sql
UPDATE kwave_entities SET canonical_zh='', canonical_zh_source='' WHERE canonical_zh_source='gtranslate-raw';
UPDATE kwave_entities SET canonical_ja='', canonical_ja_source='' WHERE canonical_ja_source='gtranslate-raw';
```
**판단 근거**: 같은 배포에서 `locale_absent` + `fill_hint` 가 나가기 시작했다.
"빈칸이 한글 잔존을 만든다"는 문제는 **값이 아니라 지시**로 이미 풀렸다.
그렇다면 40% 틀린 값을 더할 이유가 약해진다 — 그래도 운영자 결정이다.

### 안전한 채움은 소진됐다
```
en=46      유형 제외(term·unknown) 또는 번역 불가
vi/es/id/pt_br ≈70씩   en 자체가 비어 전파할 것이 없다
zh=1,246 · ja=1,065    대부분 인물. **zh 음역기가 없다**(ja 는 kana-rule 이 있다)
```

## 3.6 ★압축 시점 상태 — 2026-09-15 08:5x (여기부터 이어간다)

### 지금 브랜치에 있고 **아직 배포 안 된 것**: `homonym-evidence`

```
b3fd423  동명 가드가 근거를 읽게 (I01·I05)          ← 핵심
…        Search/Get 에 qualifier · 정확일치 우선
…        Go raw string 백틱 수정(빌드 깨졌던 것)
…        시험을 회귀 DB 데이터에 안 기대게 수정
…        docs: id 는 검색 키가 아니라 신뢰 표식
```

**해야 할 일: 회귀 한 번 더 → main 머지 → 배포.**
마지막 회귀에서 세 개가 실패했고 원인이 갈렸다:
- `TestRestoredSearchReturnsQualifierAndExactFirst` — **내 시험이 환경에 기댐.** 고쳤다(이름 바뀜)
- `TestEntityCenterAgainstRestoredInventory` — **main 에서도 실패.** 내 변경 탓 아님(별건)
- `TestCommonFillAgainstRestoredSchema` — statement timeout. **아직 안 가림.**
  가드 비용은 실측 233ms→503ms 라 타임아웃 원인 같지는 않다. 다시 돌려 확인할 것.

```bash
# 회귀 (23번 서버, 겹쳐 돌리면 DB 재생성이 충돌한다 — 하나씩)
KDB_REGRESS_REF=homonym-evidence bash scripts/run-isolated-regression.sh
```

### 오늘 운영에 **이미 반영된 것**
```
gtranslate-raw 222칸 되돌림 → 0     (레인도 코드에서 제거·배포됨)
구분값 승격 qualifier_ko 0 → 496,539
표기 총수 2,699,388 · 차단 0 · 근거없는검증 0
검증 외부ID 17,475 · 활성 흡수분 301
배포 버전 ci-20260915-a072ba9
```

### ★운영자 지적 — 되새길 것
> "너가 원하는것이 무엇인데 기존에 내가 작업지시내용과 다른거야? 나는 분명히 너한테
> 동명이인일 때, 한 사람이 여러 개 직업을 가지고 있을 때 등 명확하게 주었는데
> 너가 원하는 것이 무엇이고 나보고 무엇을 결정해 달라는 거지?"

**맞다. 규칙은 이미 있었다(I01·I05·M06). 허락을 물을 일이 아니라 구현할 일이었다.**
가드가 이름만 보고 무조건 막는 것은 설계 선택이 아니라 **규칙 미구현**이다.

> "아이디는 동명이인 구분값을 명확하게 줄려고 하는 거지만 검색에서는 사실 사용하기
> 힘들지. 다만 id 같이 보내주면 다른 사람이 아니고 동일인이구나 하는 신뢰는 더 갈 수 있지."

**ID 는 검색 키가 아니라 답에 실려 나가는 신원 표식이다.** 실측 확인:
```
채영  id=84ab95a9 구분=(TWICE) ja=チェヨン
채영  id=2dd14daf 구분=(CLC)   ja=チェヨン   ← 표기가 같아도 id 가 다르면 다른 사람
```
동명 가드에 막힌 candidate 도 **ID 로 물으면 다 나온다**(휘문고등학교 → Whimoon High
School · フィムン(徽文)高等学校). 가드는 이름 경로만 막는다.

### 기존 원장에 남은 I01 위반 4건 (아직 안 고침)
```
이승철    (구분없음)/(가수)   영문 둘 다 Lee Seung-chul     ← 같은 사람이 쪼개짐
선우용여  (배우)/(구분없음)   영문 둘 다 Sunwoo Yong-nyeo   ← 같은 사람이 쪼개짐
한혜진    (구분없음)/(Esteem) 영문 둘 다 Han Hye-jin        ← 진짜 동명이인일 수 있음
채영      (CLC)/(TWICE)       en: fromis_9 Chaeyoung / Chaeyoung  ← 구분값과 en 이 어긋남
```

## 3.7 ★P4.07 동일인 판정 — 2026-09-15 진행

### 결론부터: **"동명 22,231개"는 허수였다. 진짜는 3,339건이다.**

```
흡수분끼리 동명        22,231 이름 / 98,908 대상
  └ 구분값 승격 후      98,039 구분됨 · 866 미상 · 3 없음
       구분값은 흡수 때 이미 잡혀 있었다(중앙동 = 인천 제물포구 / 경남 창원시 …).
       classification_reason 문장 안에 갇혀 있어서 화면이 "9만 건 검수"로 보였다.
       qualifier_ko 로 옮겼다 — 496,539건. (D-04: 표시 한정어, 정체성 키 아님)

  └ 866 '구분값 미상' 도 판정 대상이 아니었다.
       person 531 의 상대는 location/district 1,203 · restaurant 153 …
       가인 = 브아걸 가인 + 전남/경남/경북의 마을 가인
       강남 = 방송인 강남 + 지명 강남 + 지하철역
       **유형이 다르면 정의상 다른 대상이다.** 판정할 것이 없다.

  └ 678 무리(이름+구분값 둘 다 겹침)도 대부분 다른 대상이다.
       돌하르방 × 9 (제주 제주시) — 서로 다른 돌하르방 9기
       구분값이 시군구까지만이라 겹치는 것이지 같은 대상이 아니다.
       → 더 잘게(읍면동·리) 구분값을 만들면 풀린다.
```

### ★진짜 대기열 — 흡수분 ↔ **기존 원장** 동명 5,159

```
person ↔ person   3,339   ← 같은 사람일 수 있다
유형 불일치       1,838   ← location↔work 768 · location↔person 630 … 다른 대상
```

**3,339 중 1,631(49%)은 지금 근거로 갈린다:**
```
① 같은 QID — 같은 사람    1,565
② 다른 QID — 다른 사람       66
③ 한쪽만 QID              1,549
④ 양쪽 다 QID 없음          159
```

### ★막힌 곳 — **가드가 판정을 안 읽는다**

```go
guardNoHomonymTrap = `NOT EXISTS (SELECT 1 FROM kentity_entities k
    WHERE k.canonical_ko = e.canonical_ko AND k.write_owner='kdb' AND k.status='active')`
```
이름만 보고 무조건 막는다. 판정 기록 자리는 이미 있다 —
`kentity_crosswalks.candidate_ids` · `decided_by` · `decided_at`, 후보 **10,231건** 기록됨.
**가드가 그것을 읽게 하는 것이 P4.07 의 남은 설계다.** 공급 계약을 바꾸므로 운영자 결정.

## 3.9 ★남은 일 전체 — 2026-09-15 갱신

전제가 하나 바뀌었다. 9-14 밤엔 **P4.07 동일인 판정이 병목**이라고 적었다.
9-15 에 재보니 그 숫자(동명 22,231)는 허수였고, 대신 **원장 자체가 오염돼 있다**는
것이 실측으로 드러났다. 오염된 원장 위에 분야 원천을 더 얹는 것은 순서가 틀렸다.
**순서를 바꾼다: 오염 정리 → 정체성 기제 → 분야 파일럿.**

### 0. 브랜치에 있고 배포만 남은 것

| 브랜치 | 무엇 | 검증 |
|---|---|---|
| `person-anchor-audit` | `kdb-app anchor-audit` (읽기 전용) + 0135 구분값 투영 | 격리 회귀 rc=0 · 0135 는 회귀 DB 에서 멱등·투영·표식제거 확인 |
| `ci-docs-skip` | 문서만 바뀐 밀어넣기는 배포 안 함 | 회귀 미실시 |

### 1. 오염 정리 — **여기가 새 병목** (P4.12 · P4.13)

```
앵커가 틀린 활성 인물            110건   전부 authoritative · 전부 소비자에게 나감
  ├ 주어진 이름·성씨 항목          88     미나 Q69507266 = 여성의 이름
  ├ 작품·단체 항목                 14     가비 Q5515395 = 영화 → ja `GABI/ガビ-国境の愛-`
  ├ 허구의 사람(배역)               1     이로하 Q107577910
  └ 기타                            7
그 앵커에서 따라온 표기 칸       155칸   ja 67 · en 63 · zh 16 · vi 9

배역이 숨은 자리 (QID 로는 안 잡힘)
  QID 없는 활성 인물             1,530   그중 1,529 가 이미 외국어 표기를 내보내는 중
  autopilot 무검증 활성 인물     1,037
  소비자가 character 라 했는데 person    17 (활성 8)
```

원인은 하나다. **가드 넷이 전부 인입 시점만 본다.** 이미 앉은 active 를 되돌아보는
레인이 없다. 고칠 때 대상이 아니라 **앵커를 뗀다** — 가비는 실존 무용가다.

### 2. 정체성 기제 — P4.07 의 실물

```
kentity_redirects                  0건   ★기제는 다 만들어져 있는데 아무도 안 쓴다
kentity_identity_operations merge  0건
[merged→uuid] 표식                31건   병합 결과가 표시용 문자열 안에 박혀 있다
                                         (전부 rejected 라 검색엔 안 샌다. 대상은 31/31 실재)
서빙이 redirect 를 따라가는가      아니오  Go 코드 어디에도 kentity_redirects 조회가 없다
```

- [ ] `Get`/`Match` 가 병합된 ID 를 따라가게. **이게 먼저다** — 이것 없이 병합하면
      옛 ID 를 쥔 소비자가 끊긴다. I01 이 막으려던 바로 그 피해다.
- [ ] `[merged→]` 31건을 제대로 된 자리(operations + redirects)로 옮긴다.
- [ ] I01 위반 4건: 이승철·선우용여·한혜진은 **같은 QID** 라 병합 대상.
      채영(CLC) 2dd14daf 는 구분값=CLC · QID=TWICE · en=fromis_9 — 셋이 다 다르다.
      병합이 아니라 **틀린 근거를 떼는 것**이 맞다.
- [ ] 흡수분↔기존원장 5,159: person↔person 3,339 (같은 QID 1,565 · 다른 QID 66 ·
      한쪽만 1,549 · 없음 159) · 유형 불일치 1,838.
- [ ] 678 무리(이름+구분값 둘 다 겹침)는 구분값을 읍면동·리까지 잘게.

### 3. 기존 계획 잔여 — 33항목

| 단계 | 남음 | 무엇인가 | 막고 있는 것 |
|---|---|---|---|
| **P4** | **11** | P4.03~06 분야 파일럿 · P4.07 동일인 · P4.08~11 재시도·예산·G4 · **P4.12 앵커 오염** · **P4.13 배역** | P4.12/P4.13 이 P4.03~06 보다 앞선다 |
| **P5** | 6 | dual-read → 단일 writer, 복구 연습, 구 writer 종료 | P4 의 G4 |
| **P6** | **8** | 업무 흐름 인수 · 권한/CSRF · 24시간 관측 · 복구 훈련 · 공개 문서 · **P6.08 파이프라인** | P5 (P6.08 만 지금 가능) |
| **P7** | 8 | span 추출 · Glossary · PressLocale 큐 · Gemma 풀 · validator · E2E | P6 · 상당 부분 presslocale 쪽 |

### 4. 원장 밖

- [ ] nginx/TLS 를 23번으로 (컨테이너, sudo 없음) → DNS 이전 → 옛 서버 정리
- [ ] 간체 교정: **자동 불가**(오판 28). 간체가 확실한 것만 손으로
- [ ] 인물 zh 표기: 음역기가 없다. ja 는 `kana-rule` 이 있는데 zh 는 없다
- [ ] `kwave_entities_homonym_key` — (이름,유형,구분값) UNIQUE. 구분값을 아직 모르는
      두 번째 동명인은 저장 자체가 안 된다(D-29). 0135 시험 중에 다시 부딪혔다

### 임계경로

```
앵커 오염 회수(P4.12) ─┐
배역 판별(P4.13)      ─┼→ redirect 서빙 → 동일인 판정(P4.07) → 분야 파일럿(P4.03~06) → G4 → P5 → G5 → P6 → G6
파이프라인(P6.08)     ─┘
```

## 4. 운영자가 내린 결정 (이미 반영됨, 되묻지 말 것)

| 결정 | 내용 |
|---|---|
| TDB 데이터 | 사용 가능. 권리 판단은 운영자 몫 |
| `편의오락` 94,024 | `location`(세부 NULL)로 올린다. `decided_by='operator'` 로 표시 |
| 지점형 | **전부 범위 밖**. 삭제가 아니라 `status='rejected'` |
| 브랜드 | 지점 **5개 이상**인 체인만 추출 (실행 결과 앞머리 2,288 중 **1,515개** 생성, 773개는 이미 있어 건너뜀) |
| 서버 이전 | **5개 작업을 끝낸 뒤** 이전한다 → **5개 끝났다. 이전이 다음 순서다** |

---

## 5. 지금의 배치 (이전 완료 뒤)

```
 소비자 6개 (옛 서버 dockers_backend)          nginx (옛 서버)
   http://kdb-app:9100  ─┐                      kdb.aiinplanet.com
                          │                     tdb.aiinplanet.com
                          ▼                        │
              kdb-shim 172.19.0.240 ◄──────────────┘  (upstream IP 그대로)
              tdb-shim 172.19.1.0
                          │  autossh -L, 옛 서버 → 23번 (암호화, 공유기 설정 불필요)
                          ▼
              aiin23  127.0.0.1:9100 / 9101 / 9200
              kdb-app · kdb-db · kdb-searxng · tdb-api · tdb-db · tdb-worker
```

**왜 중계인가.** 23번은 공유기 뒤 사설 IP(192.168.0.23)라 9100 을 밖으로 열 수 없고
(포워딩은 38383→38371 하나뿐), 소비자 6개는 전부 **내부망 이름**으로 부른다.
중계가 옛 `kdb-app` 의 IP·이름을 그대로 물려받으므로 **소비자도 nginx 도 안 고쳤다.**

**중계 만든 곳:** 옛 서버 `~/kdb-shim` (Dockerfile·Dockerfile.tdb·key).
열쇠는 `~/.ssh/kdb_migration_ed25519`, 23번 authorized_keys 에 `from="114.203.210.38"` 제한.

**되돌리는 법(옛 DB 가 아직 그대로다)**
```
옛 서버: docker rm -f kdb-shim tdb-shim && docker start kdb-app tdb-api tdb-worker
```
단 깨끗한 되돌리기는 **넘긴 직후 몇 분뿐**이다. 그 뒤엔 23번에 쌓인 것이 사라진다.

**아직 안 한 것 (맨 마지막)**
1. 23번에 nginx·TLS (없다. 컨테이너로 세우면 sudo 불필요)
2. DNS 를 23번 공인 IP(175.198.102.238)로 — 공유기에 80/443 포워딩 필요
3. 옛 서버 떼어내기: kdb-db·tdb-db(낡은 사본)·kdb-searxng·kdb-p1-restore-db 정리
4. 소비자 6개의 `KDB_API_URL` 을 공개 도메인으로 (또는 중계를 계속 쓴다 — 잘 돈다)

---

## 6. 작업 환경

- **저장소** `github.com/rickyjoo73/kdb` · 브랜치 `main`
- **배포** main push → GitHub Actions → **aiin23** (build → `migrations/*.sql` 적용 → `--no-deps kdb-app` 교체 → 헬스)
  - compose 조합은 `docker-compose.kdb.yml` + **`docker-compose.aiin23.yml`** (override.yml 은 옛 서버용)
  - `docs/p1`~`docs/p4` 의 SQL 은 `migrations/` 밖이라 **자동 적용되지 않는다**
  - CI 는 `kdb-db`·`kdb-searxng` 를 건드리지 않는다 — compose 값 변경은 수동 재생성 필요
- **운영 서버** `ssh aiin@atikar.com -p 38383` (aiin23) — scratchpad 의 `r3` 로 감싸 실행
  - 옛 서버는 `r` (`ssh -p 38371 kdb@114.203.210.38`) — 중계만 살아 있다
  - 22번은 `r2` (포트 38382). **OS 디스크 고장 중이라 쓰지 않는다**
  - 래퍼는 expect 다. **무응답이 길면 타임아웃으로 끊긴다** — 장시간 대기엔 하트비트를 찍어라
  - 23번에 node·go·psql·nginx 가 **없다**. sudo 는 암호를 요구한다 → 전부 컨테이너로 돌린다
- **격리 회귀** `bash scripts/run-isolated-regression.sh` (23번에서). 설계 근거는
  [`docs/KDB_REGRESSION_ENV.md`](KDB_REGRESSION_ENV.md).
  - template 은 **운영 전량 사본**이다. 종전엔 표기 1,249건짜리 옛 스냅샷이라
    "격리 회귀 통과"가 무엇에 대한 통과인지 말할 수 없었다(2026-09-14 발견)
  - 체크아웃은 워크트리가 아니라 **별도 클론**이고 SHA 를 대조한다 — 워크트리는
    배포 체크아웃과 브랜치가 겹쳐 조용히 옛 코드를 시험한 전례가 있다
- **관리자 UI** https://kdb.aiinplanet.com/admin · **소비자 API** `/v1/kentity/entities`

### 하지 않는 것
- 승인된 게이트 밖의 운영 DB 쓰기·마이그레이션·배포
- `sync-kdb-owner.js` 전량 실행 · 시험용 승인값(`prov-x`)의 운영 유입
- 표본 0건을 전체 0건으로 보고 · 문서에 비밀키 기록
- **다른 사이트 컨테이너 조작**

---

## 7. 실측이 잡아낸 내 오판 (같은 실수 반복 금지)

1. **"적재 비계 880MB 정리"** — 681MB 는 비계가 아니라 흡수분의 **자체 ID 앵커**
   (`basis_record_id`)였다. FK 세 개가 `ON DELETE RESTRICT`.
2. **`tourapi_ko 12` → 관광지** — 문서 기억으론 맞지만 이미 분류된 2,033건 중
   **heritage 1,170 · nature 739**. 세부 일치율 57.6%. 코드는 "장소다"까지만 말한다.
3. **`shared_buffers` 4GB 권고** — 가용 메모리를 재보고 1GB 로 철회했다.
4. **"3분에 3건 오류"** — `--since` 를 잘못 걸어 재시작 **전** 로그를 보고 있었다.
5. **③과 ④를 이어 붙이면 브랜드 12개가 사라진다** — 각 스크립트는 옳은데 이어 붙이면
   지키려던 대상을 잃는다. 스크립트 하나씩만 읽어선 안 보이는 자리다.
6. **⑤를 그대로 돌리면 ④를 되돌린다** — 범위 밖으로 뺀 45,734건에 분류 근거를 다시 쌓을
   뻔했다. "각 단계가 맞다"가 "이어 붙여도 맞다"를 뜻하지 않는다.
7. **되돌리기 처방을 시험하지 않고 문서에 적어 뒀다** — `classification_reason` 이 NOT NULL
   이라 적어 둔 처방이 실제로는 실패한다. 시험해 보고서야 알았다.
8. **격리 회귀가 5커밋 전 코드를 시험하고 있었다** — 워크트리 체크아웃 실패를 `|| true` 가
   삼켰다. "PASS" 가 **무엇에 대한 PASS 인지** 확인되지 않으면 PASS 가 아니다.

9. **`locale_ambiguous` 상관 서브쿼리가 match 를 느리게 한다** — 계획을 보니 **5번만**
   돈다. 범인은 2~3자 이름 6,468건마다 **정규식을 새로 컴파일**하는 필터였다.
   같은 종류의 결함을 앞서 고쳐 봤다는 것이 오히려 성급한 지목을 낳았다.
10. **22번 SMART 가 PASSED 니 디스크는 멀쩡하다** — 커널 로그에 진행 중인 I/O 오류가
   있었다. 운영자 피드백이 먼저 맞았다. **SMART 는 전부가 아니다.**
11. **`docker-compose.override.yml` 은 장비별 파일이다** — 추적되는 파일이었다.
   23번에서 덮어썼다가 `git checkout` 으로 되돌렸다. 하마터면 `git reset --hard`
   한 번에 조용히 사라질 뻔했다.
12. **172.19.0.0/16 을 23번에도 그대로 쓰면 된다** — `gemma4-moe_default` 가 쓰고 있었다.
   "Pool overlaps" 로 실패해서 알았다. 옛 장비의 주소를 새 장비에 그대로 옮길 수 없다.

13. **"격리 회귀 5/5 통과"라고 여러 번 보고했다** — 그 template 은 표기 1,249건짜리
   흡수 이전 스냅샷이었다(운영 2,686,762). 수치를 재 보고서야 알았다.
   **통과했다는 말은 무엇에 대고 통과했는지 말할 수 있을 때만 뜻이 있다.**
14. **회귀 template 이 낡은 줄 모른 채 계속 썼다** — 8번(옛 코드를 시험)과 짝이다.
   시험 환경 자체를 주기적으로 재지 않으면, 시험은 조용히 의미를 잃는다.

**전부 그럴듯했고 전부 틀렸다.** 기억·직관이 아니라 데이터에서 도출한다.
그리고 **하나씩은 맞는 것들을 이어 붙일 때** 특히 조심한다 — 5·6·7 이 그 자리였고,
**같은 종류의 결함을 전에 고쳤다는 기억**이 9번처럼 엉뚱한 지목을 낳기도 한다.
8·13·14 는 한 덩어리다: **검증 장치 자체를 검증하지 않으면 통과가 통과가 아니다.**

15. **"P4.02 는 여행 출처를 연결하는 일"이라고 읽고 시작했다** — 재어 보니 표기는 이미
   전부 들어와 있었고(en 341,796·ja 303,379), 충돌도 0건이라 규칙도 필요 없었다.
   막고 있던 것은 **출처가 아니라 대상 상태 두 가지**였고, 그것을 여는 코드 경로가
   wikidata 만 받아 자체 ID 앵커라는 계약 자신의 원칙과 어긋나 있었다.
   **과제 제목이 가리키는 곳과 실제 병목이 다를 수 있다.** 제목을 믿고 짓지 않고 먼저 쟀다.
16. **수요 기반 규칙이면 대상을 고를 수 있다고 봤다** — 302건을 고르는데 그 안에
   `아일랜드`(소비자는 **drama** 로 보냈다)·`돈`(**movie**)·법정동 `상원` 9건이 그대로 있었다.
   규칙은 선별을 **돕는** 것이지 대신하지 못한다. 그래서 스크립트에서 선별을 빼냈다.
17. **내가 만든 활성화 도구가 결함 인물 15,009명을 열어 줬다** — 여행 대상을 기준으로 쓴
   규칙을 인물에 그대로 적용한 것이 문제였다. 여행 출처(tourapi·aihub·seoul_dict)는
   **데이터셋 단위**로 권리가 정해져 항목 식별이 근거가 아니고, wikidata 는 **항목 단위**다.
   같은 규칙이 두 출처에 다르게 적용돼야 했다. 5·6·7 과 같은 자리다 —
   **조각마다 옳은 것을 이어 붙일 때 틀린다.** 도구를 만든 당일에 그 도구를 의심해서 잡았다.
18. **"흡수분 인물의 표기가 wikidata recorded 라 좋다"고 P4.02 결론에 적었다** — 맞는 말이지만
   근거 행을 열어 보니 `source_record_id` 가 QID 가 아니라 TDB UUID 였다(65,487/65,972).
   **provider 칸이 'wikidata' 라는 것과 그 표기가 추적 가능하다는 것은 다른 말이다.**
19. **회수한 QID 를 `export_allowed=true` 로 넣었다** — 그러면 DB 트리거의 "재사용 가능한
   정체성 근거"를 **그 QID 가** 만족시켜 대상이 **QID 때문에** 열린다. 실측: 회수 434건이
   전부 `tdb export=false / wikidata export=true` — 자체 관측은 자격이 없고 외부 항목이
   문을 열고 있었다. **운영자가 "QID 는 기본이 아니라 보조"라고 짚어 줘서 알았다.**
   고치려고 false 로 내렸더니 트리거가 외부 ID 까지 `withdrawn` 으로 만들어(677→243)
   방금 회수한 항목 지목이 날아갔다. **플래그가 아니라 문을 여는 쪽에서 강제해야 했다** —
   활성화 자격을 `자체ID근거` 로 바꿨다.
20. **"P5 = 동일성 판정"이라고 문서 9곳에 적었다** — P5 는 **단일 writer 전환**이고
   동일인 판정은 P2.05·P4.07 이다. 운영자가 "round 5 이니?"라고 물어서 확인하다 드러났다.
   **번호를 외워서 쓰면 안 된다. 원장을 열어 확인한다.**
22. **"기존 원장 보강을 공통 원장으로 흘리는 다리"를 만들려 했다** — 트리거가 막는다:
   `legacy names belong to original KDB writer`. kdb 소유 행에 표기가 0건인 것은
   **결함이 아니라 설계**였다(I04). 정해진 길은 채택(AdoptLegacy)이고 그것이 P5 다.
   **"비어 있다"를 "고장났다"로 읽지 않는다 — 계약을 먼저 본다.**
23. **wikidata 만 보고 "출처가 없다"고 했다** — 운영자가 "wikidata 만 wiki 가 아니잖아",
   "기존 kdb 는 여러 군데서 소스를 찾아 업데이트했잖아"라고 짚어 줬다. 세어 보니
   **어댑터가 22개**(kofic·kmdb·kopis·musicbrainz·itunes·discogs·mdl·ott·tmdb …) 있었다.
   **코드를 안 보고 데이터만 보면 이미 있는 것을 못 본다.**
24. **kowiki 수율을 기존 기록(10%)으로 가정할 뻔했다** — 표본을 재니 **1.0%** 였다.
   같은 도구라도 **대상 집단이 다르면 수율이 다르다**(기존은 미검증 K-엔티티,
   여기는 식당·숙박 POI). 남의 실측을 내 실측으로 쓰지 않는다.
25. **`git add -A` 로 관계없는 작업까지 main 에 밀어 넣었다** — 분류 현황 UI 가
   tourapi 마이그레이션 커밋에 섞여 **격리 회귀 없이** 배포됐다(CI 는 통과).
   브랜치 검증 절차를 스스로 정해 놓고 건너뛴 것이다.

21. **UI 를 만들지 않아도 일이 된 줄 알았다** — 근거·원장 계층만 고치고 화면을 안 건드렸다.
   운영자가 "머 크게 달라진 것이 없어"라고 했을 때 확인해 보니 공통 원장 관리 화면이
   **다 구현돼 있는데 메뉴에 링크가 없었다.**
   **화면이 없는 기능은 없는 기능과 구별되지 않는다.**

26. **이전 때 옛 `kdb-app` 이 붙어 있던 망 4개 중 1개만 옮겼다** — 새 중계(`kdb-shim`)를
   `dockers_backend` 에만 붙이고 끝냈다. 옛 컨테이너는 `mediafine_default` ·
   `kdb-platform_kdb_internal` · `kdb-platform_dockeraiinplanetcom_backend` 에도
   붙어 있었다. `mediafine_default` 전용 컨테이너에서는 **`kdb-app` 이름 자체가 안 풀렸다.**

   **이름 확인을 안 한 게 아니다.** 이전 절차(`cut.sh` 7단계)는 소비자와 같은 방식으로
   `curl http://kdb-app:9100/v1/health` 를 돌렸고 통과했다. 문제는 **그걸 한 망에서만**
   했다는 것이다. 바꿔 놓는 대상이 몇 개의 망에 붙어 있었는지를 **먼저 세지 않았다.**

   **교체 절차에는 "바꾸기 전 원본의 접점을 전부 열거한다"가 들어가야 한다.**
   한 곳에서 통과한 것은 한 곳에서 통과한 것일 뿐이다.

   조치: `docker network connect --alias kdb-app mediafine_default kdb-shim`
   (내 컨테이너만 붙였다. 남의 컨테이너는 재기동도 안 했다.)
   재발 방지: 중계 정의를 임시 파일이 아니라 `scripts/old-server-shim.sh` 로 원장에 넣었다.

27. **500개가 쌓인 임시 폴더에서 `full.sh` 라는 이름을 다시 썼다** — 스크립트를 만드는
   파이썬이 서식 오류로 죽었는데(`date +%H` 의 `%` 가 파이썬 서식으로 해석됐다),
   **파일은 안 만들어졌고 같은 이름의 9월 13일자 옛 파일이 대신 운영 서버에서 돌았다.**
   그 옛 파일은 회귀 실행기였고 안에 `pg_terminate_backend` 와 `DROP DATABASE` 가 있었다.

   피해는 없었다 — 대상 변수가 내 일회용 컨테이너(`kdb-p1-restore-db`)를 가리켰고
   운영은 표기 2,686,762·restarts=0 그대로였다. **운이 좋았던 것이지 설계가 막은 게 아니다.**

   두 가지를 바꾼다:
   ① 임시 스크립트 이름에 **날짜와 용도**를 박는다(`qidrun_20260914_a.sh`).
      `full.sh` · `run.sh` · `chk.sh` 같은 이름은 재사용 사고를 부른다.
   ② 스크립트 본문을 만들 때 **파이썬 `%` 서식을 쓰지 않는다.** `.replace()` 를 쓴다.
      본문에 `%` 가 들어갈 일이 흔하다(`date +%H`, `%s`, 백분율).
   그리고 생성이 실패했으면 **실행하지 않는다** — 파일 존재 여부를 확인한다.

28. **간체↔번체 자동 교정을 만들었다가 감사에서 세웠다 — 절반이 틀렸다**
   `사랑이 온다` 의 zh_hant 가 `爱情来了`(간체)인 것을 찾고 28건인 줄 알았다. 글자표로
   재보니 1,850건, opencc 로 재보니 992건(zh_hant 458 · zh 534)이었다. **세 번 다 다른 수**다.

   쓰기 전에 읽기 전용 감사(`kdb-app zh-variant-audit`)를 만들어 눈으로 봤고,
   **제안된 변환의 절반이 틀렸다**:
   ```
   박보영 朴寶英 → 樸寶英    朴은 번체에도 있는 한국 성씨 한자다. 樸은 '순박하다'
   박규리 朴奎利 → 樸奎利    (외 7건 더 — 朴씨 전부가 걸렸다)
   최리   崔里   → 崔裏      里=마을 / 裏=안쪽
   김기두 金基斗 → 金基鬥    斗=말·이름 / 鬥=싸우다
   ```
   `s2t(v) != v` 는 **"v 가 간체다"가 아니라 "v 를 간체로 읽을 수도 있다"** 였다.
   朴·里·斗·准·后·云 는 양쪽에 다 있는 글자다. opencc 는 그걸 모른다.

   **돌리지 않았다.** 돌렸으면 번체 칸의 한국 인명을 수백 개 망가뜨렸다.

   **교훈 두 개.**
   ① 결정적이라고 안전한 게 아니다. 한자 변환은 결정적이지만 **문맥 없이는 틀린다.**
      "외부호출 0 · 벌크안전" 이라는 말에 속으면 안 된다.
   ② **쓰기 전에 읽기 전용으로 세어 보는 도구를 먼저 만든다.** 이번엔 그게 값을 했다.
      숫자만 봤으면(992건) 그럴듯해서 돌렸을 것이다. **표본을 눈으로 봐야 했다.**

   남은 일: 간체가 확실한 것만 고른 목록을 손으로 만들어 교정한다. 자동은 안 된다.

29. **대응표가 존재하지 않는 값을 보고 있었다 — 문법 오류도, 시험 실패도 없었다**
   9-15 에 동명 가드에 유형 대응표를 넣었다. `k` 가 기존 원장이라고 생각하고 이렇게 썼다.
   ```
   OR (e.entity_type = 'work'  AND k.entity_type IN ('drama','movie','show','song_album','character'))
   OR (e.entity_type = 'event' AND k.entity_type = 'event_tour')
   OR (k.entity_type = 'term')
   ```
   그런데 `k` 는 `kentity_entities` 다. 거기엔 사전(`kentity_types`)의 canonical 13코드만
   들어간다. **drama·character·group·event_tour·brand_place·term 은 그 표에 존재할 수 없는
   값**이라 그 가지들은 한 번도 참이 되지 못했다. 죽은 가지를 **배포까지 했다.**

   실측 피해는 1짝(work↔work)뿐이었다. 동명 짝 5,179 의 유형 조합이 실제로는
   person↔person 3,341 · location↔work 768 · location↔person 630 … 이라
   죽은 가지에 걸릴 짝이 거의 없었기 때문이다. **운이 좋았던 것이지 설계가 막은 게 아니다.**
   그리고 0136 이 투영을 고쳐 character·event·concept 이 실제로 들어오기 시작하면
   그 가지들이 **조용히 엉뚱하게 맞기 시작한다** — 그때는 1짝이 아니다.

   **왜 아무도 못 잡았나.** SQL 문자열 안의 유형 이름은 컴파일러가 안 본다. 시험은
   "통과하는 것이 0이 아니다"만 확인했는데, 죽은 가지는 통과를 **늘리는** 쪽이라
   그 시험을 더 잘 통과시킨다. 결함이 지표를 좋아 보이게 만들었다.

   조치: 표를 kentity↔kentity 로 다시 쓰고, `TestHomonymTypeTableUsesOnlyRealTypes` 가
   대응표의 모든 유형 문자열을 `kentity_types` 사전과 대조한다.
   **교훈: 두 체계를 잇는 표를 쓸 때 양쪽이 각각 어느 표의 값인지 먼저 확인한다.**
   한쪽 이름을 다른 쪽에 써도 SQL 은 조용하다.

30. **브랜치에 커밋한다고 해 놓고 main 에 커밋했다**
   `git checkout -q -b anchor-withdraw` 로 브랜치를 만든 뒤, 그 사이 `git checkout main`
   으로 머지·배포를 하고 **main 에 머문 채** 다음 작업을 커밋했다. `git push origin
   anchor-withdraw` 는 브랜치 ref 를 밀기 때문에 **성공(up-to-date)** 을 반환했고,
   회귀는 옛 SHA(4b2b1d1)를 받아 **바뀌지도 않은 코드를 시험**했다.
   밀어넣기 전에 알아챘다(`git rev-parse origin/<branch>` 대조). 피해 없음.
   **조치: 커밋 전에 `git branch --show-current` 를 확인하고, 밀어넣은 뒤
   `origin/<branch>` 가 로컬 HEAD 와 같은지 대조한다.** 회귀 로그의 SHA 도 그래서 찍는다.

33. **자동 치환으로 코드를 고칠 때 실제 선언을 안 읽었다 — 두 번**
   ① `handlers_anchor_review.go` 에 `strings` import 를 넣는 치환이 **파일의 실제 import
      블록과 안 맞아** 조용히 아무것도 안 바꿨다. 빌드가 `undefined: strings` 로 깨졌다.
   ② `corrections` 목에서 `SearchAndFetch` 가 `(*Entity, *Candidate, error)` 를 돌려주는데
      둘만 돌려줬다. `not enough return values`.
   둘 다 회귀 build/vet 이 **CI 전에** 잡았다(배포엔 안 갔다).
   **치환 전에 대상 파일의 그 부분을 먼저 읽는다.** "이럴 것이다"로 고치지 않는다.

34. **내 시험이 15초를 먹어 같은 패키지의 다른 시험을 연쇄로 떨어뜨렸다**
   `TestRestoredSearchPrefersPrefixAndLiveRows` 가 **표본을 데이터에서 찾아내려고**
   537k 행에 상관 EXISTS 를 두 겹 걸었다:
   ```
   WHERE tok ~ '역$'
     AND EXISTS (... LIKE tok || '%')
     AND EXISTS (... LIKE '%' || tok || '%')
   ```
   그 한 질의가 연결을 15초 붙잡아 `-race` 에서 매번 **다른** 시험이 끊겼다
   (TestResolverApproval… · TestTDBShadow… · TestCatalogOverview…).
   패키지 시간이 브랜치 50.9s 대 main 29.7s 였다.

   ★처음엔 내 `Search` 변경을 의심했다. 재 보니 **안 느려졌다**(옛 정렬 97ms → 새 86ms).
     A/B(같은 DB에서 브랜치와 main 을 나란히)로 갈랐고, 그제야 시험이 범인인 줄 알았다.
   조치: 시험을 신고 사례(`익산역`)로 고정했다. Search 한 번이면 끝난다 —
   **시험이 곧 신고 내용**이라 읽기도 낫다. 18.3s 로 내려갔고 main 보다 빠르다.

   **교훈: 시험이 비싸면 시험이 결함이다.** 무엇을 지키는지만 보고 얼마나 드는지를
   안 봤다. 그리고 실패가 **매번 다른 시험**에서 나면 그건 그 시험들의 문제가 아니라
   **자원을 먹는 누군가**가 있다는 신호다.

35. **"구분값 미상"과 "유형 미상"을 흡수기가 섞었다**
   소비자가 "`익산역` 을 찾는데 GS25 익산역점만 나온다"고 했다. 역은 원장에 **있었다**:
   ```
   강남역  unknown/candidate   en `Gangnam Station` · ja `カンナム駅` · zh `江南站`
   분류사유: "TDB transit 선매핑. 구분값 미상 — 검수 대기(지어내지 않음)"
   ```
   `guardTyped` 가 `unknown` 을 공급에서 빼므로 **898건이 표기를 다 갖고도 한 번도
   소비자에게 나갈 수 없었다.**

   "어느 강남역인가"(구분값)를 모르는 것과 "그것이 장소인가"(유형)를 모르는 것은
   **다른 문제다.** 지어내지 않겠다는 원칙은 옳았는데 **적용된 자리가 틀렸다** —
   유형은 지어낸 것이 아니라 출처가 말한 것이다(`source_table='tdb_places'` +
   TDB transit 선매핑 판정).

   교훈: "모른다"를 기록할 때 **무엇을 모르는지**를 같이 적어야 한다. 한 가지를 몰라서
   다른 것까지 미상으로 두면, 아는 것마저 서빙에서 사라진다.

36. **유튜브 한 페이지가 브라질 매체 5곳으로 둔갑해 매체합의를 만들고 있었다**
   `site:domain` 은 요청일 뿐 보장이 아니다. 최근 30일 발굴로 가져온 **3,012건 전부**가
   요청한 도메인 밖 페이지였는데, `enqueueRaw` 는 **요청한 도메인**을 `source_domain` 으로
   적었다. 합의 질의는 `COUNT(DISTINCT source_domain)` 으로 "서로 다른 매체가 같은 말을
   했다"를 세므로, 한 URL 이 여러 이름으로 적히면 그 수만큼 세어졌다.
   ```
   구잘       pt-br "GuzalTV"  ← youtube.com 한 페이지 = 브라질 매체 5곳
   쉿(Shhh)   ja    "Shhh!"    ← store.steampowered.com (스팀 상점)
   이지혁     zh-hant "jh_lee_actor"  ← 인스타그램 아이디
   강수       zh-hant "降水"     ← 강수량을 다룬 위키백과
   ```
   실측: 683묶음 · 관측 2,426건 · 대상 168건, 그중 **151건이 원장 칸까지** 갔고,
   최근 90일 합의 통과 560건 중 **145건(26%)이 이렇게 만들어진 값**이었다.

   조치: ⑴ 요청한 도메인의 페이지만 받는다 ⑵ 관측 출처를 **실제로 읽은 페이지의
   호스트**로 적는다 ⑶ 합의가 **페이지도 센다** — 한 페이지는 여러 이름으로 적혀도
   한 곳이다 ⑷ 오염 82칸 회수.

   **교훈: 무엇을 세는지보다 세는 재료가 참인지가 먼저다.** 독립성을 세는 장치는
   독립성이 위조될 수 있는 자리를 반드시 같이 막아야 한다. 그러지 않으면 장치가
   **위조를 증폭한다** — 한 페이지가 다섯 배의 확신이 됐다.

37. **한 대상을 채우려고 가져온 페이지에서 추출기가 덤으로 주운 낱말이 원장에 들어왔다**
   ```go
   if entityID == uuid.Nil {           // 힌트에 없는 이름
       _ = s.Cand.Observe(...)         // 후보로 넣는다
   }
   ```
   우리는 `벡터` 하나의 일본어 표기를 찾으러 갔는데, 그 페이지에 있던 다른 고유명사들이
   후보가 됐다. 운영자가 "gemma 가 고유명사를 제대로 파악 못 해 kdb 가 오염된다"고 한
   현상의 실제 입구가 여기였다 — 고르는 쪽이 틀린 게 아니라 **고를 자격이 없는 자리에서
   골랐다.**

   조치: 힌트에 없는 이름은 버린다. 일반 기사 수집은 `KDB_ENABLE_RSS_POLLING=1` 을
   명시해야 켜진다(기본 꺼짐). 고유명사 분리는 소비자 쪽(GPT)이 하고 KDB 는 지목된
   것만 받는다 — `/v1/docs` 에도 그렇게 적었다.

38. **"검증됐다"를 "범위 안이다"로 읽어, 534건을 한 번에 되돌릴 뻔했다**
   기각인데 검증 등급이 붙은 행 534건을 보고 "누구인지 입증해 놓고 범위 밖이라 답하는
   모순"이라 판단했다. 표본 30건을 뽑아 보니 절반이 **기각이 맞았다** —
   `안규백`(국방장관) · `김어준` · `한병철`(철학자) · `한국어` · `판옥선` · `부천` · `경주시`.

   검증 등급은 **누구인지 안다**이고 범위는 **다룰 대상인가**다. 다른 축이다.
   43건을 한 줄씩 읽어 23건만 되돌렸다(활성 10 · 후보 13). `유튜브 채널` ·
   `미니앨범` · `더블 타이틀곡` · `2026년` · `215주` · `석권` 은 기각 유지가 맞다.

   **교훈: 큰 수를 한 규칙으로 처리하고 싶을 때가 가장 위험하다.** 규칙을 세우기 전에
   표본을 먼저 읽는다. 오늘 그 순서 덕에 511건을 안 건드렸다.

39. **template 이 운영보다 뒤처져, 시험이 SKIP 으로 조용히 넘어갔다**
   0135~0139 를 넣은 날, 회귀 template 은 그 표들이 없는 옛 스냅샷(82건)이었다.
   앵커 판정 시험은 "표가 없으면 건너뛴다"로 만들어 두었으므로 **SKIP** 됐고 회귀는
   초록으로 끝났다. 새로 고치자(89건) 그 자리에서 **진짜 결함 둘**이 나왔다:
   `/admin/entities/anchors` 500(0140 미적용) · **I01 위반 58건**.

   I01 쪽이 더 깊다. `homonymProvablyDistinct` 가 근거 둘을 `OR` 로 묶어 "유형이 안
   맞는다"가 혼자서 '다른 대상'을 선언했고, **같은 위키데이터 항목을 가리키는 쌍도
   갈려 나갔다.** 0136 으로 배역 3,946건이 `character` 로 돌아오자 드러났다 —
   실존 인물에 배역 앵커가 붙어 있던 계열(character 178 중 136)이 정확히 그 자리다.
   유형은 우리가 붙인 이름표, QID 는 세상이 붙인 항목이다. 어긋나면 **항목이 이긴다**.

   같이 고친 것: 회귀가 **미적용 마이그레이션을 배포와 같은 순서로 적용**한다.
   안 그러면 마이그레이션이 필요한 코드는 회귀에서 반드시 실패하고, 그 실패를
   "원래 그렇다"로 넘기는 습관이 생긴다. 그 습관이 생기면 진짜 실패도 같이 넘어간다.
   그리고 `DROP DATABASE ... 2>/dev/null` 이 오류를 삼켜 다음 줄의 CREATE 가
   "이미 있다"로 죽었다 — 원인은 한 줄 위에 있는데 화면엔 결과만 나왔다.

   **교훈: 시험 하나하나가 "내 전제가 이 환경에 있나"를 스스로 판단하면, 없을 때
   아무도 말해 주지 않는다.** 그 판단은 환경 쪽에서 한 번 크게, 시끄럽게 해야 한다.
