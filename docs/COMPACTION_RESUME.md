# 이어서 진행하기 — 2026-09-14

**이 문서는 대화 압축 뒤에도 방향을 잃지 않기 위한 것이다.** 새 세션은 여기부터 읽는다.
단일 실행 원장은 여전히 [`docs/KDB_INTEGRATION_TODO.md`](KDB_INTEGRATION_TODO.md) 다.

---

## 1. ★ 방향 — 운영자가 준 규칙 (절대 잃으면 안 된다)

1. **한 사람 = 하나의 ID.** 한 사람이 배우·가수·MC 를 겸하면 직업 행이 여러 개일 뿐 ID 는
   하나다. ID 가 갈리는 유일한 이유는 **다른 사람**이라는 것이다. 겸업으로 ID 를 쪼개지 않는다.
   → [식별 계약 §1.1](KDB_IDENTITY_CONTRACT.md)
2. **흡수할 때 ID·분류·구분값을 함께 부여한다.** 나중에 채울 칸으로 비워 두면 이름이 사실상
   키가 되고 같은 이름의 다른 대상이 한 UUID 로 합쳐진다. 구분되지 않으면 **지어내지 않고
   검수로 보낸다.** → [식별 계약 §1.2](KDB_IDENTITY_CONTRACT.md)
3. **wikidata QID 는 보조다.** 자체 ID 가 주 앵커다. QID 는 있을 때 얹어 빈 곳을 채우는 역할이지,
   없는 DB 를 채우는 역할이 아니다.
4. **관측이 먼저, 흡수가 나중이다.** 흡수가 원본 binding 이나 관측 해시를 지어내면 안 된다(D-37).
5. **있는 것을 제대로 통합하고 확장에 집중한다.** 소스 공백 문제는 나중 일이다.
6. **권리 판단은 운영자 몫이다.** 운영자가 "사용 가능"이라고 한 것을 내가 다시 게이트로 막지
   않는다. 기본 차단(S03)은 **승인 절차가 없을 때** 공급을 막는 장치이지, 이미 내려진 판단을
   되돌리는 장치가 아니다. (2026-09-14: "TDB 사용해 추후 더 모아 학습 사용할 거야.
   사용 가능하니 너가 판단하지 마.")
7. **AI 가 스스로 판단해 작동하는 시스템이다.** 없던 사람 담당자·승인 단계를 만들어내지 않는다.

---

## 2. 지금까지 한 일

**P0 · P1(G1) · P2(G2) · P3(G3) 인수 완료. P4 착수.**

- **TDB 전량 흡수 완료** — 536,322 / 536,322, 누락 0. 원본 UUID 전건 대조.
- **P4.00 표기 공급 개방 완료** — 상류 원천 정책 14종 승인(0128) 후 흡수 표기를
  `unverified` → `verified` 로 승격. 근거는 흡수 당시의 실제 관측에서 가져왔다.
- **D-38 수정(0129)** — brand·character 유형은 열려 있는데 세부유형이 닫혀 있어
  분류를 영원히 확정할 수 없었다. 열고 회귀 시험으로 재발을 막았다.

---

## 3. 다음에 할 일 (순서대로)

1. **P4.01 자동 보충** — 유형 미상 113,966 · 구분값 미상 39,781 을 줄인다.
   추측으로 채우지 않는다. 근거가 없으면 검수 대기로 남긴다.
2. **P4.03~P4.06 분야 공급** — 정치·경제·스포츠·사회/연예/문화 원천을 한정 파일럿으로 인수.
   사전(유형 13 · 세부유형 · 직군 26 · 분야 8)은 이미 전부 열려 있다.
3. **P5** — 실사용 API 와 단일 writer 전환. D-36(자동 병합 ↔ S04 상호 배타) 해소와
   이름 겹침 5,174건의 동일인 판정이 여기 들어간다.
4. **P6** — 24h 관측이 필요해 한 세션에서 끝나지 않는다.

---

## 4. 작업 환경

- **저장소**: `github.com/rickyjoo73/kdb`, 브랜치 `main`
- **배포**: `main` push → GitHub Actions (build gate → `migrations/*.sql` 적용 → 컨테이너 교체 → 헬스)
  - `docs/p1`·`docs/p2`·`docs/p3`·`docs/p4` 의 SQL 은 `migrations/` 밖이라 **자동 적용되지 않는다**
- **운영 서버**: `ssh -p 38371 kdb@114.203.210.38` — scratchpad 의 `r`/`rsh` 로 감싸 실행
  - 운영 DB `kdb-db`, 앱 `kdb-app`, 원본 `tdb-db`(**읽기 전용**), 격리본 `kdb-p1-restore-db`
  - 격리 회귀: `kdb_platform_migration_test` 를 `kdb` 템플릿으로 재생성 후
    `go test ./internal/... -run Restored` (golang:1.23-bookworm 컨테이너)
- **관리자 UI**: https://kdb.aiinplanet.com/admin — 흡수분 보기 `/admin/kentity?origin=tdb`
- **소비자 API**: `/v1/kentity/entities`, `/v1/kentity/entities/{id}` (`KDB_COMMON_ENTITY_ENABLED=1`)

### 하지 않는 것

- 승인된 게이트 밖의 운영 DB 쓰기·마이그레이션·배포
- `sync-kdb-owner.js` 전량 실행
- 시험용 승인값(`prov-x` 등)의 운영 유입
- 표본 0건을 전체 0건으로 보고하는 것
- 문서에 비밀키·API 키 기록
