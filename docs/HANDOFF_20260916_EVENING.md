# KDB 이어작업 — 2026-09-16 저녁 (오전분은 HANDOFF_20260916.md)

## 저녁에 배포된 것 (전부 회귀 통과)

| SHA | 내용 |
|---|---|
| 34557de | **요청 훅** — 소비자가 물은 낱말의 candidate 행을 그 자리에서 다시 민다 |
| 5b1b198 | 프롬프트 예시 누출 차단 + **인용 대조**(지어낸 근거로 승급 못 하게) |
| 542828c | **판정기가 제목만 읽고 있었다** — 네이버를 본문 유무로 부른다 |
| e6c11c7 | 네이버 예산 로그 (+542828c 동반 배포) |
| d89dc98 | **이미 서빙 중인 행에도 앵커를 찾아 준다**(active-anchor) |
| 493383f | **빈칸을 «현재 값이 정확»이라며 소비자 제안을 거절**하던 것 |

## 지금 미완 — 이어서 할 것

**`codex-restore` 브랜치 (2b46248)** — 회귀 돌리는 중이었다. 통과하면 병합·배포.

  운영자 지시: "나는 codex 를 추천해 5.6 sol low 로 설정하면 좀더 좋은 판단을 할거야"
  + "검수내용이 많아 너무 많이 사용되면 gpt 감당못하고, 간단히 짧게 사용하는내용이면 사용할수 잇지"

  한 것:
   · codexcli 실행 경로·Dockerfile·compose 3개 복원 (7374253 을 되돌림)
   · RoleProvider 의 codex→gemma 고정 해제 — 역할이 가리키면 따른다
   · **RunP** 추가 — 실제로 답한 공급자를 돌려준다(폴백해도 원장이 거짓말 안 하게)
   · **일일 상한 60회**(KDB_CODEX_DAILY_CALLS). 넘으면 멈추지 않고 gemma 로 내려간다
   · compose: KDB_LLM_CORRECTION=codex · CODEX_MODEL=gpt-5.6 · CODEX_EFFORT_CORRECTION=low
   · 폐기를 지키던 시험 3개를 새 계약 시험으로 교체
   · corrections: verify 가 (판정, 공급자, ok) 를 돌려준다 — Service 필드에 담으면 경합
   · review.go 재검증 경로에도 빈칸 가드

  ★남은 것: **auth.json 이 없다.** 운영자가 직접 넣기로 했다.
    경로 `/home/aiin/kdb/codex-home/auth.json`. 없으면 codex 호출이 실패하고
    gemma 가 답하며 원장에 그렇게 기록된다(조용히 죽지 않는다).

  ★왜 codex 를 이 자리에만 쓰나 (실측):
    정정 검증  하루 21건(최대 49) · 프롬프트 300~400토큰 · **근거 0** → 순수 지식 과제
    뉴스근거   하루 342건 · 스니펫 5건 → 읽는 과제, gemma 유지
    유입 분류  전량 → gemma 유지

## 저녁에 확인된 효과

```
근거 출처     구글 단독 84%(본문 0) → 네이버가 절반, 그 전부 본문 포함
인용 게이트   승급 6 / 보류 0 — 멀쩡한 걸 안 붙잡는다
앵커 감사     전 유형 5,958건 조회 · 어긋남 470건(type-mismatch 278 · human-on-character 139 …)
active 앵커   20건 신규(성남 FC·가요톱10·쇼미더머니6·넥슨게임즈·UBC울산방송…), 적중 6.7%
요청 훅       15분에 18회 발화 · 캡버림 0
오늘 요청     2,336 낱말 · ready 707(30%) · 유형힌트 95% · 기사문맥 95%
```

## 저녁에 찾은 것 — 손 안 댄 것

- **wikidata_locale_drain 이 en 을 다시 안 채운다.** 선별 조건의 «기계값 재채움» 목록이
  ja/zh/zh_hant 소스만 본다(verify 아님, wikidata_locale_drain.go:74-81).
  그래서 앵커를 새로 붙여도 **다른 locale 이 비어 있어야** 그 행이 집히고,
  en=gtranslate 만 남은 행은 영영 안 바뀐다. active-anchor 의 효과를 반감시킨다.
- **정정 검증에 소비자가 준 근거 URL 을 안 보여준다.** 3,857건 중 96%가 URL 을 갖고
  있는데(wikidata 1,745 · kstory 780 · en.wikipedia 224 …) allowlist 도메인이면
  LLM 없이 즉시 반영, 아니면 **버린다**. 기각 1,038건 중 926건이 URL 을 갖고 있었다.
  그 페이지를 읽어 프롬프트에 넣으면 기억 문제가 독해 문제로 바뀐다 — 모델 교체보다 먼저.
- **pending 정정 25건**(최고 2026-08-15). 22건이 근거 URL 보유. 운영자 심사 대기.
- **기계번역 서빙**: active 13,534 중 앵커 없는 5,137(기계번역 3,819).
  앵커 있음 기계번역 16% vs 없음 74%. active-anchor 적중률 6.7% → 전량 훑으면 약 250건.
- 기사 본문 영문 병기 추출은 **재 보고 버렸다** — 2,911건 중 값이 실제로 달라지는 것 32건(1.1%).
- 오전 인수인계의 나머지(소비자 ID 없는 68% · 함수 색인 · testdb 시험 59개 · 서버 이전)는 그대로.

## 저녁에 내가 틀린 것 (다음 사람이 같은 함정에 빠지지 않게)

1. **"제목만으로 승급된 건 없다"** — 스니펫이 비었는지만 보고 내용이 제목 반복인지 안 봤다.
   실제로는 84%가 제목만. Sonnet 에이전트 진단이 맞았고 내가 틀렸다.
2. **"OO 자리표시자 누출 9건"** — `COOL`·`BOYNEXTDOOR` 부분일치. 내 질의 오류.
3. **"대상이 100배"** — 적중률을 안 쟀다. 실제 8배(32 → 약 250건).
4. **배포 중 main push** — deploy.yml 이 서버 HEAD 와 DEPLOY_SHA 를 대조하는데 그 사이
   문서 커밋을 밀어 SHA 가 어긋나 배포 실패. 그리고 뒤 커밋이 «문서만» 으로 정상 스킵돼
   success 로 보여 **실패가 성공에 가려졌다**. 배포가 끝나기 전에 main 에 밀지 않는다.
5. **대조 규칙 오설계** — 판정문에서 고유명사를 뽑아 근거와 대조하려 했다. 운영 데이터
   2,026건에 걸어 보니 1,251건(62%)을 잡고 대부분 오탐(조사·표기변형·일반어).
   **코드로 옮기기 전에 SQL 로 재 본 것이 잡았다.**
6. **"장치는 있는데 아무도 안 켠"** — 오늘 일곱 번. 그중 셋은 내가 만들었다
   (요청훅 집계 · 네이버 예산 집계 · active-anchor CLI 배선).
