# API 요청 모니터 — 회차 기록

오너 지시(2026-09-20): *"api 로 모두 요청 들어오는것 모니터링하면서 개선할것 개선해 지속적으로"*.

매 회차 같은 다섯 개를 재고, **악화되었거나 새로 생긴 구멍 하나**만 고른다.
나열은 개선이 아니다. 고칠 것이 없으면 고치지 않고 그대로 적는다.

---

## 1회차 — 2026-09-20 14:30 KST

```
배포     kdb-app:ci-20260920-632fc6c · healthy · panic/fatal 0 (6h)
요청24h  prepare 771(고유 536) · lookup 176(129) · correction 67(23)
해소율   64.9%   (7일 고유 4,585 · 미해소 1,611)        ← 직전 측정 62.9%
기사준비 ready 229 / review 2,521 = 8.3%                ← 직전 측정 7.0%
정정24h  자동반영 39 · 기각 7 · 대기 5 / 막힌 proposed 9건(최고령 9/16)
```

해소율·준비율이 둘 다 올랐다. 같은 시각 다른 에이전트가 범위 재개(scope_reopen)를
배포했으므로 그 효과로 보이나, 인과는 확인하지 않았다 — 다음 회차에서 갈라 본다.

### 고치려다 **안 고친 것** — 위키백과 근거 자동 반영 레인

막혀 있던 정정 3867(`나 혼자 산다 / zh`)을 보고 「클라이언트가 위키백과 근거를 냈는데
집는 경로가 없다」고 판단해 레인을 하나 썼다. 그리고 배포 전에 실물을 셌다:

```
위키백과 근거가 달린 미결 정정      1건
그 레인이 실제로 적용 가능한 건     0건
최근 30일 위키백과 근거 정정 40건 → auto_applied 30 · rejected 9 · proposed 1
```

**기존 경로가 이미 이 부류를 처리하고 있었다.** 3867 이 멈춘 이유도 경로 부재가 아니라
**운영자 잠금**이었다:

```
나 혼자 산다  canonical_zh = "Home Alone" [operator-locked] · operator_locked = true
              canonical_zh_hant = 我獨自生活 [correction-verified]
```

잠금은 설계상 불변이고 자동 반영 대상이 아니다. 레인을 배포했으면 아무것도 안 잡는
**조용한 0건**이 됐을 것이다 — 이 저장소가 iTunes 1,025건으로 데인 그 모양이다.
커밋을 되돌렸다.

**남는 것은 운영자 결정 하나다**: 간체 칸에 영어(`Home Alone`)가 잠겨 있고 번체 칸에는
`我獨自生活` 가 있다. 소비자가 zh.wikipedia 근거로 정정을 신고했다. 잠금을 풀지 결정이 필요하다.

### 새로 찾은 것 — 간체 칸의 로마자 25건 (자동 수리 금지)

간체 칸이 라틴 전용인데 번체 칸에는 한자가 있는 active 행이 **25건**이다:

```
김민정   zh=Winter        zh_hant=金玟廷    ← Winter 는 다른 사람 예명이다
슬리피   zh=Sleepy        zh_hant=金聖源
조슈아   zh=Joshua        zh_hant=洪知秀
스텔라장 zh=Stella Jang   zh_hant=張星銀
우즈     zh=WOODZ         zh_hant=曹承衍
```

`zh_variant_repair` 는 한자↔한자 오배치만 보므로 이 부류를 못 본다. 그런데
**t2s 로 자동 변환하면 안 된다** — 번체 칸 자체가 오염된 행이 섞여 있다:

```
룰라(group)       zh_hant=第100天    ← 노래 제목
멜로디데이(group)  zh_hant=藝仁       ← 멤버 이름
아이코닉(song)     zh_hant=伊藤由美   ← 일본 가수 이름
```

옮기면 오염이 간체 칸으로 번진다. **먼저 25건의 번체 칸을 검수해야 한다.**
검수 뒤 깨끗한 것만 t2s 로 채우는 것이 순서다.

ja 칸도 같은 모양이 **236건**이지만 판단이 다르다 — 일본어는 로마자 예명이 실제로
쓰인다(WOODZ·MAYA). 같은 규칙을 적용하면 안 된다.

### 다음 회차에 볼 것

- 해소율 상승이 범위 재개 배포 때문인지 (유형별로 갈라 본다)
- 간체 칸 로마자 25건의 번체 칸 검수 — gemma 판정 대상으로 적합한가
- 기사 준비 review 2,521건의 `no_evidence` 비중 변화

---

## 2회차 — 2026-09-20 15:10 KST (회차 중 오너 지시: 묻지 말고 조사해서 시스템을 고쳐라)

### 근본 원인 — zhwiki 레인은 **구조적으로 한자를 못 가져온다**

1회차에서 찾은 「간체 칸의 로마자 25건」을 파고들었다. zh.wikipedia 표제가 이미 답을
갖고 있는데(`스텔라장 → 張星銀`) 왜 안 들어오는지 조건을 하나씩 갈라 봤다.

```
status/잠금 · 칸조건 · en있음 · URL있음 · QA통과   → 전부 통과
실제 기각 사유:
  기각 호시     (en-mismatch) 제목=權順榮 en=Hoshi
  기각 스텔라장 (en-mismatch) 제목=張星銀 en=Stella Jang
  기각 윤채     (en-mismatch) 제목=鄭尹彩 en=Yoonchae
```

`zhWikiVerdict` 는 **문서 제목이 canonical_en 과 다르면 기각**한다. 라틴 제목
(BIGBANG·GOT7)에는 맞는 앵커지만, **한자 제목은 정의상 en 과 같을 수 없다.**
그래서 이 레인은 이름이 「zh.wikipedia 제목 레인」이면서 한자 표기를 **한 건도**
가져오지 못한다. 매 회차 434건을 뽑아 0건을 쓴다.

9/19 에 「zh.wikipedia 제목이 있는데 기계값이 남았다 — 414건」으로 넓힌 것도
실제로 채운 것은 **라틴 제목뿐**이었을 가능성이 크다. 다음 회차에서 확인한다.

### 이번에 고친 것 — dry-run 이 거짓말하고 있었다

종전 dry 는 SELECT 를 통과한 것을 전부 「채움」으로 찍었다. UPDATE 에는 한자 조건이
더 걸려 있어 실제로 써지는 수와 달랐다:

```
종전 dry:  "채울 것 = 389건"
정직한 dry: "실제로 써질 것 = 0건"
```

389건 대부분이 `웨이커 WAKER→WAKER` 처럼 **같은 값**이거나 라틴→라틴이었다.
로그가 «389건 채움»이라 말하는 동안 원장은 한 칸도 바뀌지 않았다. 판단 근거가
거짓이면 그 위의 모든 결정이 틀어진다 — UPDATE 의 WHERE 를 Go 로 재현한
`zhWikiWouldApply` 하나를 dry 와 실제 쓰기가 **같이** 보게 했다.

### 함께 넣은 규칙 — 라틴 전용 값은 한자 표제가 덮는다

zh.wikipedia 표제(prio 6)는 wikidata-label(prio 5)을 못 덮는다. 위키데이터 라벨은
봇이 영어 라벨을 복사한 경우가 많아서, 중국어 칸에 로마자가 **영구히** 남는다.
등급은 그대로 두고 **값의 모양**을 조건으로 걸었다 — 현재 값이 라틴 전용이고 표제가
한자면 표제가 이긴다. 반대 방향(b16194b)은 그대로 막혀 있고 운영자 잠금도 불가침이다.
번체 표제는 t2s 로 변환해 간체 칸에 쓴다(9/17 에 87건 치운 「간체 칸의 번체」 재발 방지).

### 다음 회차에 할 것 (순서 확정)

1. **한자 제목의 앵커를 따로 만든다.** en 대조는 라틴 제목에만 쓰고, 한자 제목은
   보유 `canonical_zh_hant` 와 t2s/s2t 대조로 확인한다 — 일치하면 독립 확인 성립.
   `윤채`(제목 鄭尹彩 vs 보유 鄭允彩)처럼 **불일치는 기각**해야 한다(어느 쪽이 맞는지 모른다).
2. 9/19 의 414건이 실제로 무엇을 채웠는지 원장으로 확인
3. `김민정` 은 다른 문제다 — source_urls 에 `Winter (歌手)` 문서가 붙어 있다.
   aespa Winter 의 문서이고 김민정과 다른 사람일 수 있다. 앵커 오배치 점검 대상.

---

## 3회차 — 시스템 파악 (오너 지시: "제대로 작동되도록 시스템을 파악해")

레인 하나씩 고치는 것으로는 답이 안 나와서, **원장이 실제로 무엇으로 채워져 있는지**를
전수로 쟀다. 코드에 레인(`Drain*`)은 **46개**다. 원장은 이렇게 생겼다.

### 서빙 칸 11.3만개의 출처 구성 (active · 8 locale)

```
기계·파생   59.9%   gtranslate 23.7 · romanization 23.0 · codex-fallback 6.2 · opencc 4.9 · kana-rule 2.1
권위 API    29.8%   wikidata-label 19.9 · tmdb 8.0 · itunes 1.0 · musicbrainz 0.6 · discogs 0.2 · kofic 0.1
현지 실사용  3.2%   local-usage 1.9 · rss-observation 1.0 · media-consensus 0.3
정정·운영자  2.6%   correction-verified 2.0 · operator-locked 0.6
위키 보조    3.4%   wikipedia-zh-variant 2.2 · langlinks 1.2 · sitelink 0.1
```

**원장의 60%가 기계값이다.** 기획안(`ENTITY_DB_PLATFORM.md`)이 최우선으로 세운
«현지 매체 표기»(media-consensus, prio 2)는 **0.3%** 다.

### 선언은 있는데 원장에 0칸인 출처 33개

```
naver-people · mydramalist · tvmaze · namuwiki · naver-encyc · naver-search · kakao-search
melon · genie · bugs · vibe · qq-music · netease-music · tencent-music · komca · spotify
tving · wavve · watcha · coupang-play · viki · ott-official · broadcaster-official
official-page · cube-official · warner-japan · lollapalooza · yes24-livehall
baidu-baike · gemini-search · llm-provisional · gtranslate-raw(은퇴·정상)
```

일부는 「후보로 선언만」이 맞다(source_policy 가 수집 가부를 따로 정한다). 그러나
`mydramalist` 처럼 **레인 함수가 실제로 있는데 0칸**인 것이 섞여 있다. 레인이 있다는
것과 그 레인이 무엇을 채웠다는 것은 다르다 — 이 구분이 지금까지 없었다.

### ★내가 처음에 틀리게 읽은 것 — RSS 는 고장이 아니라 폐기다

```
kwave_news_whitelist  rss_url 보유 90곳 · poll_status='ok' 55곳
마지막 폴링 2026-07-03 23:32   (79일 전)
kdb-app env:  KDB_DISABLE_RSS_POLLING=1
```

이것을 보고 「기획의 심장이 멈췄다」고 적었다가 근거를 찾아보니 **의도적 폐기**였다:

- `f993d1c fix(discovery): 유튜브 한 페이지가 브라질 매체 5곳으로 둔갑해 매체합의를 만들고 있었다`
- `KDB_UNRESOLVED_BOTTLENECK_ALTERNATIVES_20260710.md` — 「상시 RSS 키워드 수집 = **의도적 폐기**」
- `tick.go:28` — 「on-demand 검색 발굴(research worker)로 대체 중」

매체 합의가 오염원이어서 끄고 on-demand 검색으로 갈아탔다. **기획 문서만 그대로다.**

### 그래서 진짜 문제 — 1안도 2안도 그 자리를 못 메웠다

```
폐기한 1안 (매체 합의)      0.3%
갈아탄 2안 (on-demand 검색)  2.1%   local-usage 1.9 + local-search 0.2
둘의 합                      2.4%
그 자리를 메운 것            기계값 59.9%
```

24시간 로그의 레인 활동도 같은 말을 한다 — `romanize 36 · localfill 11 · opencc 10 ·
ondemand 6` 으로 **파생·기계 레인만 돈다.** 권위 API 레인은 로그에 안 나온다.

**전어→鲭鱼(고등어) 는 이 구조의 결과다.** 현지 표기를 확보하는 경로가 2.4% 밖에
못 채우니 나머지를 기계번역이 메우고, 기계번역은 뜻을 옮기므로 종명·고유명사에서 틀린다.
레인 하나를 고쳐도 이 비율은 안 움직인다.

### 정정 — 2회차 표현

「zhwiki 레인은 한 건도 못 가져온다」고 적었는데 정확하지 않다. `wikipedia-sitelink`
칸이 **zh 에 110개** 있다(다른 locale 은 0). 한자 제목이 en-mismatch 로 막히는 것은
맞고, 지금 dry-run 이 0건인 것도 맞다. 과거에 라틴 제목으로 110칸을 채운 이력이 있다.

### 다음 회차 — 순서를 바꾼다

레인 하나 고치기(한자 앵커)보다 **비율을 움직이는 것**이 먼저다.

1. on-demand 검색 발굴이 왜 2.1% 에 머무는지 — 하루 처리량·성공률·병목을 잰다
2. 권위 API 레인(tmdb·itunes·musicbrainz·kofic)이 24시간 로그에 안 보이는 이유
3. 그 다음에 한자 앵커(2회차 과제)

---

## 4회차 — on-demand 발굴이 왜 2.1% 인가 (3회차 1번 과제)

### 답: 처리율이 하루 ~50건인데 대상이 1.5만 칸이다

```
발굴 큐 (14일)   유입 = 종결 100%  — 큐는 막혀 있지 않다
   결말 구성     활성화 22~26% · **후보 멈춤 59~61%** · 기각 2~8%
현지표기 생산량  하루 49~54건 (9/18~9/20 원장 기준)
대상             ja 기계 6,344 · zh 기계 5,467 · ja 빈칸 1,408 · zh 빈칸 2,152 = 약 1.5만 칸
```

하루 50건으로 1.5만 칸 → **300일.** 설정은 `KDB_LOCALFILL_BATCH=10 · FANOUT=3`.
큐가 막힌 게 아니라 **삽이 작다.** 그리고 퍼낸 것의 60%가 candidate 에서 멈춘다.

### 검색 0건률 — 퍼낸 것의 3/4 을 그 자리에서 버린다

앱 재배포 직후 48분 로그 기준 localfill 13건 중 **10건이 「검색결과 0건」**.
(주의: 처음에 이것을 「24시간 13건」으로 읽었다. 앱이 48분 전에 재시작해 로그가 그때부터다.
 실제 처리율은 로그가 아니라 원장에서 봐야 한다 — 위의 하루 50건이 그 수치다.)

### 이번에 고친 것 — sogou 제거

```
2시간 로그: sogou 689줄 · wikidata 281 · mojeek 18 · wikipedia 8 · baidu 2
sogou 의 suspend 줄: 0
오류: ErrorContext('searx/engines/sogou.py', 56, ..., 'AttributeError')
```

HTTP 실패가 아니라 **엔진 코드 안의 예외**라 SearXNG 자동 격리가 안 걸린다. 매 검색마다
다시 불려 매번 같은 자리에서 터진다(345줄/시). `searxng/settings.yml` 이 2026-08-08 에
qwant 을 뺀 기준(「자동 격리를 우회하는 엔진」)과 **같은 조건**이라 같은 처방을 했다.

**정직하게**: 이건 노이즈·지연을 줄이는 위생 수정이지 0건률을 고치지 않는다.
그리고 zh native 엔진이 baidu 하나만 남는다(baidu 는 격리 정상·생존).

### ★적용이 막혀 있다 — 설정이 다른 에이전트의 작업 공간에 있다

`kdb-searxng` 의 설정은 `/home/aiin/kdb/repo/searxng` **바인드 마운트**다. 그 디렉터리는
다른 에이전트가 쓰는 공유 배포 체크아웃이라 직접 고치지 않았다. 브랜치에만 담았다.
적용하려면 main 반영 후 `docker compose ... up -d --force-recreate kdb-searxng` 가 필요하다
(CI 는 `kdb-app` 만 건드린다).

### 다음 회차 — 비율을 움직이는 것

1. **후보 멈춤 60%** 가 최대 손실이다. 발굴이 끝났는데 왜 active 로 못 가는지 —
   근거 요건을 못 넘는 것인지, 넘었는데 승급 경로가 안 도는 것인지 가른다.
2. `KDB_LOCALFILL_BATCH` 상향의 안전 상한 — gemma 호출량·검색 부하로 계산한다.
3. 권위 API 레인이 24시간 로그에 안 보이는 이유 (3회차 2번 과제)

### 범위 정리 (오너 지시 2026-09-20)

일반명사 DB·정정 요청 처리는 **다른 에이전트 담당**이다. 이 브랜치의 taxon 레인
(`internal/kdb/taxon*.go`, `migrations/0150_taxon_name.sql`)은 그 영역과 겹치므로
**병합하지 않고 그대로 둔다.** 내 일은 요청 구간 모니터링과 자체 수정이다.

---

## 5회차 — 후보 멈춤 60% 의 원인 (4회차 1번 과제)

### 답: 순환 교착이다

```
최근 7일 신규    active 1,904 · candidate 976 · rejected 58
                 active    평균conf 0.71 · 검증등급 1,904/1,904
                 candidate 평균conf 0.40(전부 동일) · 검증등급 0 · 출처URL 1 · 시도기록 976/976
```

후보는 **시도를 안 한 게 아니다.** 976건 전부 `enrich_attempts` 기록이 있다.
발굴은 했고 **근거를 못 찾았다.** 승급 경로는 정상이다(active 는 검증등급 100% 보유).

그런데 표기를 채워 줄 레인들의 선정 조건이 전부 이렇다:

```
zhwiki · mdl · itunes · opencc   →   WHERE status='active'
실측: song_album 중 ja 빈칸   active 449 · candidate 263
```

**후보는 「근거가 없어서」 후보인데, 근거를 만들어 줄 레인이 후보를 안 본다.**
후보 → 근거 없음 → active 못 감 → 레인 대상 아님 → 근거 못 받음 → 계속 후보.

일부 레인은 후보 전용 승급 경로를 갖고 있다(`DrainITunesSongCandidates`,
`DrainTMDbCandidates`, `DrainMusicBrainzCandidates`). 배치는 `8` 이다(cmd/kdb/main.go).
**그 경로가 없는 유형이 문제다.**

### 멈춘 후보 976건의 유형 — 권위 소스가 없는 영역이 드러난다

```
song_album 213   iTunes/Discogs 있음 · Spotify 키 없음(오너 대기)
person     196   권위 API 없음 — wikidata 에 없으면 검색뿐
event_tour 120   권위 소스 없음
show        82   TMDb 있음
organization 75  권위 소스 없음
company     75   권위 소스 없음
agency      33   권위 소스 없음
character   31
```

`event_tour + organization + company + agency = 303건(31%)` 은 **후보 승급 레인
자체가 없는 유형**이다. 여기는 배치를 키워도 안 움직인다.

### 곁가지로 확인된 것 — 「선언만 된 출처」의 정체

`SourceNaverPeople` 은 상수와 우선순위 표에만 있고 **값을 쓰는 레인이 없다.**
NAVER 키는 설정돼 있지만 검색 보조(localfill)로만 쓰인다. 3회차의 「0칸 출처 33개」 중
적어도 일부는 이렇게 «선언만» 이다 — 레인이 있다는 것과 다르다.

### 이번 회차 산출물

`scripts/kdb-candidate-gap.sh` — 위 세 가지(근거 보유·시도 여부·레인 사정거리)를
한 화면에 본다. 매 회차 같은 질의를 새로 짜던 것을 끝낸다.

### 다음 회차

1. **후보 승급 레인이 없는 유형 303건** — organization/company/agency 는 공식 사이트가
   1차 근거여야 한다(로드맵 6위와 같은 판단). 가장 싼 경로를 하나 고른다.
2. `KDB_LOCALFILL_BATCH` · 후보 승급 배치 `8` 의 안전 상한 계산
3. person 196 — wikidata 에 없는 인물을 무엇으로 채울지

---

## 6회차 — 권위 소스 없는 303건 (5회차 1번 과제)

### 먼저 찾은 것: 레인 유형 목록에 둘이 빠져 있었다

```
OrgAnchorTypes = political_party · government_body · company · organization ·
                 sports_team · school · game · musical_play · webtoon · publication
```

`agency` 와 `event_tour` 가 없다. 실측으로도 그대로였다:

```
company 75/75 wdorg 시도완료   organization 75/75 시도완료   ← 레인은 정상, 대상이 없던 것
event_tour 시도 0건 (후보 풀 141)   agency 시도 1건 (후보 풀 45)  ← 문이 닫혀 있었다
```

P31 관문 표에는 두 유형이 **이미** 있다(agency 6 QID — record label·entertainment
agency 등 / event_tour 5 QID — concert·concert tour·festival). 관문은 준비됐는데
목록에만 빠진 명백한 누락으로 보였다. 그래서 추가하고 dry-run 을 돌렸다.

### 그리고 되돌렸다 — 실측 수확 0/50

```
event_tour  25건 검사 → 앵커 0 (NoHit 18 · NameMismatch 3 · TypeUnknown 2 · TypeMismatch 1)
agency      25건 검사 → 앵커 0 (NoHit 22 · QIDTaken 1 · NameMismatch 1 · TypeUnknown 1)
```

표본이 답을 말한다:

```
event_tour: 부산카페쇼 · 전국기능경기대회 · 2026 농식품 창업콘테스트 · 세계불꽃축제
agency:     해바라기 엔터테인먼트 · 스토리제이컴퍼니 · 쿼터뮤직 · 하이지음스튜디오
```

지역 행사·시상식·소형 기획사다. **위키데이터에 없는 것이 정상이다.** 목록에 넣으면
186건에 대해 검색을 태우고 앵커는 0건을 얻는다 — 또 하나의 조용한 0건이 됐을 것이다.
**추가를 되돌렸다.**

### 그래서 확정된 것 — 303건은 위키데이터로 못 푼다

NoHit 40/50(80%). `organization·company·agency·event_tour` 는 위키데이터 **밖에**
있다. 로드맵 6위(「government_body/organization 근거 규칙 — 뉴스 대신 공식 사이트를
1차 근거로」)가 추측이 아니라 실측으로 확인됐다. 이 유형들은 **공식 사이트·사업자
정보가 1차 근거**여야 한다. 같은 유형 active 도 gtranslate 가 60% 다
(agency 246/412 · event_tour 417/686) — 지금은 기계번역이 기본값이다.

품질 실례:
```
세트렉아이       → Setrek-i                          (공식명 Satrec Initiative)
밥상공동체 연탄은행 → Bapsang Community Briquette Bank   (직역)
```

### 곁가지 신호 — 중복 QID 1건

`FANTAGIO Entertainment [agency] → Q13424955 를 다른 행이 이미 쓴다`.
같은 대상이 두 줄로 있다(병합 대상). dry-run 로그가 이것을 스스로 알려 준다 —
레인에 `QIDTaken` 계수기가 있어서다. 이 계수기를 보는 화면이 없다.

### 다음 회차

1. **공식 사이트 1차 근거 경로** — 사업자 정보(공공데이터 키 필요)와 도메인 추정 중
   어느 쪽이 싼지 실측한다. 오너 대기 항목(data.go.kr)과 겹치는지 확인한다.
2. `QIDTaken` 같은 레인 계수기를 모아 보는 자리 — 지금은 dry-run 을 돌려야만 보인다.
3. person 196건(5회차 3번 과제)

---

## 7회차 — 위키데이터로 못 푸는 것의 **대안** (오너 지시: 리서치해서 해결방안을 찾아라)

6회차에서 「303건은 위키데이터 밖에 있다」까지 갔다. 거기서 멈추면 아무것도 안 바뀐다.
**이미 키를 가진 것부터** 찔렀다.

### 실증 ① NAVER 검색 API — 키를 이미 갖고 있다

`KDB_NAVER_CLIENT_ID/SECRET` 은 설정돼 있고 지금은 검색 보조로만 쓰인다. 조직 이름으로
부르면 **공식 사이트가 1순위로 나온다**:

```
세트렉아이           → satreci.com
소니뮤직엔터테인먼트코리아 → sonymusic.co.kr
하이브 레이블스        → YouTube 채널 "HYBE LABELS"
```

### 실증 ② 한국 기업 사이트는 title 에 한글·영문을 병기한다

```
satreci.com       <title> 쎄트렉아이 - SATREC INITIATIVE     ← 우리 값은 Setrek-i (gtranslate)
sonymusic.co.kr   og:site_name  Sony Music Entertainment Korea
```

한국어 원문이 같은 title 에 있으면 **「이 사이트가 그 대상의 것」이라는 앵커가 공짜로
성립**한다. 영문명과 앵커를 한 번에 얻는다.

### 파일럿 12건 — 그리고 판별식이 실측에서 나왔다

후보(organization·company·agency) 12건으로 돌렸다. 영문명 확보 6건, 그중 **2건이 오매칭**.
그런데 오매칭 2건은 성격이 같았다 — **제3자 플랫폼 페이지**다:

```
채택  메이아이       → mAy-I                  may-i.io/
채택  픽스업헬스      → PhyxUp Health          phyxuphealth.com/
채택  KICCA        → Korea Institute Of Certified Cost Analyst   kicca.or.kr/
채택  LIG디펜스앤에어로스페이스 → LIG Defense&Aerospace   ligdefenseaerospace.com/
기각  이타디엑스      → InnoForest             innoforest.co.kr/company/CP0…   스타트업 DB
기각  케이바이오게이트웨이 → Wbiz                 wbiz.or.kr/biz/areaDetail.do    중소기업 포털
```

**공식 사이트는 루트에서 자기 이름을 보여주고, 제3자 DB 는 경로 안쪽에 싣는다.**
`URL 경로 깊이 ≤ 1` 조건 하나로 오매칭 2건이 정확히 걸러진다 — 4/4 정확, 오매칭 0.

도메인 일치(영문명의 알파벳이 호스트에 포함)만으로는 **안 된다** — InnoForest·Wbiz 도
그 조건을 통과한다. 한국어 병기 앵커도 단독으로는 안 된다(제3자 DB 페이지에도 한국어
회사명이 있다). **경로 깊이가 결정적이다.**

### 설계는 이미 있고 레인만 없다

`SourceOfficialPage`("official-page", prio 4) 상수가 **이미 선언돼 있다.** 3회차에서
「원장에 0칸인 출처 33개」로 찾았던 그것이다. 채울 레인만 없다.

```
후보(organization·company·agency)
  → NAVER 검색(키 보유)  → 1~3위에서 제3자 도메인 제외
  → 경로 깊이 ≤ 1 인 페이지만  → title/og:site_name 에서 라틴 추출
  → 한국어 원문 병기 확인(앵커)  → canonical_en = 값, source='official-page'
```

### 다른 대안들의 상태 (실측)

| 원천 | 상태 | 비고 |
|---|---|---|
| **NAVER 검색** | **키 보유·작동 확인** | 위 경로. 지금 쓸 수 있는 유일한 것 |
| DART OpenAPI | 키 필요 (`status 010 등록되지 않은 인증키`) | **무료·자동 발급.** 한국 법인의 공식 **영문명 전량**을 준다 — 기업 유형에 가장 정확하다 |
| data.go.kr 사업자정보 | 키 필요 | 이미 오너 대기 목록 |
| GBIF | 키 불필요 | 생물종 전용(5회차 범위 밖) |

**DART 키는 받을 가치가 크다** — company 유형은 검색 추정이 아니라 법정 영문명으로 끝난다.

### 다음 회차

1. 위 레인을 구현한다(`official-page`). 배포 전 dry-run 으로 수확과 오매칭을 센다.
2. 제3자 도메인 목록은 실측으로 채운다(namu.wiki·saramin·jobkorea·innoforest·wbiz…).
3. person 196건은 이 경로가 안 맞는다 — 따로 본다.

---

## 8회차 — **스스로 성장하는 시스템**의 첫 조각 (오너 지시)

> *"너의 역할은 시스템을 제대로 구축하는 것이다. 요청과 답변을 보면서 우리 KDB 가
> 부족한 것을 빨리 찾아 답변하도록 개선하는, 스스로 성장하는 시스템을 구축해야 한다."*

맞는 지적이다. 7회차까지 나는 매 회차 손으로 SQL 을 파서 결핍을 찾았다. 그건
시스템이 아니라 내 노동이고, 내가 없으면 멈춘다.

### 무엇부터 지어야 하나 — 반복해서 데인 자리가 답이다

하루 동안 같은 병으로 세 번 데였다. 전부 **「레인이 무엇을 했는지 어디에도 안 남는다」**:

```
zhwiki    dry-run 이 「389건 채움」이라 말하는 동안 원장은 0건 바뀌었다
localfill 로그 13줄을 24시간치로 읽었다 — 앱이 48분 전 재시작한 것이었다
org-anchor 유형 둘을 넓혔는데 실제 수확은 50건 검사에 0건이었다
```

로그는 앱 수명만큼만 살고, 0건일 때 아예 안 찍는 레인이 많다. **안 돈 것과 돌았는데
0건인 것을 구별할 수 없다.** 이 구별이 없으면 다음 판단이 전부 추측이 된다.

### 지은 것 — 레인 성과 원장 (0151 + lane_ledger.go)

세 수를 **절대 섞지 않고** 적는다.

```
scanned  선정 쿼리가 뽑은 행
applied  원장이 실제로 바뀐 수     ← 이 둘을 섞은 것이 389건 거짓말의 원인이었다
skipped  뽑았지만 안 쓴 수 + 사유별 계수(jsonb)
```

DB 제약으로 `applied <= scanned` 를 강제한다. 기록이 실패해도 레인은 계속 간다 —
계측기가 본체를 죽이는 것이 가장 나쁘다.

### 끝단 확인 — 원장이 **스스로 진단을 말한다**

복원 DB 에 붙여 zhwiki 레인을 dry 로 돌렸다:

```
zhwiki-title [dry] ★조용한0건: scanned=429 applied=0 skipped=429
  reasons={"바꿀 근거 없음":349, "en-mismatch":42, "bad-url":35, "multi-url":3}
```

**2회차에 내가 하루 걸려 찾은 `en-mismatch` 를 이제 한 줄이 말한다.** 그리고
`IsSilentZero(runs, scanned, applied)` 는 순수 함수라 시험이 규칙을 고정한다 —
한 회차 0건은 정상이고(대상이 없을 수 있다) **여러 회차 뽑기만 하는 것**이 병이다.

`LaneHealthSince(24h)` 로 「조용한 0건인 레인」이 질의 한 줄로 나온다.

### 이것이 성장 고리의 어디인가

```
요청 로그 ─→ 결핍 진단 ─→ 공급원 선택 ─→ 레인 실행 ─→ [수확 측정] ─┐
   ↑                                                              │
   └──────────────── 다음 대상 선정 ←─────────────────────────────┘
```

`[수확 측정]` 이 비어 있어서 고리가 끊겨 있었다. 그 자리를 메웠다. 남은 것:

1. **나머지 레인에 배선** — zhwiki 하나만 붙였다. 46개 레인 전부가 같은 원장을 쓰면
   「어느 레인이 헛도는가」가 매일 자동으로 나온다.
2. **결핍 진단의 자동화** — 지금 요청 로그를 SQL 로 파는 부분. 유형×locale 결핍
   순위를 상시 표로 두면 레인 선정이 그 순위를 따라간다.
3. **화면** — `/admin/health` 에 두 표를 올린다. 3회차에서 「장치는 있는데 아무도
   안 켠 경우를 하루에 여덟 번 만났다」고 적은 것의 처방이다.

---

## 9회차 — 레인 배선 확산 + 자동 신호

8회차에서 원장을 만들고 zhwiki 하나에만 붙였다. 나머지에 확산했다.

### 배선 방식 둘

레인마다 사정이 다르다. 두 가지로 나눴다.

```
① 계수기가 없는 레인  NewLaneRun → Scan/Apply/Skip → defer Record   (zhwiki · opencc)
② 계수기가 이미 있는 레인  RecordCounts(가진 수를 그대로 옮긴다)      (org-anchor · kowiki)
```

②를 따로 둔 이유: org-anchor 는 사유별 계수를 이미 열 개 갖고 있다. 거기에 Scan/Apply 를
다시 심으면 **같은 것을 두 번 세게 되고, 두 수가 갈라지면 어느 쪽이 맞는지 알 수 없다.**

### 끝단 — 정상 레인과 병든 레인이 한눈에 갈린다

```
opencc:canonical_zh_hant  scanned=8  applied=4  {"문자셋 규칙 위반":2,"상위 출처라 못 덮음":2}
opencc:canonical_zh       scanned=0  applied=0  {}            ← 대상이 없다(신호 아님)
org-anchor [dry]          scanned=0  applied=0  {}
zhwiki-title              scanned=349 applied=0 {"바꿀 근거 없음":349}   ← 조용한 0건
```

`scanned=0` 은 병이 아니라 대상이 없는 것이다 — 판정이 그 둘을 정확히 구분한다.
opencc 가 「건강한 레인은 이렇게 생겼다」의 본보기다: 뽑고, 쓰고, 왜 못 썼는지 말한다.

### 자동 신호 (물음: 「신호가 올라오면 자동으로 잡을 수 있도록 설정했나?」)

8회차 시점의 정직한 답은 **아니오**였다. 기록만 하고 잡는 것은 사람이었다. 그래서 붙였다:

- **기록하는 그 자리에서 판정한다.** 스케줄러를 새로 두지 않았다 — 「장치는 있는데
  아무도 안 켠」 경우를 하루에 여덟 번 만났기 때문이다. 레인이 돌 때마다 스스로 본다.
- **3회차 연속부터 울린다.** 한두 회차 0건은 대상이 없던 것이다. `CountSilentStreak`
  는 순수 함수고 시험이 경계를 고정한다 — 임계가 1이면 경보가 늑대소년이 된다.
- **`GET /v1/health` 에 `lanes_silent` 를 싣는다.** 로그는 앱 수명만큼만 살고 아무도
  안 본다(backlog-watch 가 같은 이유로 조용했다). 헬스는 이미 주기적으로 불린다.
  700ms 예산으로 따로 돌리고 실패하면 필드만 빼고 나간다.

실증(복원 DB, non-dry 3회차): 1·2회차 기록만 → **3회차에 `★신호` 발화** →
`SilentLanes(24h) = [zhwiki-title]`.

### 배포 경로를 갈랐다

`feat/lane-ledger` — origin/main 에서 새로 따서 **taxon 레인을 완전히 뺀** 브랜치다
(`taxon_name`·`LocaleFillHintFor` 참조 0건 확인). 단독으로 빌드·vet·전체 시험을 통과한다.
일반명사 DB는 다른 에이전트 담당이므로 그쪽과 섞이지 않게 한 것이다.

담긴 것: 레인 원장(0151)·자동 신호·헬스 노출 · zhwiki dry 정직화와 라틴 규칙 ·
searxng sogou 제거 · 후보 교착 진단 스크립트.

**main 에 병합되는 순간부터 운영에서 자동으로 쌓이고 울린다**(CI 가 migrations/*.sql 을
적용한다). searxng 설정만 컨테이너 재생성이 따로 필요하다.

### 다음 회차

1. 남은 레인 배선 — itunes · localfill · romanize · mdl · tmdb
2. **결핍 진단 자동화(0152)** — 「유형×locale 별 지금 못 답하는 수요」를 상시 표로.
   지금은 내가 매 회차 SQL 로 판다. 이게 성장 고리의 [결핍 진단] 자리다.
3. `/admin/health` 에 두 표(레인 건강·결핍 순위)

---

## 10회차 — 병합·배포. 그리고 내가 만든 사고 둘

오너 지시: 「커밋하고 병합해서 제대로 작동되게 해」.

### 사고 ① 회귀 template 에 직접 썼다

끝단 확인을 하며 복원 DB 에 non-dry 로 레인을 돌렸다. 그런데 그 DB 가
**회귀의 template** 이었다:

```
scripts/run-isolated-regression.sh:  CREATE DATABASE kdb_platform_migration_test TEMPLATE kdb
내가 쓴 곳:                          kdb-p1-restore-db 의 kdb  ← 바로 그 template
```

첫 회귀에서 `TestEntityCenterAgainstRestoredInventory` 가 실패했다. 내 코드 탓인 줄
알았는데, template 을 정리한 뒤 실패한 시험만 양쪽에서 돌리니 **둘 다 통과**했다:

```
main (632fc6c)            ok internal/kdbadmin 2.420s
feat/lane-ledger (341a778) ok internal/kdbadmin 2.475s
전체 회귀 main             33/0 · race 31/0 · 전부 통과
```

즉 실패는 코드가 아니라 **내가 만든 환경 오염**이었다. 바꾼 4행은 확인해 보니 운영과
같은 값이었고, 내가 만든 `kwave_kdb_lane_runs` 표를 template 에서 지웠다.
**앞으로 끝단 확인은 template 이 아니라 일회용 사본에서 돌린다.**

### 사고 ② searxng/settings.yml 은 git 으로 바꿀 수 없다 — 배포가 죽었다

병합 직후 배포가 이 줄에서 멈췄다:

```
error: unable to unlink old 'searxng/settings.yml': 허가 거부
drwxrwxr-x 2 977 977  /home/aiin/kdb/repo/searxng     ← 컨테이너 사용자 소유
배포 계정 aiin 은 uid 1000 — 그 디렉터리에 쓰기 권한이 없다
```

파일을 지우려면 **디렉터리** 쓰기 권한이 필요하다. 그래서 내용이 무엇이든 이 경로는
git 으로 못 바꾼다. 4회차에 「설정이 공유 체크아웃에 바인드 마운트돼 있다」고 적어
두고도 그 함의를 끝까지 따라가지 않았다.

더 나쁜 것은 **배포가 중간에 끊겨 공유 체크아웃이 더러워진 것**이다(추적 파일 12개
변경 + untracked 6개). 다른 에이전트가 쓰는 자리다. 즉시 복구했다 —
`git checkout -- .` 후 **내 커밋 소속 untracked 만 골라** 제거했다(남의 것은 안 건드린다).

처방: sogou 제거를 되돌렸다(ca0aec5). 진단은 유효하다(sogou 가 자동 격리를 우회해
345줄/시). 다만 **디스크만 고치는 것도 안 된다** — git 버전과 달라지는 순간 다음
배포가 같은 자리에서 또 죽는다. 올바른 해법은 디렉터리 소유권 복구(sudo)나 설정
주입 방식 변경이고 **운영자 판단이 필요하다.**

### 결과 — 운영에서 작동한다

```
kdb-app:ci-20260920-ca0aec5 · healthy · panic/fatal 0
migrations/0151 적용됨 (kwave_kdb_lane_runs 존재)
GET /v1/health 정상 (lanes_silent 없음 = 아직 신호 없음)
```

레인이 한 바퀴 돌면 원장이 쌓이고, 3회차 연속 조용한 0건이면 헬스에 이름이 나간다.

### 함께 잡은 신호

- **기사 준비 `ready` 8시간째 0건**(review 27 · `no_evidence` > `resolved`). 열려 있다.
- **정정 「유실」은 내 오보였다.** 13시 4건은 `미래시: 보이지 않는 미래` 한 낱말이
  4행이고 `item_status=preparing` — 접수 여부가 아니라 **대상의 요청 시점 상태**다.
  다만 7일 `preparing` 195건은 「근거를 들고 왔는데 받을 그릇이 없다」는 뜻이라
  corrections 담당에게 신호로 넘긴다.

### 다음 회차

0152(결핍 원장)를 빌드·시험해 올린다. 코드는 썼고 아직 검증 전이라 이번 병합에서 뺐다.

---

## 11회차 — 시스템이 스스로 다음 대상을 말했다

### 성장 고리가 닫혔다

```
결핍 원장 360칸 · 최근 측정 17:28:30   ← 내가 부르지 않았다. 레인 기록에 업혀 스스로 돌았다
레인 원장  zhwiki-title 2회차 824건 뽑아 0건   ← 같은 곳을 가리킨다
헬스       lanes_silent 없음(아직 2회차 — 3회차부터 신호). 판정이 정확하다
```

시스템이 말한 결핍(내가 1회차에 손으로 판 것과 같은 결론):

```
unmet      person 367낱말/566요청 (이재명·이재용·조국) · song_album 342/487 · show 120/154
locale_gap person zh_hant 299/412 (결이·구름·최보필) · show zh 82/149 · song_album zh 114/145
```

`person unmet` 은 다른 에이전트의 범위 작업과 겹치므로, 내 영역인 `person zh_hant` 를 잡았다.

### 계획한 처방이 실측에서 0건이었다 — 쓰기 전에 쟀다

2회차에 찾아 둔 「en-mismatch 게이트가 한자 제목을 구조적으로 기각한다」를 고치려 했다.
앵커를 보유 zh_hant 대조로 바꾸는 설계였다. **코드를 쓰기 전에 수확을 쟀다:**

```
zh위키 URL 보유 3,657 · 번체 보유 3,214 · **번체있고 간체빈칸 0**
person 결핍의 실체: zh·zh_hant **둘 다 빈칸 515건** · 번체만 있음 2건
둘 다 빈칸 515건 중 — zh위키 URL 4 · 위키데이터 QID 156 · ko위키 URL 135
표본: 결이(QID 0·URL 0) · 구름(0,0) · 최보필(0,0)
```

앵커로 쓸 zh_hant 가 없다. zhwiki 레인을 고쳐도 상한이 **4건**이다. 안 썼다.

### 그래서 이 결핍은 「채우는 문제」가 아니다

`결이·구름·최보필` 은 어느 외부 DB 에도 없다. 한국 인물의 한자 이름은 **본인의 한자**라
규칙으로 만들 수 없다 — ja 는 kana-rule 이 있지만 zh 는 그런 규칙이 없다(KDB 문서가
이미 인정한 한계다). 그러면 처방은 「채운다」가 아니라 **「못 채운다고 정확히 말한다」** 다.

### ★그런데 그 말하기가 고장나 있다

```
둘 다 빈칸 person 515건 — zh 시도 기록 439건(85%) · **소진(exhausted) 표시 5건(1%)**
소비자 응답(24h): ready 340 · preparing 139 · review 110 · new 108 · unfillable 16
```

**439건이 시도해서 실패했는데 소진 표시가 안 붙는다.** 그래서 소비자에게 계속
`preparing` 으로 나간다 — 기다리면 채워질 줄 알고 폴링하지만 영원히 안 채워진다.
`unfillable` 상태도 `ExhaustedLocales` 도 이미 있다. **장치는 있는데 표시가 안 붙는 것**이
병이고, 이 저장소가 반복해 만난 그 모양이다.

### 곁가지 — 다른 에이전트가 0150 을 썼다

`7b44122 fix(mig-0150)` · `c1e7324 feat(common-noun): 일반명사에 제 구간을 주고…`.
일반명사 DB 가 올라갔고 마이그레이션 0150 도 그쪽이 가져갔다. 내 taxon 브랜치
(feat/category-sources)의 0150 은 번호가 충돌한다 — 어차피 병합하지 않기로 한 것이라
그대로 둔다. **그 영역은 그쪽이 닫았다.**

### 다음 회차

`MarkFillAttempt` 가 exhausted 를 언제 세우는지 보고, 반복 실패한 칸이 소진으로
표시되게 한다. 그러면 소비자가 `preparing` 대신 `unfillable` 과 `fill_hint` 를 받는다.
배포 전 dry-run 으로 「몇 건이 preparing→unfillable 로 바뀌는가」를 센다.

---

## 12회차 — 자동 신호가 실제로 떴고, 거짓 답 하나를 고쳤다

### ★신호가 운영에서 스스로 올라왔다

```json
GET /v1/health → "lanes_silent":["opencc:canonical_zh_hant","org-anchor","zhwiki-title"]
```

4회차 연속 뽑기만 하고 한 건도 못 쓴 세 레인을 **시스템이 이름으로 내보냈다.**
사람이 로그를 뒤지지 않았다. 8회차에 「신호가 올라오면 자동으로 잡히나」라는 물음에
「아직 아니다」라고 답했던 것이 이제 「예」가 됐다.

### 소진 표시가 안 붙던 이유 — 정책 스킵은 시도로 세지 않는다

`exhausted` 는 `attempts >= 2`(maxAttempts) 일 때만 선다. 그런데 근거 없는 칸은
L4 를 건너뛰는 **정책 스킵(ground-strict-skip)** 으로 끝나고, 그 경로는
`recordPolicySkip` 이 **attempts 를 건드리지 않는다.** 그래서 영원히 0 이다:

```
canonical_zh attempts=0 4,564건 중 ground-strict-skip 2,481(54%) · (빈값) 1,327 · wikidata 652
그중 30일 이전에 멈춘 것 3,605건 · 가장 오래된 것 2026-06-13 (3개월+)
비교: canonical_vi 는 72%가 소진 표시(4,066/5,644) — zh 만 5.7%였다
```

그동안 소비자는 `preparing` 을 받았다 — 「기다리면 채워진다」는 뜻인데 채워질 일이 없다.
**최근 7일 271회 요청(120낱말)이 그 거짓 답을 받고 있었다.**

### 고친 것 — 값이 아니라 **안내**를 참으로

`ExhaustedLocales` 가 「오래 멈춘 정책 스킵」도 소진으로 본다. 값은 건드리지 않는다.
소비자는 `unfillable` + `fill_hint` + 정정신고 경로를 받는다. 근거가 생겨 칸이 채워지면
`recordAttempt` 가 행을 지우므로 자동으로 풀린다. 임계 30일은 정책 스킵 쿨다운(7일)보다
넉넉해야 「아직 재방문 중인 것」을 소진이라 부르지 않기 때문이다.

```
새 조건으로 소진 처리될 칸: zh_hant 1,664 · zh 1,555 · ja 992 · vi 279 · pt_br 277 ·
                            id 259 · es 214 · en 7   (그중 zh 는 실제 빈칸 active 1,035)
```

### 이번 회차의 순서가 규칙대로였다

시스템이 말한 결핍 순위를 먼저 읽고 → 대상을 고르고 → **코드를 쓰기 전에 수확을 재고**
→ 구현 → 시험 → 병합. 11회차에 상한 4건을 확인하고 안 쓴 것과 같은 절차다.

### 다음 회차

배포 반영 확인(`preparing` → `unfillable` 이 실제로 줄어드는가) 후, 남은 레인 배선
(itunes·localfill·romanize·mdl·tmdb). 헬스가 지목한 세 레인 중 `zhwiki-title` 은
이미 원인을 안다(en-mismatch, 수확 4건이라 보류), `org-anchor` 는 대상 소진,
`opencc:canonical_zh_hant` 는 문자셋 위반·상위 출처라 못 덮음 — **셋 다 설명이 붙어 있다.**

---

## 13회차 — 배선 확산, 그리고 잴 수 없는 것을 지어내지 않기

### 12회차 수정의 반영 — 아직 단정하지 않는다

```
배포됨 ci-20260920-4c51bd8 · panic/fatal 0
배포후 30분: ready 5 · review 5 · preparing 3 · new 2 · unfillable 2   ← 표본 15건
배포전 24h : ready 340 · preparing 142 · review 110 · new 108 · unfillable 17
```

unfillable 비율이 2.3% → 12% 로 보이지만 **표본이 15건이라 단정할 수 없다.**
방향만 적고 다음 회차에 다시 잰다. 이 저장소는 집계만 보고 결론 내렸다가 여러 번 틀렸다.

### 배선 둘 — 한계는 속이지 않고 적는다

```
romanize-latin  scanned=39 applied=39 skipped=0   (일회용 사본 끝단)
itunes-songs    검사 수와 적용 수를 제대로 나눠 적는다
```

★`romanize` 는 **대량 UPDATE** 다. WHERE 가 곧 선정이고 `RowsAffected` 가 곧 적용이라
「뽑은 수」와 「쓴 수」를 나눌 수 없다. 그래서 `scanned=applied` 로 적고 **그 한계를
코드에 적어 뒀다** — 0 이 나와도 「대상이 없다」와 「막혔다」를 구분하지 못한다는 뜻이다.

없는 수를 지어내면 조용한 0건 판정이 그 레인에서 **거짓**이 된다. 계측기가 거짓말하면
그 위의 모든 판단이 틀어진다 — 8회차에 zhwiki 가 「389건 채움」이라 말한 것이 정확히
그 사고였다. 같은 실수를 계측기 자체에서 반복하지 않는다.

### 헬스가 지목한 세 레인은 그대로

`opencc:canonical_zh_hant` · `org-anchor` · `zhwiki-title` — 셋 다 원인이 붙어 있고
새로 뜬 레인은 없다. 신호가 늑대소년이 되지 않았다는 뜻이다.

### 다음 회차

① 12회차 효과 재측정(표본이 쌓인 뒤) ② 남은 배선(localfill·mdl·tmdb)
③ 결핍 원장이 다음 6시간 측정을 스스로 돌렸는지 확인

---

## 14회차 — 배포 파이프라인의 구멍. 그리고 내 수정이 조용히 실패하고 있었다

### 먼저 찾은 것 — 코드가 운영에 안 올라간 채 조용히 남아 있었다

13회차에 배선한 romanize·itunes 가 원장에 안 보였다. 이미지는 `4c51bd8` 그대로였다.

```
21b1353 (코드·레인 배선)  배포 실패 — "Deployed SHA 825b3de does not match expected 21b1353"
825b3de (문서)            배포 건너뜀 — 「문서만 바뀌었다」 규칙대로
→ 운영은 4c51bd8 에 머물렀고 아무도 몰랐다
```

내가 코드 커밋 직후 문서 커밋을 연달아 밀어 넣어서, 앞 커밋의 배포가 돌 때 main 이
이미 앞서 있었다. **SHA 불일치로 멈춘 것은 안전장치가 제대로 작동한 것**이다(엉뚱한
코드를 배포하지 않는다). 그런데 그 뒤 문서 커밋이 배포를 건너뛰면서 **실패한 코드가
영영 묻혔다.** 두 규칙이 각각은 옳은데 겹치면 구멍이 된다.

`workflow_dispatch` 로 즉시 복구했다(825b3de → 배포 성공).

### 고친 것 — 기준을 「직전 밀어넣기」에서 「마지막 배포 성공 SHA」로

배포 성공 때마다 `refs/deployed/prod` 를 찍고, 배포 여부 게이트가 그것과 비교한다.
실패한 코드는 다음 밀어넣기에서 자동으로 따라 올라간다. 표식이 없거나 못 읽으면
종전대로 직전 SHA 를 쓴다 — **판정 불가는 배포하는 쪽으로 기운다**(이 파일의 기존 원칙).

### ★그런데 그 수정이 조용히 실패하고 있었다

첫 시도 뒤 표식을 확인했더니 `refs/deployed/prod` 가 **없었다.** 로그를 보니:

```
fatal: not a git repository (or any of the parent directories): .git
```

deploy job 은 SSH 만 하므로 원래 체크아웃이 없다. 그런데 `continue-on-error: true` 가
그 실패를 덮어서 **작업은 초록색이었다.** 「고쳤다고 보고하고 실제로는 안 고쳐진 상태」다
— 이 저장소가 반복해 데인 모양을 내가 계측기 쪽에서 또 만들 뻔했다.

체크아웃을 붙이고 표식 단계가 무엇을 했는지 로그에 남기게 했다(`set -x` + `ls-remote`).
**확인까지 했다:**

```
652a8220b63e67108e94bd40d9c5f44c8d80b5d0	refs/deployed/prod
```

### 배선 결과 — 건강한 레인이 늘었다

```
romanize-latin | 16 | 16   ← 뽑은 만큼 썼다
itunes-songs   | 11 | 11   ← 건강
zhwiki-title   | 391 | 0   ← 조용한 0건(원인 확인됨)
opencc:zh_hant |   8 | 0
```

### ① 12회차 효과 — 아직도 표본이 얇다

```
배포전 24h  전체 709 · preparing 138(19.5%) · unfillable 19(2.7%)
배포후      전체  20 · preparing  2(10.0%) · unfillable  2(10.0%)
```

방향은 두 회차 연속 일관되지만 표본이 20건이다. 요청이 시간당 ~20건이라 천천히 쌓인다.
**단정하지 않는다.** 다음 회차에 다시 잰다.

### ③ 결핍 원장

마지막 측정 17:28:30, 주기 6시간이므로 다음은 23:28경 — 아직 돌 때가 아니다. 정상이다.

### 다음 회차

① 12회차 효과 재측정(표본) ② 남은 배선(localfill·mdl·tmdb)
③ 새 배포에서 `refs/deployed/prod` 기준이 실제로 쓰이는지 로그로 확인

---

## 15회차 — 12회차 수정의 결론, 그리고 배선 둘

### ① 결론: 12회차 수정은 의도대로 작동한다

세 회차에 걸쳐 표본이 쌓였다(13회차 15건 → 14회차 20건 → 15회차 75건).

```
배포전  653건 — preparing 19.0% · unfillable 2.5%
배포후   75건 — preparing 10.7% · unfillable 9.3%
```

방향이 세 회차 연속 일관되고 효과가 3.7배다. **거짓 「기다리면 채워짐」 대신 정확한
「못 채움 + fill_hint + 정정신고 경로」가 나간다.** 값은 하나도 바꾸지 않았고 안내만
참이 됐다.

유보할 것: 배포 후 구간은 저녁 시간대라 요청 구성이 다를 수 있다. 다만 세 회차 연속
같은 방향이고 효과 크기가 커서 결론을 낸다 — **과도한 유보도 그 자체로 문제다.**

### 레인 건강 — 배선한 것들이 건강하게 돈다

```
romanize-latin |  4회차 |   54 뽑음 |  54 씀   ← 완전 건강
itunes-songs   |  4회차 |   41      |  24      ← 건강
zhwiki-title   | 10회차 | 3,943     |   0      ← 조용한 0건(원인 확인됨)
opencc:zh_hant | 10회차 |    80     |   0
org-anchor     | 13회차 |     1     |   0
```

`lanes_silent` 는 여전히 같은 셋이고 새로 뜬 레인이 없다 — 신호가 늑대소년이 되지 않았다.

### ② 배선 둘 — localfill · mdl

`localfill` 은 4회차에 「48분 로그 13건 중 10건이 검색결과 0건」으로 읽었던 레인이다.
그때는 로그를 세서 추정했고 앱 재시작 때문에 기간을 오해하기도 했다. 이제 선정 수와
적용 수를 원장이 직접 적는다 — **추정이 관측이 된다.**

둘 다 자기 계수기가 있어 `RecordCounts` 로 그대로 옮겼다(9회차 org-anchor 와 같은 판단 —
같은 것을 두 번 세면 두 수가 갈라진다).

### 다음 회차

① localfill 원장으로 「검색 0건」의 실제 비율 확인 — 4회차의 추정을 관측으로 대체한다
② tmdb 배선 ③ 결핍 원장의 다음 6시간 측정(23:28경) 확인

---

## 16회차 — 원장이 내 추정을 정정했고, 성장 고리의 마지막 연결이 끊겨 있었다

### ① 추정이 관측으로 바뀌었다 — 그리고 틀렸다

4회차에 로그를 세서 「localfill 13건 중 10건이 검색 0건」(성공률 23%)이라고 적었다.
이제 원장이 직접 말한다:

```
localfill | 5회차 | 50 뽑음 | 2 씀 (4%) | {"채우지 못함": 10}
```

**성공률 4%다.** 내 추정은 다섯 배 낙관적이었다. 로그를 세는 것과 원장이 적는 것은
다르다 — 이 차이가 0151 을 만든 이유였고, 그 값이 첫 회차에 증명됐다.

덤: `lanes_silent` 에서 `opencc:canonical_zh_hant` 가 **빠졌다**(이번에 1건을 썼다).
판정이 정확히 작동한다 — 쓰면 신호가 내려간다.

### 대안을 재고 버렸다 — NAVER 는 답이 아니다

localfill 은 SearXNG 만 쓴다. 우리는 NAVER 키를 갖고 있고 7회차에 조직 영문명에서
잘 작동하는 것을 봤으므로 후보였다. **쓰기 전에 쟀다:**

```
"이정훈 일본어 표기" → 야구선수 경력 · 환단고기 기사
"김동식 中文"        → 목사 기사 · 김동식참치(인천 식당)
"박재현 프로필"       → KBO 외야수
```

한국 포털이라 ja/zh **현지 표기가 안 나오고 동명이인만 나온다.** 7회차의 성공은
「한국 조직의 영문명」이었고 지금 찾는 것은 「무명 한국인의 일본어·중국어 표기」다 —
다른 문제다. **안 썼다.**

성공률 4%는 레인의 결함이 아니라 **대상의 성질**이다(11회차: 결이·구름·최보필은 어느
외부 DB 에도 없다). 웹에 없는 것을 검색으로 찾을 수 없다.

### ★그래서 다른 곳을 봤다 — 순서

성공률을 못 올리면 **무엇부터 하느냐**를 바꾸면 된다. 선정 순서를 실측했다:

```
종전 순서(rq 있음 → 오래된 순)로 50건 → 덮는 요청   6건 (요청 있는 건  4/50)
수요(최근 7일 요청 횟수)순으로 50건   → 덮는 요청 315건 (요청 있는 건 50/50)
```

**52배다.** 종전 조건 「research_queue 에 있는가」는 과거 것까지 전부 참이라 변별력이
없고, 그 안에서 `updated_at ASC` 라 **최근 수요와 무관하게** 일하고 있었다.

성장 고리에서 [결핍 진단] → [레인이 그 순서로 일한다] 연결이 끊겨 있던 것이다.
0152 가 수요를 알고 있는데 레인이 그걸 안 보고 있었다. 선정 정렬에 최근 7일 요청
횟수를 1순위로 넣었다(reground 경로도 같이).

### 병합·배포했다

dry-run(일회용 사본): `selected=20` — 새 조인·정렬이 SQL 오류 없이 작동한다.
`applied=0` 은 사본 컨테이너에 gemma 가 없어서다(운영에는 있다) — 코드 문제가 아니다.

```
ci-20260920-6d46050 · healthy · panic/fatal 0
```

선정이 실제로 수요순으로 바뀌었는지는 localfill 이 다음 틱을 돈 뒤에 보인다.
**다음 회차에 원장의 scanned/applied 와 「덮는 요청」이 늘었는지 확인한다** —
고쳤다고 보고하기 전에 실제로 됐는지 본다(14회차 교훈).

### 다음 회차

① 수요순 선정의 실제 효과 확인 ② tmdb 배선 ③ 결핍 원장 6시간 측정(23:28경)

---

## 17회차 — 수요순 선정은 작동한다. 그리고 내 「52배」는 과장이었다

### 확인 — 고친 대로 돌고 있다

배포 후 localfill 이 집은 것은 `해병대`(최근 7일 요청 2회)다. 종전 순서라면 요청 0회짜리
오래된 행을 집었을 자리다.

```
수요 상위(동치미 22 · 결이 9 · 톡파원 9 · 구름 7 · 숏박스 7)는 전부
  지문 동일 + 쿨다운 중 → FillRetryPredicate 가 제외한다(정상)
쿨다운 제외 후 남은 후보의 최대 요청 = 2회
해병대 마지막 시도 2026-09-20 20:37:26 ← 배포 직후 집혀서 시도됐다
```

**쿨다운을 뺀 뒤의 후보 중에서 수요 상위를 집었다.** 고친 대로 작동한다.

### ★정정 — 「52배」는 쿨다운을 빼지 않은 수였다

16회차에 이렇게 적었다:

```
종전 순서 50건 → 덮는 요청   6건
수요순    50건 → 덮는 요청 315건   = 52배
```

그 측정은 **`FillRetryPredicate`(지문 동일 + 90일 쿨다운)를 빼지 않았다.** 실제 선정은
쿨다운을 제외하므로, 수요 상위 대부분은 애초에 후보가 아니다 — 남은 후보의 최대 요청은
2회다. 방향은 맞지만 **크기는 과장됐다.**

고친 값어치가 없다는 뜻은 아니다. 종전이면 요청 0회를 집을 자리에 요청 2회를 집는다.
다만 「52배」라고 말한 것은 틀렸고, 다음에 같은 실수를 하지 않으려면 **수확을 잴 때
레인의 제외 조건까지 그대로 재현해야 한다.** 이 저장소가 「SELECT 과 UPDATE 가 같은
목록을 보게 하라」로 배운 것과 같은 교훈이 측정에도 적용된다.

### 레인 건강

```
itunes-songs   | 2회차 | 61 | 61   ← 완전 건강
romanize-latin | 2회차 | 12 | 12
opencc:zh      | 2회차 |  1 |  1   ← 처음으로 썼다
localfill      | 3회차 | 30 |  0
zhwiki-title   | 2회차 | 780 | 0   ← 조용한 0건(원인 확인됨)
lanes_silent   : org-anchor · zhwiki-title (둘로 줄었다)
```

### 다음 회차

① tmdb 배선 ② 결핍 원장 6시간 측정(23:28경) ③ localfill 쿨다운이 수요 상위를 오래
묶어 두는 것이 맞는지 — 수요가 큰데 90일 잠기는 것이 옳은지 따로 본다.

---

## 18회차 — 쿨다운은 옳다. 재서 확인하고 안 바꿨다

### ① 쿨다운에 묶인 수요는 크다

```
쿨다운 아님  대상   177 · 요청합   128 · 최대요청  3
쿨다운 중    대상 2,753 · 요청합 1,102 · 최대요청 22
```

**요청의 89%가 잠겨 있다.** 레인이 볼 수 있는 것은 128요청뿐이다. 17회차에서 「남은
후보의 최대 요청이 2회」였던 이유가 이것이다. 여기까지만 보면 쿨다운을 풀고 싶어진다.

### ★그런데 풀어도 수확이 없다 — 표본 6건에 0건

쿨다운 중인 수요 상위를 지금 ko.wikipedia 로 쳐 봤다:

```
동치미            langlink 있음 → 그런데 'Dongchimi'/'トンチミ' = **김치 음식**이다.
                  우리 대상은 MBN 예능 「동치미」 — 다른 것이다.
톡파원 25시       Q 있음 · langlink 없음
하이브미디어코프  Q 있음 · langlink 없음
숏박스            위키데이터 항목 없음
결이 · 더 커뮤니티2  문서 없음
```

「입력 지문이 그대로면 같은 결과」라는 전제가 이 표본에서 **참**이다. 90일을 풀면
검색·API 비용만 태우고 0건을 얻는다. **바꾸지 않았다.**

덤으로 위험을 하나 봤다. 동치미처럼 **동음이의가 있는 대상은 쿨다운을 풀고 자동으로
langlink 를 가져오면 음식 표기가 예능에 붙는다.** 쿨다운이 그 오염도 막고 있었다.

이번이 **세 번째로 「재보고 안 쓴」 경우**다(11회차 상한 4건, 16회차 NAVER, 18회차 쿨다운).

### ② 배선 넷 — 권위 API 레인도 원장에 들어왔다

`tmdb-candidates` · `tmdb-locale` · `wikidata-locale` · `kmdb`.

3회차에 「권위 API 레인이 24시간 로그에 한 줄도 없다」고 적었지만, 그때는 「돌았는데
조용한가, 아예 안 도는가」를 구별할 수 없었다(0건이면 로그를 안 찍는다). 이제
그 구별이 질의 한 줄로 끝난다.

### 배선 현황

```
zhwiki · org-anchor · kowiki · opencc · romanize · itunes · localfill · mdl
· tmdb-candidates · tmdb-locale · wikidata-locale · kmdb   = 12개
```

### 다음 회차

① 권위 API 레인 넷이 실제로 도는지 원장으로 확인 — 3회차의 물음에 이제 답할 수 있다
② 결핍 원장 6시간 측정(23:28경) 후 순위 1위 ③ lanes_silent 변화

---

## 19회차 — 3회차의 물음에 답했고, 「아무도 안 켠 레인」을 원장이 드러냈다

### ① 권위 API 레인은 돌고 있었다 — 조용했을 뿐

3회차에 「권위 API 레인이 24시간 로그에 한 줄도 없다」고 적었다. 그때는 **「돌았는데
0건이라 안 찍었다」와 「아예 안 돈다」를 구별할 수 없었다.** 이제 원장이 답한다:

```
tmdb-candidates | 14회차 | 0 뽑음 | 0 씀   ← 돈다. 대상이 0이다
tmdb-locale     | 14회차 | 0      | 0      ← 돈다. 대상이 0이다
wikidata-locale |  5회차 | 2      | 0      ← 돈다. 1건씩 뽑아 못 채운다
kmdb            | 기록 없음                 ← 주기가 1시간이고 앱이 26분 전 재시작했다(정상)
```

`kmdb` 를 처음엔 「60분 로그 0줄이니 안 돈다」고 읽었는데, `kmdbInterval` 기본값이
**1시간**(일 100건 쿼터 때문)이라 아직 첫 티커가 안 온 것이었다. **성급했고 정정한다.**

새 신호: `lanes_silent` 에 `wikidata-locale` 이 추가됐다(2건 뽑아 0건). 규모가 작아
우선순위는 낮지만 판정 자체는 정당하다.

### ★그리고 원장이 「아무도 안 켠 레인」을 드러냈다

배선한 13개 중 24시간 기록이 없는 것을 세어 봤다:

```
kmdb        → 1시간 주기, 곧 돈다(정상)
mdl-works   → **티커가 없다**
```

`DrainMDLWorks` 는 `cmd/kdb/main.go:1387` 에서 **CLI 서브커맨드로만** 호출된다.
`mdlTicker` 도 `KDB_MDL_DRAIN_ENABLED` 도 없다. 즉 **운영자가 손으로 돌리지 않으면
영영 안 돈다.** 3회차에 「장치는 있는데 아무도 안 켠 경우를 하루에 여덟 번 만났다」고
적은 바로 그 부류이고, **원장에 배선했더니 스스로 드러났다.**

3회차의 「선언은 있는데 원장에 0칸인 출처 33개」에 `mydramalist` 가 있었던 이유가
이것이다 — 레인은 있는데 아무도 안 켰다.

### 빈틈 하나 — 원장은 「돈 것」만 기록한다

「한 번도 안 돈 레인」은 원장에 없으므로 보이지 않는다. 이번엔 내가 손으로 기대 목록을
만들어 비교했다. 그걸 코드로 옮기면 시스템이 스스로 말한다.

### 다음 회차

① **mdl 레인을 켤지 판단** — 켜기 전에 수확을 잰다(일회용 사본에서 실제로 돌려
   본다). `mydramalist` 출처가 원장에 0칸인 것이 「켠 적이 없어서」인지 「켜도 안
   되는지」를 가른다.
② 「배선했는데 기록 없는 레인」을 자동으로 드러내기(기대 목록 대비)
③ 결핍 원장 6시간 측정(23:28경) 후 순위 1위

---

## 20회차 — mdl 은 켜도 안 된다. 그리고 「안 도는 레인」을 시스템이 말하게 했다

### ① mdl 수확 측정 — 15건에 0건

19회차에 「mdl-works 는 티커가 없어 아무도 안 켠다」를 찾았다. 켤지 판단하려고
**일회용 사본에서 실제로 돌렸다:**

```
kdb.mdl: DrainMDLWorks processed=15 filled=0
mdl-works ★조용한0건: scanned=15 applied=0 reasons={"AKA 에서 못 찾음":15}
```

`mydramalist` 가 원장에 0칸인 이유는 「켠 적이 없어서」가 아니라 **「켜도 못 찾아서」**다
(3회차의 독립 증거와 같은 방향이다). **티커를 붙일 이유가 없다.**

이번이 **네 번째로 「재보고 안 쓴」 경우**다 — 11회차 상한 4건 · 16회차 NAVER ·
18회차 쿨다운 · 20회차 mdl.

### ② 「한 번도 안 돈 레인」을 시스템이 말한다

원장은 «돈 것»만 적으므로 **한 번도 안 돈 레인은 원장에 없고, 없는 것은 보이지 않는다.**
19회차에 mdl-works 를 찾은 것도 내가 손으로 기대 목록을 만들어 비교해서였다.

`WiredLanes`(배선한 이름 목록) + `MissingLanes`(차집합) 를 두고 헬스에 실었다.
배포 직후 실제로 나온다:

```json
GET /v1/health → "lanes_missing":["mdl-works"],
                 "lanes_silent":["org-anchor","wikidata-locale","zhwiki-title"]
```

`kmdb` 는 1시간 티커가 와서 빠졌다 — **19회차의 정정(「주기가 길어 아직 안 온 것」)이
옳았음이 데이터로 확인됐다.**

창을 24시간으로 잡은 이유가 이것이다. 짧은 창은 주기가 긴 레인을 「안 돈다」고 오해한다.

### 지금 헬스가 말하는 것

```
lanes_missing : mdl-works            ← 티커 없음. 켜도 수확 0이므로 그대로 둔다(이유가 문서에 있다)
lanes_silent  : org-anchor           ← 대상 소진
                wikidata-locale      ← 1건씩 뽑아 못 채움(규모 2)
                zhwiki-title         ← en-mismatch(수확 4건이라 보류)
```

**셋 다 원인과 판단이 붙어 있다.** 신호가 늑대소년이 아니다.

### 다음 회차

① 결핍 원장 6시간 측정(23:28경) 확인 후 순위 1위를 본다 ② 12회차 수정 효과의 24시간
누적 재확인 ③ 새 신호가 뜨면 그것

---

## 21회차 — 12회차 효과 확정, 그리고 발굴 레인 넷이 원장 밖에 있었다

### ② 12회차 수정 — 표본 182건에서 확정

```
배포전 490건 — preparing 19.2% · unfillable 3.1%
배포후 182건 — preparing  8.8% · unfillable 9.3%
```

15회차에 75건으로 낸 결론이 182건에서도 유지된다. **preparing 이 절반 이하로 줄고
unfillable 이 3배로 늘었다** — 거짓 「기다리면 채워짐」이 정확한 「못 채움」으로 바뀌었다.

### 신호가 또 하나 내려갔다

`lanes_silent` 에서 `wikidata-locale` 이 빠졌다(뭔가 채웠다는 뜻). 판정이 계속 정확하다.
남은 것은 `org-anchor`(대상 소진) · `zhwiki-title`(en-mismatch) 둘이고 둘 다 판단이 붙어 있다.

### ★열린 신호가 적어져서 결핍 순위를 보러 갔다가 찾은 것

결핍 원장의 unmet 2위는 `song_album 342낱말/487요청` 이다. 그런데 그것을 **발굴하는**
레인 넷이 원장 배선이 안 돼 있었다:

```
itunes-candidates · discogs-songs · musicbrainz-candidates · musicbrainz-songs
→ 스케줄에는 있는데 도는지·수확이 있는지 알 수 없었다
```

`itunes-songs`(표기 채움)는 61|61 로 건강하게 돌고 있다. 그런데 song_album unmet 이
342건인 이유가 여기 있을 수 있다 — **표기를 채우는 레인과 대상을 발굴하는 레인은 다르다.**
넷을 배선하고 `WiredLanes` 에도 넣었다(안 넣으면 「안 도는 레인」 판정이 눈을 감는다).

배선은 이제 **17개**다.

### 다음 회차

① 발굴 레인 넷이 실제로 도는지 원장으로 확인 — 안 돌면 lanes_missing 이 말한다
② 결핍 원장 6시간 측정(23:28경) 후 순위 1위 ③ 새 신호

---

## 22회차 — 발굴 레인은 돌고 있었다. song_album 은 공급원이 없는 것이다

### ① 배선한 발굴 레인 넷이 전부 돈다

```
discogs-songs           1회차 |  2 뽑음 |  2 씀   ← 건강
itunes-candidates       1회차 |  1      |  0      ← 승급 못함 1
musicbrainz-candidates 13회차 |  0      |  0      ← 대상 0
musicbrainz-songs      13회차 |  0      |  0      ← 대상 0
```

「안 돈다」가 아니었다. 배선하지 않았으면 이것도 몰랐다.

### 그러면 왜 song_album unmet 이 342건인가 — 갈림길을 쟀다

```
song_album 요청 낱말(7일)
  active         359낱말 / 477요청   ← 답한다
  candidate      214낱말 / 324요청   ← 후보엔 있는데 승급이 안 된다
  원장에 없음    101낱말 / 126요청
  rejected        37낱말 /  49요청
```

못 답하는 것의 대부분이 **candidate** 다. 그런데 발굴 레인은 1건만 뽑는다. 왜인지 쟀다:

```
candidate song_album 274건
  itunes ref 보유    0건
  30일 쿨다운      274건 (전부)
  시도이력 있음    274건 (전부)
쿨다운 아닌 후보     0건
```

**274건 전부 iTunes 로 시도했고 전부 못 찾았다.** 레인이 1건만 뽑은 것은 정상이다
(그 1건은 새로 들어온 것이다).

### 결론 — 레인 결함이 아니라 공급원 부족이다

song_album unmet 은 **iTunes 에 없는 곡**이다. 처방은 레인을 고치는 것이 아니라
**공급원을 늘리는 것**이고, 1회차에 「Spotify Client ID/Secret 이 최대 구멍」이라고
적은 것이 이제 데이터로 확정됐다 — 후보 274건이 iTunes 시도를 마쳤고 ref 는 0건이다.

**다섯 번째로 「재보고 안 쓴」 경우다**(11·16·18·20·22회차).

### 지금 헬스가 말하는 것

```
lanes_missing : mdl-works                    ← 켜도 수확 0(20회차 측정)
lanes_silent  : org-anchor · zhwiki-title    ← 둘 다 원인·판단 붙어 있음
```

### 사장님 손이 필요한 것 (변동 없음, 근거만 강해졌다)

1. **Spotify Client ID/Secret** — song_album unmet 342낱말/487요청. iTunes 후보 274건이
   전부 시도 완료·ref 0건이라 **iTunes 로는 더 못 간다.**
2. data.go.kr 인증키 · 국가법령정보 OC — organization/company/school 계열
3. DART OpenAPI 키(무료·자동발급) — 법인 공식 영문명 전량

### 다음 회차

① 결핍 원장 6시간 측정(23:28경) 후 순위 1위 ② 새 신호 ③ 없으면 unmet 3위 이하를 본다

---

## 23회차 — 결핍 원장이 스스로 갱신됐고, 1~3위가 모두 판단이 끝났다

### ① 6시간 주기가 정확히 돌았다

```
23:29:43 | 240칸   ← 내가 부르지 않았다
17:28:30 | 360칸
```

순위는 거의 안 변했다(17:28 → 23:29):

```
person     367→363낱말 / 566→560요청
song_album 342→352     / 487→499
show       120→120     / 154→151
event_tour 111→112     / 128→130
company     81→ 81     /  94→ 94
```

**수요 구조가 안정적이다.** 6시간 동안 순위가 뒤집히지 않았다.

### ③ unmet 3위 show 를 22회차와 같은 방식으로 갈랐다

```
show 요청 낱말(7일)  active 230/489 · candidate 67/83 · 원장에 없음 33/43 · rejected 20/25
candidate show 108건 — tmdb ref 9건 · tmdb 시도이력 91건
```

song_album 과 다르다. song_album 은 후보 274건이 **전부 시도에 ref 0** 이었는데,
show 는 TMDb 가 **9건은 찾아준다.** 공급원이 막힌 것이 아니고 규모도 1/3이다.

### ★결핍 1~3위가 모두 판단이 끝났다

```
1위 person     363낱말/560요청 — 구조적 한계. 결이·구름·최보필은 어느 외부 DB 에도 없고
                                 한국 인물의 한자 이름은 규칙으로 만들 수 없다(11회차)
2위 song_album 352/499        — 공급원 부족. 후보 274건 전부 iTunes 시도 완료·ref 0(22회차)
                                 Spotify 키가 있어야 움직인다
3위 show       120/151        — TMDb 가 부분 작동(9/108). 규모가 작다
```

**레인으로 더 할 수 있는 일이 지금은 고갈 상태다.** 남은 것은 외부 키(Spotify ·
data.go.kr · DART)와 구조적으로 못 채우는 것이다. 그 사실 자체가 관측 결과다 —
「할 일이 없다」가 아니라 **「자체 수단으로는 여기까지」** 가 지금의 정확한 상태다.

### 지금 시스템이 스스로 말하는 것

```
lanes_missing : mdl-works                  ← 켜도 수확 0(20회차)
lanes_silent  : org-anchor · zhwiki-title  ← 원인·판단 붙어 있음
결핍 1~3위                                 ← 위 표. 전부 판단 완료
```

세 지표 모두 **원인과 판단이 붙어 있고 새로 뜬 것이 없다.** 시스템이 조용한 것이
아니라 **말할 것을 다 말한 상태**다.

### 다음 회차부터

자체 개선 대상이 고갈됐으므로 **새 신호가 뜰 때만 움직인다.** 매 회차 확인할 것:
① lanes_silent·lanes_missing 변화 ② 결핍 순위 뒤집힘 ③ 배포/헬스 이상 ④ 새 결함.
변화가 없으면 **아무것도 고치지 않고 그대로 보고한다** — 없는 일을 만들지 않는다.

---

## 24회차 (2026-09-21 00:16~) — 감시 모드 첫 회차. **계측기가 틀렸다**

감시 모드로 돌린 첫 회차에서 확인할 것 다섯 개 중 ①만 움직였다. 그런데 그 하나가
계측기 자체의 결함이었고, 따라가 보니 데이터 오염까지 나왔다.

### ② ③ ④ ⑤ 는 변화 없음

```
결핍 순위   person 363/560 · song_album 352/499 · show 120/151 — 23회차와 동일
배포/헬스   ci-20260920-79ece0f 정상 · panic/fatal 0
요청 구조   23시 9건 · 00시 0건 — 야간 기준선 안이다(최근 5일 0시 일평균 23건,
            1시 5건, 2시 3건). 19회차 교훈대로 «끊겼다»고 읽지 않는다
다른 에이전트 커밋  없음
```

### ① lanes_silent 가 2개 → 5개. **늘어난 넷은 전부 가짜였다**

```
새로 뜬 것  itunes-candidates · kowiki-anchor · musicbrainz-candidates · tmdb-candidates
빠진 것     org-anchor (10건 뽑아 2건 씀 — 제대로 나았다)
```

24시간 실측을 보니 넷 다 **뽑은 회차가 정확히 한 번**이었다.

```
tmdb-candidates        90회차 중 뽑은 회차 1 · scanned 1 · applied 0
musicbrainz-candidates 45회차 중 뽑은 회차 1 · scanned 2 · applied 0
kowiki-anchor           4회차 중 뽑은 회차 1 · scanned 3 · applied 0
itunes-candidates       3회차 중 뽑은 회차 1 · scanned 1 · applied 0
```

1분 티커라 **회차 수만 쌓여** minRuns 를 넘겼고, 창 전체의 합으로는
`scanned>0 AND applied=0` 이라 병이 됐다. 한 행을 못 채운 것이 경보가 된 것이다.

### 반대로, **진짜 병 둘은 헬스가 숨기고 있었다**

```
localfill                 12회차 연속 같은 10행 · 매번 0건 · {"채우지 못함":10}
opencc:canonical_zh_hant  10회차 연속 같은 10행 · 매번 0건
                          {"문자셋 규칙 위반":8, "상위 출처라 못 덮음":2}
```

둘 다 창 앞쪽의 한 회차가 몇 건을 써서 **합이 applied>0** 이 되는 바람에
건강해 보였다. 로그 경보(연속 규칙)는 이 둘을 부르고 있었는데 헬스는 안 불렀다 —
**같은 것을 두 벌의 규칙으로 판정하고 있었다.** 규칙을 두 벌 두면 갈라진다.

→ 판정을 `CountSilentStreak` 하나로 모았다(`0036f5a`). 시험이 붙어 있는 쪽이다.
24시간 이력에 매 시각 적용해 검증했다 — 깜빡이 넷은 0회, 헛도는 셋만 남는다.
배포 후 확인: `lanes_silent: [localfill, opencc:canonical_zh_hant, zhwiki-title]`.

### 그 신호를 따라가니 — opencc 레인의 세 자리가 어긋나 있었다 (`bc1ad7a`)

사유가 **매 회차 똑같은 것**이 단서였다. 대상 10행을 그대로 재현해 봤다.

```
① 검증 locale 하드코딩   번체를 만들어 놓고 «간체 칸 규칙»으로 쟀다.
                        «번체 전용 글자가 있으면 거부» → 제대로 변환된 것만 버렸다.
                        실측 8행. 그중 5행은 번체 칸이 빈칸이었다(결핍 그 자체)
② operator_locked 미제외  잠근 2행을 뽑아 놓고 쓰기 직전에 막혔다. 잠금은 안 바뀌니
                        30분마다 영원히 다시 뽑혔다
③ 변환표가 대만 표준형 아님  为 → 爲 로 구워져 있었다. 생성기가 TWVariants 를
                        제외 판정에만 쓰고 변환 결과에는 안 썼다
```

③은 **이미 쓴 값을 오염시키고 있었다.** 운영 DB 로 확인했다:

```
번체 칸의 為  31건 — tmdb 22 · wikipedia 5 · codex 4   (권위 출처 전부)
번체 칸의 爲  10건 — 전부 opencc                        (우리가 쓴 것뿐)
啓 2건 · 衆 1건도 같은 모양
```

표를 다시 구워 12쌍이 바뀌었다(为→為 众→眾 伪→偽 启→啟 娴→嫻 钵→缽 …).

### ★그리고 나는 같은 회차에 **457칸을 망가뜨렸다** — 아래는 그 기록이다

「한 번 쓴 것을 스스로 못 고치는 레인은 성장하지 않는다」고 판단해 자기 출력을
다시 보는 레인(`opencc:recheck:*`)을 붙였다. 한 번 돌았고, **457칸을 더 나쁜
값으로 덮었다**(zh_hant 408 · zh 49).

전제가 틀렸다. 출처 딱지 `opencc` 를 «이 레인이 쓴 것»으로 단정했는데, 같은 딱지가
**어휘 수준 변환**의 결과에도 붙어 있었다. 지금 표는 «그 자체에서만 정자인 글자»만
바꾸는 보수적 룬 단위 표다(09-17 설계, 朴→樸 오염 133건을 막으려고 일부러 그렇게
했다). 그래서 다시 계산하니 어휘 변환이 통째로 풀렸다:

```
廣告創作挑戰賽 → 广告創作挑戰賽     간체가 번체 칸으로
全州終極音樂節 → 全州终极音樂節
亞洲舞臺       → 亞洲舞台
1分鐘前        → 1分鍾前            鍾 은 성씨 글자다
韓日歌王大賽   → 韓日歌王戰
```

**«표가 좋아지면 과거 답이 따라온다»가 아니라 «표가 약하면 과거 답이 끌려
내려간다»** 였다.

수습(같은 회차 안에 끝냈다):

```
1  레인을 껐다(9f99c2c). 배포 확인 — 그 뒤로 한 번도 안 돌았다
2  회귀 template(09-20 16:30 스냅샷)을 **읽기만** 해서 393칸(zh_hant) + 2칸(zh) 원복
3  원복이 진짜 오염 13칸(爲·啓·衆)까지 되돌렸으므로, 이번엔 **바뀐 쌍만** 치환
   (translate 10자, opencc 출처 칸만) — 爲 0 · 啓 0 · 衆 0
4  남은 15칸(zh_hant)·47칸(zh)은 하나씩 대조해 **정상**임을 확인했다 —
   각자의 원본을 제대로 변환한 값이고 template 쪽이 오히려 간체였다
   (张汝俊→張汝俊 · 赵恩率→趙恩率 · 腾讯音乐娱乐盛典→騰訊音樂娛樂盛典)
```

★내가 어긴 규칙: **덮기 전에 옛 값을 보지 않았다.** 13칸을 고치려고 5,498칸을 다시
계산하게 만들면서, 그 5,498칸이 어떻게 쓰였는지 확인하지 않았다. 수확은 그렇게
재놓고(8칸을 실측으로 확인) **덮을 값의 원본은 재지 않은** 것이다.

★다시 만들려면 «값 전체를 다시 계산»이 아니라 «표에서 바뀐 쌍만 치환»이어야 한다.
3번에서 실제로 그 방식으로 13칸을 고쳤다 — 그게 옳은 모양이었다.

### 한 번 쓴 것을 스스로 못 고치는 문제는 **아직 안 풀렸다**

표를 고쳐도 본 레인은 **자기 출력을 영영 다시 보지 않는다.** 선정이 «목적지가
비었거나 codex-fallback» 이라 출처가 이미 `opencc` 인 5,498칸(zh_hant 4,635 ·
zh 863)이 안 걸린다. 13칸을 사람이 SQL 로 고치면 **다음에도 사람이 필요하다.**

표를 고쳐도 본 레인은 자기 출력을 영영 다시 보지 않는다. 선정이 «목적지가 비었거나
codex-fallback» 이라 출처가 이미 `opencc` 인 5,498칸(zh_hant 4,635 · zh 863)이 안
걸린다. 이번 13칸은 «바뀐 쌍만 치환»으로 손수 고쳤다 — **다음에도 사람이 필요하다.**

그 문제 자체는 여전히 열려 있다. 다만 오늘 배운 것은, 그 해법이 «전체 재계산»이
아니라는 것이다. 재계산은 지금 표보다 나은 과거 답을 끌어내린다.

### 이번 회차가 남긴 규칙

- **신호가 늘면 먼저 신호를 의심한다.** 24시간 이력에 규칙을 되돌려 적용해 보면
  가짜인지 아닌지 바로 나온다 — 깜빡이 넷 대 헛도는 셋.
- **같은 판정을 두 군데에 적지 않는다.** 로그와 헬스가 다른 말을 하고 있었다.
- **사유가 매 회차 똑같으면 그건 레인이 아니라 조건이 틀린 것이다.** 0151 이
  사유별 계수를 남기게 해 둔 이유가 여기서 처음으로 값을 했다.
- ★**덮기 전에 옛 값을 본다.** 수확(새로 채울 칸)만 재고 «덮을 값이 무엇이었나»를
  안 재면, 재계산이 개선인지 손상인지 알 수 없다. 이번에 457칸으로 배웠다.
- ★**되돌릴 수단을 먼저 확인하고 쓴다.** 회귀 template 이 있었기에 같은 회차 안에
  원복했다. 없었으면 457칸은 복구 불가였다.

### 이번 회차 끝 상태

```
lanes_missing : mdl-works                  ← 변함없음(20회차 판단)
lanes_silent  : zhwiki-title                ← 둘이 빠졌다
                                              opencc:canonical_zh_hant 은 고쳐서 8/8
                                              localfill 은 다시 쓰기 시작
결핍 순위      person 363 · song_album 352 · show 120 — 변함없음
배포          ci-20260921-9f99c2c · panic/fatal 0
```

---

## 25회차 (2026-09-21 01:12) — 변화 없음. **아무것도 고치지 않았다**

```
① lanes_silent [zhwiki-title] · lanes_missing [mdl-works]  새로 뜬 것 없음
   opencc:canonical_zh_hant 은 고친 뒤 0/0 유지 — 대상이 비었다(정상)
② 결핍 순위 person 363/560 · song_album 352/499 · show 120/151  그대로
③ ci-20260921-9f99c2c · healthy · panic/fatal 0
④ 마지막 요청 23:53, 공백 79분 — **정상이다.** 이 소비자는 매일 23:20~02:10 에
   멈추고 06:00 에 재개한다(최근 6일 야간 최장 공백 400·399·339·262·229분)
⑤ 다른 에이전트 커밋 없음
```

한 가지만 적어 둔다. **wikidata-locale 이 00:27~00:47 에 120행을 뽑아 전부
「채우지 못함」**으로 끝냈다. 신호는 아니다(연속 판정 1회차) — 24회차에 내가
457+395칸의 `updated_at` 을 건드리면서 선정에 딸려 들어온 것이고, 그 뒤 네 회차는
0행을 뽑았다(쿨다운이 걸렸다는 뜻이다). **다음 회차에 이것을 「새 신호」로 읽지 말 것.**

## 26회차 (2026-09-21 01:44) — 변화 없음. **아무것도 고치지 않았다**

① lanes_silent [zhwiki-title] · lanes_missing [mdl-works] 그대로 ② 결핍 순위 그대로
(원장 마지막 관측 23:29, 다음 ~05:29) ③ ci-20260921-9f99c2c · healthy · panic/fatal 0
④ 01:41 에 요청 **1건**(found) — 재개가 아니라 산발이다 ⑤ 다른 에이전트 커밋 없음.

★없던 기준선 하나를 재 두었다. ④ 「item_status 분포의 급변」을 판정하려면 평소를
알아야 하는데 그동안 없었다(그래서 24회차까지 그때그때 표본과 비교했다).

```
일자별 prepare/lookup 응답 (n=요청 낱말)
         n     ready%  preparing%  unfillable%  미보유%
09-15  1062    37.2      37.9         2.5        10.4
09-16  2584    34.8      16.6         6.1        24.2
09-17  1503    39.9      10.0         5.5        23.3
09-18   833    44.1      12.1        12.4        20.2
09-19   660    49.1      17.7         4.1        16.2
09-20   824    36.9      12.9         3.9        23.1
```

**평소 범위: ready 35~49% · preparing 10~18% · unfillable 3~12% · 미보유 16~24%.**
preparing 이 37.9%(09-15) → 12.9%(09-20) 로 내려온 것은 12회차 수리의 자취다.
미보유가 20% 안팎으로 고정된 것은 결핍 원장이 말하는 것과 같은 이야기다
(person·song_album·show — 외부 키 대기).

다음 회차부터는 이 범위를 벗어날 때만 ④를 신호로 읽는다.

## 27회차 (2026-09-21 02:16) — **새 신호 하나. 재보고 고치지 않았다**

```
① lanes_silent [localfill, zhwiki-title]   ← localfill 이 새로 떴다
   lanes_missing [mdl-works]               그대로
② 결핍 순위 그대로 ③ ci-20260921-9f99c2c · healthy · panic/fatal 0
④ 마지막 요청 01:41(35분 전) — 야간 기준선 안 ⑤ 다른 에이전트 커밋 없음
```

### localfill — 가설을 세우고, 재고, **버렸다**

24시간 성적은 24회차 중 쓴 회차 3 · 240 뽑아 5 씀(2.1%). 최근 6시간 시도를 유형별로
보니 **person 이 76건을 먹고 0건 성공**이라, 「수요 1위라 demand-first 정렬이
person 을 앞에 몰아주고, 그게 못 채울 것을 갈고 있다」는 가설이 섰다. 21회차에
정렬을 demand-first 로 바꾼 것이 나였으니 **내가 만든 회귀일 수 있었다.**

그래서 전체 이력으로 다시 쟀다 — 6시간은 표본이 너무 작았다.

```
유형별 localfill 성공률 (전체 이력)
company 10.5% · channel_outlet 9.0% · brand_place 8.1% · group 7.3%
song_album 7.0% · person 6.3% · agency 6.2% · event_tour 5.3% · movie 5.2%
character 4.9% · drama 3.7% · show 3.0%
```

**person 은 6.3% 로 중간이다.** 0% 가 아니었다. 6시간 창의 0/76 은 잔여물의
작은 표본이었을 뿐이고, person 을 뒤로 미뤘다면 **멀쩡한 6.3% 를 버릴 뻔했다.**

대기열도 재봤다(레인의 제외 조건 그대로 재현). **66행**이고, 뽑히는 10행의
`updated_at` 이 22:19 → 00:07 로 **앞으로 나아간다** — 같은 행을 도는 것이 아니라
줄을 소화하는 중이다. 그 66행 중 상당수가 왜 다시 열렸는지도 확인했다:
`fill_input_hash` 가 `canonical_zh`·`canonical_zh_hant` 를 포함한다(0104). 즉
**24회차에 내가 다시 쓴 852칸이 입력지문을 바꿔 재선정 대상이 된 것**이다. 설계대로다.

→ **수확 0. 아무것도 고치지 않는다.** 유형별 성공률이 3~10% 로 고르고(고칠 편중이
없다), 대기열은 작고 줄어드는 중이다. localfill 의 낮은 수율은 잔여물의 성질이지
조건의 어긋남이 아니다 — zhwiki-title 이 393행을 **매 회차 그대로** 다시 뽑는 것과
다르다.

★이번 회차가 남긴 것: **작은 창의 0% 를 「구조적 0」으로 읽지 않는다.** 6시간 0/76 과
전체 이력 6.3% 는 같은 대상의 다른 표본이다.

## 28회차 (2026-09-21 02:50) — 변화 없음. **27회차 판단이 검증됐다**

```
① lanes_silent [zhwiki-title]  ← localfill 이 **스스로 빠졌다**
   대기열 66 → 60행, 최근 회차 10 뽑아 1 적용 → 연속 0
   lanes_missing [mdl-works] 그대로
② 결핍 원장 23:29 (다음 ~05:29) ③ healthy · panic/fatal 0
④ 마지막 요청 01:41(69분 전) — 야간 기준선 안 ⑤ 다른 에이전트 커밋 없음
```

27회차에 「고치지 말고 줄이 줄어 신호가 꺼지는지만 보라」고 판단했고 그대로 됐다.
그때 코드를 건드렸다면 멀쩡한 레인을 손댄 것이 된다. 같은 화면에서 zhwiki-title 은
391행을 **매 회차 그대로** 다시 뽑고 있어, 두 신호의 성질이 다르다는 것도 확인된다.

**29회차 (03:51)** — 변화 없음. lanes_silent [zhwiki-title] · lanes_missing [mdl-works] ·
결핍 원장 23:29(다음 05:29, 아직 아님) · healthy · panic/fatal 0 · 마지막 요청 01:41(130분 전,
야간 기준선 229~400분 안) · 다른 에이전트 커밋 없음. itunes-songs 가 65분에 14건 채웠다.

**30회차 (04:52)** — 변화 없음. lanes_silent 에 localfill 이 다시 떴으나 **파지 않았다** —
27회차에 이미 잰 신호이고, 대기열이 66→60→**21행**으로 계속 줄고 있어 줄을 소화하는
중이다(다 소화되면 다시 꺼진다). lanes_missing [mdl-works] · 결핍 원장 23:29(다음 05:29) ·
healthy · panic/fatal 0 · 마지막 요청 01:41(191분 전, 기준선 안) · 다른 에이전트 커밋 없음.

**31회차 (05:37)** — ★**결핍 원장이 세 번째로 스스로 갱신됐다**: 23:29:43 → **05:31:32**
(6시간 주기, 250칸 = locale_gap 206 · unmet 44). 내가 부른 것이 아니다 — 레인 실행에
업혀 도는 구조가 사람 없이 유지된다(17:28 → 23:29 → 05:31). 결핍 1~3위 불변
(person 362/559 · song_album 352/499 · show 120/151). 하위에서 company 자리에
character(51/92)가 들어왔으나 7일 창에서 옛 요청이 빠지며 생긴 표류다. 나머지 변화 없음
— lanes_silent [localfill, zhwiki-title] · lanes_missing [mdl-works] · healthy · panic/fatal 0 ·
마지막 요청 01:41(236분 전) · 다른 에이전트 커밋 없음. **아무것도 고치지 않았다.**

**32회차 (06:17)** — 소비자가 예상대로 **06:00 에 재개**했다(88건). 아무것도 고치지 않았다.

재개 트래픽의 `ready` 가 77.6% 로 26회차에 재둔 범위(35~49%)를 벗어나 보였는데, **그
범위는 「하루 전체」 수치**였다. 시각대로 다시 재니 06시대는 원래 높다:

```
06시대 ready%   09-15 75.9 · 09-16 50.0 · 09-17 68.7 · 09-18 64.0
                09-19 63.8 · 09-20 49.3 · 09-21 77.6(오늘)    하루 전체 평균 41.4%
```

밤새 준비해 둔 낱말을 아침에 다시 묻기 때문이다. **시각대 기준선과 하루 기준선을 섞어
보지 말 것** — 하마터면 정상을 신호로 읽을 뻔했다(야간 공백을 잴 때와 같은 함정이다).

나머지 변화 없음 — lanes_silent [localfill, zhwiki-title] · lanes_missing [mdl-works] ·
healthy · panic/fatal 0 · entities 22615→22625 · 다른 에이전트 커밋 없음.
재개와 함께 레인도 일한다: 40분에 itunes-songs 36뽑아 35건, discogs-songs 6/6.

**33회차 (06:49)** — 변화 없음. lanes_silent [localfill, zhwiki-title] · lanes_missing
[mdl-works] · 결핍 원장 05:31(다음 ~11:31) · healthy · panic/fatal 0 · entities 22625→22628 ·
06시대 154건 ready 76.6%(06시대 정상 범위 49~78% 안) · 다른 에이전트 커밋 없음.
itunes-songs 가 35분에 15뽑아 15건 전부 채웠다.

**34회차 (07:20)** — 변화 없음. lanes_silent [localfill, zhwiki-title] · lanes_missing
[mdl-works] · 결핍 원장 05:31(다음 ~11:31) · healthy · panic/fatal 0 · entities 22628→22633 ·
다른 에이전트 커밋 없음. 06시 180건(기준선 94의 약 2배) · 07시는 진행 중.

★**시각대 프로파일을 한 번에 떠 두었다.** 32회차에 「하루 기준선을 시각대에 대볼 뻔한」
일이 있었는데, 매 회차 다시 재는 것도 낭비다. 최근 6일 평균(어제까지):

```
시각  일평균  ready%      시각  일평균  ready%      시각  일평균  ready%
 0      47     30          8      30     27         16      54     19
 1       5     40          9      49     45         17      50     38
 2       3      0         10      67     27         18      72     48
 3~5     0      -         11      53     16         19     126     50
 6      94     60         12      60     33         20     108     47
 7      32     46         13      72     25         21     131     42
                          14      84     15         22      88     58
                          15      88     32         23      14     37
```

**ready% 는 시각대에 따라 15~60% 로 출렁인다.** 26회차의 「하루 전체 35~49%」는 하루
단위 판정에만 쓰고, 시각대 판정에는 이 표를 쓴다. 03~05시는 요청이 아예 없는 것이 정상이다.

**35회차 (07:52)** — 변화 없음. lanes_silent [localfill, zhwiki-title] · lanes_missing
[mdl-works] · 결핍 원장 05:31(다음 ~11:31) · healthy · panic/fatal 0 · entities 22633→22640 ·
다른 에이전트 커밋 없음. 요청은 06시 180건/ready 73%(표 94/60) · 07시 18건/ready 39%(표
32/46) — 아침이 표보다 굵게 몰렸고 07시는 표본이 작다. 급변 아님. 34회차 시각대 표를
처음 실전에 썼고, 재측정 없이 판정이 끝났다.

**36회차 (08:23)** — ⑤가 처음 울렸다. **회귀는 없다.** `134a105` 는 docs 전용(+62줄,
`docs/STATUS_20260920.md`)이고 코드 변경 0 · 내 파일 무변경(lane_ledger·demand_gaps·
opencc·api.go·0151/0152). 내용은 그쪽 영역이다 — 기각 회수 레인이 250건을 살렸고
`keyword_triage.go` 의 옛 범위가 정당·기업·학교를 되기각해 38건을 되죽였다는 보고.
원인·처방을 그들이 이미 짚었고 `scope_*.go` 는 내가 손대지 않기로 한 파일이라 **건드리지 않는다.**

★**다음 회차에 오독하지 말 것**: 250건의 상태가 한꺼번에 바뀌었으므로 11:31 결핍 원장
갱신에서 `unmet` 수치가 움직일 수 있고 레인 선정량도 늘 수 있다. 그것은 이상이 아니라
저쪽 회수의 파급이다(24회차에 내 852칸 churn 이 wikidata-locale 을 부풀린 것과 같은 모양).

나머지 변화 없음 — lanes_silent [localfill, zhwiki-title] · lanes_missing [mdl-works] ·
healthy · panic/fatal 0 · entities 22640→22654 · 요청 06시 180/73% · 07시 18/39% ·
08시 14/14%(표 94/60 · 32/46 · 30/27, 아침에 몰아 넣고 이후 한산·합계는 표보다 위).

**37회차 (08:55)** — 36회차에 예고한 **파급이 실제로 나타났고 좋은 쪽이다.** 아무것도 고치지 않았다.

```
최근 35분        romanize-latin 58 뽑아 58 채움 (직전 회차 19/19 · 7/7)
                 kowiki-anchor 23 뽑아 2 (야간엔 0~4행) · wikidata-locale 14 뽑아 5 (야간 0~1)
                 org-anchor 14 뽑아 2 · opencc:zh 2/2 · zh_hant 1/1
```

저쪽이 되살린 250건이 active 가 되자 **채움 레인들이 자동으로 집어 약 70칸을 채웠다.**
아무도 배선하지 않았고 두 에이전트가 서로 부르지도 않았다 — 회수 레인이 문을 열고
채움 레인이 들어갔다. 성장 고리가 **다른 에이전트의 작업까지 받아 도는 것**을 처음 봤다.
entities 22654→22683.

나머지 변화 없음 — lanes_silent [localfill, zhwiki-title] · lanes_missing [mdl-works] ·
결핍 원장 05:31(다음 ~11:31, 여기서 unmet 이 줄 것이다) · healthy · panic/fatal 0 ·
8시 48건/ready 31%(표 30/27, 표보다 위) · 새 커밋은 내 것과 저쪽 docs 뿐.

**38회차 (09:56)** — 신호 둘이 떴고 **둘 다 원인이 나와 고치지 않았다.**

**① kmdb 가 lanes_silent 에 새로 떴다 — 가짜가 아니다.** 연속 3회차(07:37·08:37·09:37)가
진짜로 뽑고 못 채웠다(1·4·2행). 다만 **어제까지 kmdb 는 뽑을 것이 아예 없었다**(24시간
11회차 중 8회차가 0행). 저쪽 회수 250건이 영화·드라마를 되살리며 일감이 생긴 것이다.
규모 7행, 주기 1시간 — 28회차 규칙대로 다음 회차(10:37)를 보고 스스로 꺼지는지 본다.

**④ 9시 ready 12%(표 45%) — 기준선을 또 잘못 댈 뻔했다.**

```
9시 ready%  09-16 50 · 09-17 36 · 09-18 14 · 09-19 17 · 09-20 39 · 09-21 12
```

**34회차 시각대 표의 숫자는 「6일 평균」이고 날짜별 폭은 12~50% 다.** 오늘 12% 는 하단이나
범위 안이다. ★**평균을 범위처럼 쓰지 말 것** — 오늘만 세 번째 기준선 함정이다
(하루↔시각대 32회차 · 작은 표본 35회차 · **평균↔범위 38회차**).

원인도 분명하다: 9시 요청 낱말을 원장에 대보니 **candidate 25건**이다. 저쪽이 되살린 것들이
「모르는 낱말」에서 「아는데 아직 못 채운 낱말」로 바뀌며 miss 가 preparing·review 로 옮겨
갔다. **커버리지는 오히려 좋아졌고** 채움 레인이 메우는 중이다(지난 1시간 romanize-latin 86칸).

나머지 변화 없음 — lanes_missing [mdl-works] · 결핍 원장 05:31(다음 ~11:31) · healthy ·
panic/fatal 0 · entities 22683→22741 · 다른 에이전트의 새 코드 커밋 없음.

## 39회차 (10:47) — **계측기가 멀쩡한 레인 셋을 잘못 세고 있었다**

38회차에 뜬 kmdb 신호가 한 회차를 더 보고도 안 꺼졌다(4회차 연속). 28회차 규칙대로
기다렸으니 이제 팠다. 그리고 **레인이 아니라 내 배선이 틀렸다**는 것이 나왔다.

```
kmdb 가 오늘 한 일   승급 3건(더 스튜디오·이타미 준의 바다·동갑내기 과외하기)
                     kmdb 외부참조 3건 생성 · 시도 9건
원장에 적힌 것        applied = 0 · 사유 "채우지 못함" 9
```

`RecordCounts(..., checked, filled, ...)` 에서 **`promoted` 가 빠져 있었다.** 이 레인은
원장을 두 갈래로 바꾼다 — canonical_en 채움과 candidate→active 승급. 승급만 한 회차는
applied=0 으로 적혔고, 그 건이 사유에서 「채우지 못함」으로까지 세어져 **이중으로
거짓말**을 했다. 계측기가 자기 레인을 병으로 신고한 것이다.

**같은 모양을 세 자리에서 찾았다**(이 저장소에서 「한 곳을 빠뜨렸다」가 반복된 자리다):

```
① kmdb             승급을 안 셈 → 고침
② tmdb-candidates  같은 결함 그대로(promoted 두 곳·filled 한 곳, filled 만 기록) → 고침
③ discogs-songs    scanned 와 applied 에 **둘 다** confirmed+anchored → 언제나 같다.
                   `scanned>0 AND applied=0` 이 영영 성립하지 않아 **이 레인만 신호에서
                   통째로 빠져 있었다.** 못 찾는 계측기는 거짓 경보보다 나쁘다 —
                   거짓 경보는 시끄럽기라도 하지만 이건 조용하다 → 고침
```

셋 다 «원장이 바뀐 **행** 수»로 통일했다(changedRows). 한 행이 채움과 승급을 동시에 해도
한 번만 센다 — 더하면 applied>scanned 가 되어 DB 제약에 걸리고, 거기서 scanned 를 올려
맞추면 그 보정이 또 다른 거짓말이 된다.

★**확인하고 넘어간 것**: `itunes-candidates` 는 결함이 아니다. 채움과 승급이 **같은
트랜잭션**이고 승급 실패 시 롤백되므로 promoted 가 곧 「원장이 바뀐 행」이다. 지금 그
레인에 뜬 신호는 진짜이고 22회차에 판단한 song_album 공급원 부족이다. **틀리지 않은 것을
틀렸다고 고치지 않았다.**

배포 `ci-20260921-1a4ec16` 확인 · panic/fatal 0. kmdb 신호는 아직 떠 있는데 **원장의 옛
기록이 그대로이기 때문**이다 — 다음 회차(11:37)부터 제대로 세어지고, 거기서 확인한다.

나머지: 10시 ready 8% 는 날짜별 폭 0~26% 안이다(38회차 교훈대로 평균이 아니라 범위로 봤다).

## 40회차 (11:41) — **오늘 세 번째로 계측기가 틀렸다**

**ⓐ kmdb** 는 아직 확인 불가다. 11:37 회차가 없는데 **10:52 배포로 컨테이너가 재시작하며
1시간 티커가 초기화**됐기 때문이다(다음 ~11:52). 19회차 교훈대로 「안 돈다」고 읽지 않는다.

**ⓑ 결핍 갱신은 됐고 — 네 번 됐다.**

```
17:28 → 3회      23:29 → 2회      05:31 → 2회      11:32 → 4회
11:32 의 네 회차는 내용이 완전히 동일(unmet 23행 2,305요청 · locale_gap 109행)
```

`MaybeMeasureDemandGaps` 가 「마지막 관측이 6시간보다 오래됐나」만 보고 고루틴을 띄우는데,
이 함수는 레인이 원장에 적을 때마다 불린다. 레인 여럿이 **같은 초에** 적으면 전부 같은 옛
timestamp 를 보고 전부 띄운다. 읽기는 안 틀어지지만 같은 일을 네 번 한다 — 한 번이 요청
로그를 9번 훑는다. atomic CAS 빗장으로 고쳤다. **쌓인 중복은 지우지 않았다** — 내용이 같아
오도하지 않고, 「그때 네 번 쟀다」도 관측의 일부다.

**순위는 그대로, 수치는 올랐다.**

```
person      362/559 → 396/597        show        120/151 → 124/155
song_album  352/499 → 374/521        (7일 창에 오늘 아침 트래픽이 들어왔다)
```

아침에 메운 것보다 **새로 물어본 것이 더 많았다**는 뜻이다. 뒤집힘은 없다.
unmet 유형 수는 44 → 23 으로 줄었는데, 이는 term_type 이 덜 흩어졌다는 뜻이고 다음
회차에 한 번 더 보고 판단한다 — 지금 단정하지 않는다.

★오늘 계측기가 틀린 것이 세 번이다: 24회차 합판정 · 39회차 승급 미계수 · 40회차 중복 측정.
**신호를 따라가기 전에 계측기부터 의심한다**가 오늘의 규칙이 됐다.

## 41회차 (12:12) — **zh 결핍을 재고, 고치지 않았다**

오너 질문에 답하려다 **내 초점이 틀렸다는 것**을 알았다. 0152 는 결핍을 두 종류로 나눠
재게 해 놨는데(unmet=대상을 모름→발굴 · locale_gap=아는데 표기 없음→표기 레인) **나는 매
회차 unmet 순위만 보고했다.** 전체로 보면 이쪽이 더 크다:

```
unmet      person 396낱말/597요청 · song_album 374/521 · show 124/155
locale_gap **zh 835/1,209** · zh_hant 831/1,202 · ja 413/558 · vi 40 · id 38 · en 32
```

### 잰 것

```
① active + zh 빈칸            2,326건 (person 537 · 그 외 1,789)
② 그중 en 이 라틴             2,254건 (97%) ← 변별력이 없어 「en 라틴이면 zh 라틴」 규칙은 못 쓴다
③ 그중 QID 보유                 262건 (12%)
④ QID 표본 25건 중 wikidata 에 zh 라벨      3건 (12%) — 需雲雜方·釜山長神大學校·韓承起
⑤ 대상 풀(active+QID+zh_hant 빈칸) 270건, 표본 50 중 zh 계열 라벨 3(6%),
   zh_hant 채울 근거 있는 것 **1건(2%)** → 기대 수확 **5~16건**
```

### 찾은 결함(고치지 않기로 한 것)

`wikidata_locale_drain` 의 zh 분기는 `SimplifiedZh()` 를 쓴다. 평문 `zh` 라벨이 번체면
문자셋 게이트가 간체 칸에서 **옳게** 막는다(09-17 에 87건 오염을 낸 자리다). 그런데
**막은 값을 zh_hant 칸으로 보내지도 않고 버린다.** 위 세 건이 그래서 빈칸으로 남았다 —
같은 행의 ja 칸은 채워져 있다(ハン・スンギ·釜山長神大学校).

넣을 곳이 있는데 버리는 것은 결함이 맞다. 그러나 **수확이 5~16칸**이고, 고치려면 간체/번체
판정 자리를 다시 건드려야 한다. 87건 오염을 낸 그 자리다. **비율이 맞지 않아 고치지 않는다.**

### ★진짜 결론: wikidata 는 한국 고유명사의 중국어 표기 공급원이 아니다

zh 빈칸 2,326건 중 QID 보유가 270건(12%)뿐이고, 그 270건조차 zh 라벨 보유가 6%다.
person unmet 이 「구조적 한계」인 것과 같은 종류의 확정이되 **이번엔 locale_gap 쪽**이다.

### 다음 회차 본무

8월 주석에 **「zh 위키백과 문서 98/359(27%)」** 라는 실측이 남아 있다. 27% 가 사실이면
QID 경로(6%)보다 네 배 큰 공급원이다. 그런데 `zhwiki-title` 레인은 **보유 URL 이 있는
것만** 처리한다 — **검색 경로가 없다.** 그 비율부터 잰다.

(감시 다섯: 배포 `ci-20260921-7ec5234`(CAS 빗장) 확인 · healthy · panic/fatal 0 ·
lanes 동일 · entities 22884 · 다른 에이전트 새 코드 커밋 없음.)

## 42회차 (12:41) — zhwiki 경로를 접었고, **zh 공급원이 비어 있는 진짜 이유**를 찾았다

### ① zhwiki 표제어 경로 — 측정이 대형 오염을 막았다

표본 50(active · zh·zh_hant 둘 다 빈칸)을 ko/en 위키백과 langlinks 로 찍었다.
겉보기 적중 **8/50(16%)**. 그런데 찾은 것을 하나씩 보면:

```
클리셰(group)→陳腔濫調 「진부한 표현」     가물치(character)→烏鱧 물고기
마노(person)→瑪瑙 보석 agate              템포(song_album)→速度(音樂) 음악 용어
리안(drama)→李安 감독 Ang Lee             아워즈(agency)→小时 시간 단위
인연(term)→因緣 일반어(대상 밖)           릴파(person)→异世界女团 그룹 문서로 흘러감
```

**정확히 맞는 것 0건.** KDB 대상은 짧은 한국어 표제어가 일반명사와 동음인 경우가 많아
표제어로 찾으면 **일반명사 문서가 먼저 걸린다.** 「16%면 유의미하다」로 갔다면 중국어 칸에
「진부한 표현」·「물고기」·「보석」을 대량으로 썼을 것이다 — 그럴듯한 한자라 눈으로도 못 잡는,
24회차보다 훨씬 나쁜 오염이다. ★**오매칭을 비율보다 먼저 잰 것이 이 회차의 전부다.** 접는다.

### ② 매체 관측이 1~3주째 멈춰 있었다 — **오너의 결정이었다**

```
kwave_media_observations 마지막 관측
  es 09-02 · id 09-08 · vi 09-09 · zh-hant 09-13 · pt-br 09-13 · ja 09-15 · en 09-21
  zh(간체) 관측은 원래 없음
kwave_news_whitelist  RSS 피드 107개 **전부 rss_poll_enabled=false**
                      (연속실패 7회 자동 차단은 1개뿐 — 나머지는 일괄로 꺼짐)
```

추적하니 `tick.go` 에 **2026-09-15 운영자 지시**가 있다:
«더이상 일반 한국어 기사를 가져오는 경우 등 사용하지 말자. gemma 가 고유명사를 제대로
파악하지 못해서 kdb 가 오염된다.» (커밋 f993d1c — 유튜브 한 페이지가 브라질 매체 5곳으로
둔갑해 매체합의를 만들고 있었다.) 기본값이 「안 가져온다」로 뒤집혔다.

**결함이 아니다. 건드리지 않는다.** 「107개가 꺼져 있으니 켜자」로 갔다면 오너 지시를
뒤집는 사고였다. ★39회차 규칙(틀리지 않은 것을 틀렸다고 고치지 않는다)의 가장 큰 사례다.

### ★그래서 zh 결핍의 정확한 상태

```
zh 공급 경로          상태
wikidata 라벨         6% 만 보유 (41회차)
zhwiki 표제어 검색    오매칭 100% (이번 회차)
매체 합의(2순위)      09-15 오너 결정으로 꺼짐 — 추출기가 오염을 만들었다
기계번역·합성         이미 채울 수 있는 곳은 채웠다(gtranslate 3,248 · codex 1,524)
                      person 등은 ground-strict 정책으로 막혀 있다
```

**zh 835낱말/1,209요청을 풀 자동 공급원이 지금 없다.** 그리고 그 원인 하나가 오너의 결정과
맞닿아 있다. 이 판단은 내가 뒤집을 수 없다 — 오너 지시를 되돌리는 일이고 외부로 크롤링
트래픽을 내는 일이다. **오너에게 보고한다.**

(감시 다섯: 배포 `ci-20260921-3246cde` · healthy · panic/fatal 0 · lanes 동일 · entities 22894.)

## 43회차 (12:50~13:17) — **오너: «이제 결론내려서 개선을 시켜야지»**

41~42회차에 재기만 하고 결론을 미뤘다. 오너 지적을 받고 **결정하고 고쳤다.** 재는 것은
멈추지 않되, 잰 뒤에는 고친다.

### ① TMDb 변형 앵커 — 45일 막혀 있던 작품이 2분 만에 공식 중국어 제목을 받았다

zh 빈칸 show·drama·movie 461건 중 **317건은 TMDb 앵커가 없어** tmdb-locale 이 영원히 못
본다. 앵커 레인은 매 틱 돌면서 317건을 **전부 이미 봤고 315건을 no-match** 로 판정해 뒀다 —
그리고 **원장에 아무것도 안 남겨** 아무도 몰랐다. 표본의 `꽃파당(조선혼담공작소 꽃파당)` 이
단서였다: 가드가 틀린 게 아니라 **질의 형태가 틀렸다**(괄호 부제가 정확일치를 깬다).

같은 가드(SearchExactKoreanID — 정확·유일·원작 ko·상위 시리즈 배제)로 괄호 바깥/안·별칭을
다시 태우는 패스를 붙였다(`da49033`). **콜론 앞부분은 만들지 않는다** — 상위 시리즈에 붙을
수 있다. 표본 30건 측정에서 5/5 정답이었고, 운영 결과:

```
26 뽑아 4 앵커(15%) — 4건 모두 정답, 전부 다음 틱에 공식 zh·zh_hant 가 들어왔다
꽃파당(조선혼담공작소 꽃파당)  → 花党：朝鲜婚姻介绍所 / 花黨：朝鮮婚姻介紹所   (08-07 부터 막혀 있었다)
천천히 강렬하게(가제)          → 梦响年代 / 緩慢而強烈（暫定名）
나는 SOLO, 그 후 사랑은 계속된다 → 爱情在继续 / 我是SOLO，那之後的愛仍在繼續
미수다(별칭 미녀들의 수다)      → 美女们的唠叨 / 美女們的嘮叨
```

본 앵커 레인도 원장에 배선했다(tmdb-anchor, WiredLanes).
(천천히 강렬하게의 zh_hant 에 「（暫定名）」 이 붙은 것은 TMDb 쪽 데이터다 — 앵커는 맞다.)

### ② wikidata 번체 라벨 → zh_hant — 41회차에 접었던 것을 다시 폈다

41회차에 「수확 5~16칸, 87건 오염을 낸 자리라 비율이 안 맞다」며 접었다. **위험을 잘못
쟀다** — 87건은 번체를 **간체 칸**에 넣어서 생겼고, 이 수정은 번체를 **번체 칸**에 넣는다.
번체 전용 글자가 있을 때만 쓴다(`ea39126`, 순수 함수 시험). 번체 칸이 차면 opencc 가 간체를
결정적으로 만든다 — 한 값이 두 칸을 연다.

옛 코드가 건너뛴 254행의 `wd-locale` 시도 기록만 지워 한 번 다시 보게 했다(값은 안 건드림).
**41회차의 세 건은 아직 안 채워졌다** — 레인이 틱당 30~40행씩 소화 중이다. 다음 회차에 본다.

### ③ 본무 — 못 채우는 zh 칸이 정직하게 나가나: **내 가설이 틀렸다. 고치지 않는다**

```
오늘 preparing 70낱말의 빈 칸   빈칸  소진  정책스킵30일+  정책스킵30일내  시도없음  그외
zh                              17    0       1             14            0        2
zh_hant                         17    0       1             13            0        3
ja                               8    0       0              8            0        0
```

**시도 기록이 없는 칸은 0개**다. 대부분은 30일 안의 정책 스킵이고 7일마다 다시 본다 — 그리고
오늘 ①에서 실제로 다시 보다가 채워지는 것을 봤다. 이걸 unfillable 로 돌리면 **아직 채워질 수
있는 칸을 「없다」고 말하는 반대 방향 거짓말**이다. 판정은 설계대로 정직하다.

### ④ 따로 기록 — 값은 빈칸인데 출처 딱지만 남은 칸

```
active 중  zh_hant 2,217 · zh 2,191 · ja 1,486 · en 90   (zh_hant 는 2,216건이 wikidata-label)
09-20 스냅샷에 이미 1,268건 · 최근 2시간 신규 38건
```

wikidata-label 을 쓰는 두 곳(wikidata_locale_drain · readiness/worker)은 모두 값과 출처를 함께
쓰고 빈 값은 쓰지 않는다 — 누군가 **값만 지우고 출처를 남긴** 흔적이다. 오늘 내 변경 이전부터다.
채움 레인들은 「값이 빈칸이면 덮는다」를 먼저 보므로 **채움을 막지는 않는다.** 다만 「출처가
있으면 채워진 것」으로 읽는 곳이 있다면 틀린 답을 낸다. 누가 지우는지는 다음에 추적한다.

(감시 다섯: `ci-20260921-ea39126` · healthy · panic/fatal 0 · lanes_silent [kmdb, localfill,
zhwiki-title] · lanes_missing [mdl-works] · 다른 에이전트 커밋 4+1건(wiki-title 자가교정 레인·
0153·docs) — 내 파일과 겹치지 않았고 합친 뒤 전체 시험 통과.)

## 44회차 (13:46) — 둘 다 돌았고, **하나는 두 건을 틀렸다. 되돌리고 막았다**

### ① wd 번체 라우팅 — 설계한 사슬 그대로 돌았다 ✓

```
부산장신대학교  zh_hant 釜山長神大學校 (wikidata) → opencc → zh 釜山长神大学校
한승기          zh_hant 韓承起      (wikidata) → opencc → zh 韩承起
수운잡방        zh_hant 需雲雜方    (wikidata) — opencc 다음 회차
그 외 이 경로   現代卡(현대카드) · 國家遺產振興院 …  — 간체 전용 글자 섞임 없음
```

번체 라벨이 번체 칸으로 가고, opencc 가 간체를 결정적으로 만들었다. 41회차에 「87건 오염을 낸
자리라 비율이 안 맞다」며 접었던 것이 옳지 않았다는 것이 운영으로 확인됐다.

### ② TMDb 변형 앵커 — 12건 중 **2건이 틀렸다**

한 건씩 TMDb 에 대조하다 걸렸다. 같은 TMDb ID 를 두 개체가 나눠 가진 것을 모으니 두 종류였다:

```
틀림 2 — 서로 다른 작품(시즌)이 한 ID 를 나눠 가짐
  1293    사랑과 전쟁 / 부부클리닉-사랑과 전쟁2         ← 별칭 「부부클리닉 사랑과 전쟁」
  245684  흑백요리사: 요리 계급 전쟁 / … 전쟁2           ← 별칭 「흑백요리사」
맞음 5 — 원장에 **같은 작품이 두 개체로 중복**돼 있던 것
  나가수/나는 가수다 · 냉부해/냉장고를 부탁해 · 눈이 부시게(Dazzling)/눈이 부시게 ·
  꽃파당(…)/조선혼담공작소 꽃파당 · 천천히 강렬하게(가제)/천천히 강렬하게
```

**콜론 앞부분은 막았는데, 같은 상위 시리즈 이름이 별칭으로 저장된 경로로 새어 들어왔다.**
tmdb-locale 이 다음 틱에 5칸을 덮었다 — 흑백요리사2 의 zh 가 `黑白厨师：烹饪阶级战争2` 에서 시리즈
제목 `黑白大厨：料理阶级大战` 으로(시즌 칸에 프랜차이즈 제목 — tmdb.go 가 「빈칸이 낫다」고 한 그 모양).

**처리(순서가 중요했다)**:
1. **코드를 먼저 배포했다**(`b07e478`) — 제목 끝에 시즌·속편 표지(2·시즌2·3기·2부·Part 2·II)가
   있으면 변형도 같은 표지를 가져야 한다. 「같은 ID 에 둘이면 보류」로 막지 않았다 — 그러면 맞은
   5건까지 잃는다. 운영의 두 건과 맞았던 것들을 **둘 다** 시험으로 고정했다.
2. **그 다음 데이터를 되돌렸다.** 먼저 되돌리면 입력지문이 바뀌어 옛 코드가 같은 자리에 다시
   잘못 붙인다. 09-20 template 을 **읽기만** 해서 옛 값을 확인하고, 5칸을 복원 · 앵커 2개 삭제를
   한 트랜잭션으로. 원래 주인(사랑과 전쟁 · 흑백요리사 시즌1)은 앵커를 유지한다.

   (vi·es·pt_br 은 옛 값이 영어 음역 `Culinary Class Wars` 라 새 값이 오히려 나아 보였다. 그래도
   되돌렸다 — 출처가 `tmdb` 라고 적힌 것이 **거짓**이기 때문이다. 이 개체가 아니라 상위 시리즈의
   제목이다.)

**결과: 12건 중 10건 정답(83%), 틀린 2건은 원복.** 43회차에 「4/4 정답」이라 보고한 뒤 표본이
늘자 틀린 것이 나왔다 — **작은 표본의 100% 를 믿지 않는다.** 한 건씩 대조하는 확인을 계속한다.

### ★덤으로 얻은 것 — 원장의 중복 개체 5쌍

앵커가 같은 ID 를 가리키면서 원장에 **같은 작품이 두 개체**로 있던 것이 드러났다(위 「맞음 5」).
병합은 개체 정체성 영역이라 내가 하지 않는다 — 다른 에이전트(I13 실명/예명 UUID 작업)의 몫이다.
기록만 남긴다. TMDb ID 공유는 중복 탐지의 좋은 근거가 된다.

(감시: `ci-20260921-b07e478` · healthy · panic/fatal 0 · lanes 동일 · 다른 에이전트 새 코드 커밋 없음.)

## 45회차 (14:23~14:40) — 앵커 10/10 확인 · 번체 24칸 확인 · **5만 4천 칸의 거짓 출처를 찾아 고쳤다**

### ① TMDb 변형 앵커 — 남은 10건 전부 TMDb 대조, 10건 모두 정답

시즌 가드(`b07e478`) 뒤로 새로 틀린 것 없음. 「사랑과 전쟁」→#1293 은 시즌 표지 없는 일반명이라
시리즈가 맞다. 같은 ID 공유 5쌍은 전부 원장 중복(같은 작품). 누적 51 뽑아 12 앵커(되돌린 2 포함) —
변형 대상 풀은 거의 소진됐다.

### ② wd 번체 라우팅 — 13:00 이후 채워진 zh_hant 24칸 전부 확인

간체 전용 글자 섞임 **0건**. opencc 가 간체까지 만들었다(需雲雜方→需云杂方 · 釜山長神大學校→
釜山长神大学校 · 韓承起→韩承起 …). 한 건만 기록: `바보(person)` → `傻瓜`(「바보」라는 일반명사) —
번체 문자열이 아니라 **앵커가 사람 대신 개념에 붙은 것**이고, 내 라우팅(번체 전용 글자가 있어야
발동)과 무관한 기존 명시 라벨 경로다.

### ③ ★「값은 빈칸인데 출처 딱지만 남은 칸」 — 원인을 찾아 고쳤다

**추측을 버리고 모양을 갈랐다.** 09-20 스냅샷과 대조하니:

```
값이 있었는데 지워짐        0      ← 「누가 값을 지운다」는 가설이 틀렸다
빈칸에 출처가 새로 찍힘     0
그때도 이미 빈칸+출처    1,336
스냅샷 이후 생긴 개체      876    ← 여기다
```

새 개체를 직접 보니 on-demand 발굴이 방금 만든 분당서울대병원 · 카카오벤처스 · I'm Your Girl … 이
**값 0칸 · 출처 wikidata-label 6칸**으로 태어나 있었다. 생성 INSERT 는 출처를 하나도 안 넣는다 —
그렇다면 **컬럼 기본값**이다:

```
canonical_{en,ja,vi,zh,zh_hant,es,id,pt_br}_source  DEFAULT 'wikidata-label'   ← 0049
```

0049 가 출처 컬럼을 **추가하면서 기존 행을 채우려고** 준 기본값이 그대로 남아, 이후의 모든 INSERT 에
붙었다. 개체를 만드는 세 경로(on-demand 발굴 · RSS 후보 · 수요 근거)는 어느 것도 출처를 넣지 않는다.
**규모 9,527행 · 54,117칸.**

**영향을 먼저 쟀다.** 채움 가드들은 「값이 빈칸이면 덮는다」를 먼저 보고, 출처만 세는 곳도 없었다 —
기능은 멀쩡했다. 틀어진 것은 **기록**이다. 빈 칸이 권위 출처를 주장하면 출처로 판단하는 모든 감사가
속는다(43회차에 내가 그랬다).

**고친 것(`beea80c`)**:
1. 세 INSERT 가 출처를 `''` 로 **명시**(상수 하나를 공유). DDL 이 아니라 잠금 없이 새 거짓을 막는다.
2. 0154 가 기본값을 `''` 로. **배포 경로를 절대 막지 않게** 짰다 — 바쁜 테이블이라 잠금을 2초만
   기다리고 못 얻으면 NOTICE 만 남기고 넘어간다(실패하면 원장에 안 적혀 다음 배포마다 재시도되고,
   계속 실패하면 **다른 에이전트 배포까지** 멈춘다). **일회용 사본에서 두 경로를 다 확인**했다 —
   잠금 중엔 종료코드 0 으로 건너뛰고, 풀리면 바뀐다. 운영에서는 잠금을 얻어 **기본값이 바뀌었다.**
3. 쌓인 칸은 마이그레이션이 아니라 운영에서 **500행 묶음 · 잠긴 행 건너뜀 · updated_at 불변**으로
   정리했다(updated_at 을 건드리면 레인 정렬이 흔들려 24회차 같은 재선정 소동이 난다. 입력지문
   트리거는 출처 칸에 반응하지 않는 것을 확인했다).

```
정리 전 54,117칸  →  정리 후 0칸
값이 있는 wikidata-label 칸(건드리면 안 되는 것) zh_hant 1,830칸 — 그대로
```

**아직 확인 못 한 것**: 배포 뒤 새 개체가 아직 안 생겨 「빈 출처로 태어나는지」는 다음 회차에 본다.

(감시: `ci-20260921-beea80c` · healthy · panic/fatal 0 · lanes 동일 · 다른 에이전트 새 커밋 없음.)

## 46회차 (15:04) — 새 개체 출처 확인 · 사람 앵커 4,158건 전수 · **고치지 않기로 한 결론**

### ① 새 개체가 빈 출처로 태어난다 ✓

배포 뒤 생긴 개체 4건 — 값이 있는 칸만 출처가 있다(en=gtranslate · ja=kana-rule), 빈 칸은 출처도
빈칸. 「값 빈칸 + 출처 wikidata-label」 은 **0 유지**. 값 있는 wikidata-label zh_hant 는 1,830 → 1,832
(새로 채워진 2칸, 정상).

### ② ★정정 — 45회차에 `바보` 를 잘못 읽었다

`바보(person)` 의 zh_hant `傻瓜` 를 「앵커가 사람 대신 일반명사 개념에 붙었다」고 적었다. **틀렸다.**
앵커 Q627045 는 **2008년 한국 영화 「바보」**(P31=영화)이고, `傻瓜` 는 그 영화의 중국어 제목, en
`Miracle of a Giving Fool` 도 그 영화의 영문 제목이다. 값은 전부 **그 영화로 일관**하다. 틀린 것은
앵커가 아니라 **개체 유형(person)** 이다.

### ③ 사람 개체의 위키데이터 앵커 — 표본 100건은 0건이었다. 전수로 다시 쟀다

표본 100건은 **100/100 사람(Q5)** 이었다. 그런데 바보가 그 표본에 없었을 뿐이다 — 작은 표본을 믿지
않기로 했으니 **active person 4,158건 전부**의 P31 을 찍었다:

```
사람(Q5) 4,122 · 사람 아님 36 (0.9%)

(A) 사람인데 앵커가 틀림 ~17
    이름 항목 12  민혁·리사·하림·SUMIN(수민)·예훈·상화·승형·보원·창희·연호·여준 + 성씨 남궁
    그 외          육중완→밴드 장미여관 · 서원→교육기관 서원 · 덕산→언덕 · 고봉산→산 · 미르→용 ·
                   DK→Dplus Kia(e스포츠)
(B) 앵커는 맞는데 유형이 틀림 ~17 — 개체가 사실은 작품·그룹·기관
    영화   바보 · 가비 · 마돈나        드라마·예능  장희빈 · 부부 · 댄싱9 · 좋은 날
    그룹   엠블랙 · 노을 · 씨스타19 · 지온 · 옥상달빛 · 피아 · 알레프
    그 외  세종대왕(소설) · 하츄핑(캐릭터) · 한국음악실연자연합회(기관) · 이로하(버튜버)
```

**실제로 틀린 값이 사람 칸에 들어간 것은 2칸뿐**이다 — `육중완 zh_hant 玫瑰旅館`(그가 속한 밴드
장미여관의 중국어 이름) · `서원 ja 書院`(교육기관). 둘 다 **09-20 스냅샷에도 있던 오래된 값**이다.
이름 항목 12건은 값이 가타카나 읽기(ミンヒョク·スミン·ハリム…)라 **사람 이름 읽기로도 우연히 맞다.**

### 결론 — **코드는 고치지 않는다**

「사람 개체면 앵커 P31 이 Q5 여야 쓴다」 가드는 몇 줄이면 된다(`wikidata.Entity.InstanceOf` 가
이미 있다). 그러나 넣으면 **(B) 유형만 틀린 17건의 맞는 값까지 막는다** — 바보의 `傻瓜` 는 그 영화의
올바른 중국어 제목이다. 막는 틀린 값(2칸)과 비슷한 수의 맞는 값을 막는다. 진짜 원인은 **개체
유형(B)과 앵커(A)가 틀린 것**이고 둘 다 **개체 정체성 영역**이다. 지역 가드는 순손해다.

틀린 2칸도 지우지 않았다 — 앵커가 그대로면 레인이 같은 값을 다시 쓴다. 고칠 자리는 앵커다.

★**넘기는 목록**(유형·정체성 담당 에이전트용): 위 (A)는 앵커 교체/제거, (B)는 entity_type 수정 대상이다.
(B)는 소비자에게 **유형이 틀린 채로** 나간다 — 영화 「바보」를 물으면 person 으로 답한다.

(감시: `ci-20260921-beea80c` · healthy · panic/fatal 0 · lanes_silent [kmdb, zhwiki-title] —
localfill 빠짐 · lanes_missing [mdl-works] · 다른 에이전트 새 커밋 없음.)

## 47회차 (15:40~17:20) — 감시를 끄고 고쳤다: 앵커 레인 켬 · 출처 승격 · **별칭 확장은 오염을 내서 거둬들이고 되돌렸다**

오너 지시 «너두 모니터링 끄고 문제된점을 파악해서 개선을 만들고 진행해» 에 따라 루프를 끄고 네 번 내보냈다.

### ① `cdb9425` 기관류 앵커 레인을 켰다 — 13건 모두 정답

요청된 active 개체의 ja 빈칸을 앵커 상태로 나누니 **기관류는 거의 전부 앵커가 없었다**(회사 42/44 ·
단체 24/28 · 기획사 14/15 · 정부기관 12/14 · 채널 13/14). wd-locale 은 QID 가 있어야 채우므로 영원한
사각지대였다. 이 자리를 위한 레인(active_anchor_drain, 09-16)이 이미 있었는데 **CLI 전용·기본 dry-run** 이라
아무도 안 켰다(mdl-works 와 같은 모양). wd-locale 틱 앞에서 틱당 20건씩 돌게 했고 원장에 배선했다
(`wikidata-active-anchor`). 켠 뒤 붙은 **13건을 전부 Wikidata 로 대조해 13건 모두 정답**. 수율 약 3.3%,
대상 풀 2,193건.

### ② `73b66ee` 라벨과 **같은** 값의 출처만 올린다 — 35 + 130칸

기계가 우연히 맞힌 값(codex-fallback · gtranslate)이 영영 기계 출처로 남아 있었다. 값이 라벨과 같으면
**출처만** wikidata-label 로 올린다(값은 안 바꾸므로 틀린 값을 만들 수 없다). 운영에서 35칸, 시도 기록을
푼 뒤 130칸.

★**정정** — 커밋 메시지의 「hideLLMServe(기본 on)가 소비자에게 **비워서** 내보냈다」는 틀렸다. 코드 기본값은
on 이지만 **운영은 `KDB_SERVE_HIDE_LLM_ONLY=0`** 이라 값은 나가고 있었다. 실제 이득은 provenance 가
`llm-only`/`machine-translation` 에서 `wikidata-label` 로 바로잡히는 것과 `verified_only` 소비자에게 닿는 것이다.

### ③ `cf612c4` → `e429632` 별칭 확장 — **오염을 내서 거둬들였다**

anchor-mismatch 로 막힌 active 82건 중 77건이 「우리 별칭 또는 괄호 앞부분」으로 맞았다. 배포 뒤 막힌 84행 중 34행이 채워졌다. 그래서 대조 이름을 넓혔는데,
**앵커가 맞는지만 확인하고 들어갈 값이 나은지는 확인하지 않았다.** 운영 값으로 대조하니 섞여 있었다:

```
좋아짐   UNB zh ∅→UNB · 농촌진흥청 zh 大韩民国农村振兴厅 · 지붕 뚫고 하이킥 ja 明日に向かってハイキック
나빠짐   바이브 ja バイブ→「Naver VIBE」 (회귀 시험이 막던 바로 그 오염 — 그룹 유형에서 별칭 순환)
         tvN STORY →「OnStyle」(옛 채널명) · 박명수의 라디오쇼 en →「Radio Show」
         DAY6→Day6 · PENTAGON→Pentagon · GOING SEVENTEEN→Going Seventeen (공식 표기를 잃음)
```

`e429632` 에서 **우리 별칭은 대조에 쓰지 않게** 했다(순환의 뿌리). 괄호 앞부분만 남겼다(사람·캐릭터·시즌 괄호 제외).
그리고 **대소문자만 다른 값은 덮지 않게** 했다 — 위키데이터 영문 라벨은 위키백과식 표기라 공식 표기를 지운다.
시험이 버그 하나를 잡았다: `tmdbNoteRE` 가 「시즌2」를 메모로 쳐서 시즌 괄호를 벗기고 있었다 → 시즌 판정 분리.

**데이터는 배포 뒤에 되돌렸다**(먼저 되돌리면 입력지문이 바뀌어 옛 코드가 같은 자리에 다시 쓴다):
- 84행의 ja·zh·zh_hant·vi·en → 실행 전에 저장한 값(`/tmp/am_before.tsv`)으로. 대소문자만 바뀐 7칸 포함.
- ★**저장이 모자랐다.** es·id·pt_br 는 저장하지 않았다 — 그 세 칸의 잔여 변경을 09-20 template(읽기 전용)으로
  되돌렸다: TWS pt_br · 글로벌에이치미디어 es/id/pt_br(→NH Media) · 광개토대왕 3칸 · 구암 허준 2칸 ·
  달의 연인 id · tvN STORY 2칸(→OnStyle) · 지붕 뚫고 하이킥 es · 자두 es · 고잉 세븐틴 es · 네이버웹툰 3칸.
- **최종 대조: 84행 × 8칸 전부** 저장값 또는 template 과 같다. 다른 15칸(5행)은 오늘 실행 **이전**부터 있던 값이다
  (행 updated_at 이 15:40 이전이거나 출처가 romanization — template 이 모를 뿐).

★교훈: **덮기 전 스냅샷은 레인이 쓸 수 있는 칸 전부를 뜬다.** 이번에 5칸만 떠서 3칸을 template 으로 메웠다 —
template 이 없었으면 못 되돌렸다. 그리고 **앵커가 맞다 ≠ 값이 낫다.** 둘을 따로 잰다.

### ④ 84행에서 드러난 **틀린 앵커** — 정체성 영역이라 넘긴다

84행은 「이름이 안 맞아서 wd-locale 이 거부한」 행이다. 대부분은 표기만 다르고 앵커는 맞다(국방부·Karina·
현대·기아차 …). 그런데 **틀린 앵커의 라벨이 다른 레인을 통해 이미 칸에 들어가 있는** 행이 있다(출처 wikidata-label):

```
삼성전기(company)        Q2998661  삼성엔지니어링       ja サムスンエンジニアリング · vi/es/id Samsung Engineering
김상구(person)           Q129652   배우 서준영          ja/zh/zh_hant/vi/es/id/pt_br 전부 서준영
DK(person)               Q85976326 Dplus Kia(e스포츠)   es DWG KIA · pt_br Dplus KIA
바이브(group)            Q87730005 네이버 VIBE          id Naver VIBE
타잔(character)          Q97338605 LoL 선수 이승용       en Lee Seung-yong
인디고(song_album)       Q55733579 레이블 인디고뮤직     en/vi/pt_br Indigo Music
My You(song_album)       Q130213443 드라마 마이 유스     en My Youth · zh/zh_hant 我的青春 · es Mi juventud
Wife(song_album)         Q625355   드라마 아내(2003)     en Wife · ja 妻 · zh/zh_hant 妻子
```

wd-locale 은 이름 관문으로 이들을 막는다. 그러나 앵커를 붙인 레인(삼성전기는 09-18 `wdorg`)이나 그 뒤의 다른
레인이 **관문 없이** 라벨을 썼다. 앵커 철회는 `person_anchor_withdraw`(다른 에이전트, `7e6f86c` 에서 판정 집행기를
고치는 중)의 일이다 — 앵커를 떼면 그 칸도 함께 비운다(D-37). 값만 먼저 지우면 앵커가 남아 같은 값이 다시 들어온다.
**넘기는 목록으로 둔다.**

### ⑤ ★큰 그림으로 돌아갔다 — 「못 답하는 수요」의 56% 는 **이미 아는데 승급을 못 한 것**이다

결함 수리만 이어 가지 않으려고 결핍 원장으로 돌아갔다. 11:32 관측의 unmet 상위가 이상했다 — 이재명·이재용·조국·
더불어민주당·대법원·서울특별시. 누구나 아는 대상을 「모른다」고 한다. 7일치 unmet 을 원장 상태로 갈랐다:

```
                         낱말    요청    비중
candidate (알고 있음)   1,034   1,367    56%   ← 승급을 못 했다
행 없음   (정말 모름)     570     749    31%   ← 발굴 문제
rejected                  223     336    14%
```

**가장 큰 병목은 발굴이 아니라 승급이다.** candidate 1,034낱말 중 747낱말은 on-demand 발굴이 **실제로 돌았고**
(사전검사 approved), 위키데이터 검색 miss(no_match)로 candidate 에 남았다. on-demand 경로는 위키데이터가 확인해야만
승급하고, 설계 주석은 나머지를 「RSS/운영자 보강 대상」이라 부르는데 **RSS 는 09-15 오너 결정으로 꺼져 있다.**
나이는 3~7일이 654낱말로 몰려 있다(09-15 범위 확장 뒤 들어온 것).

유형별(747): person 199 · song_album 154 · event_tour 58 · company 47 · show 46 · organization 46 …
이재명·이재용·조국은 이 밖이다 — 이재명은 앵커가 **동명이인 축구선수(Q6514101)** 로 아직 붙어 있고, 이재용·조국은
candidate-gate·cand-evidence 가 옛 범위 사유(「K-엔터 인물이 아님」)로 붙들고 있다. 범위·정체성 영역이라 넘긴다.

### ⑥ 곡 — **iTunes KR 스토어에는 곡이 없다.** MV 뿐이다

곡 candidate 는 itunes-cand · mb-song 이 **전부 no_match** 였다. 레인 결함인지 원천 부재인지 가르려고 표본을
iTunes 에 직접 물었다:

```
질의 「Dynamite BTS」   KR  7건 — kind 전부 music-video      JP  song 8 · music-video 2
질의 「아이유」          KR  6건 — 전부 music-video          JP  song 8
질의 「Spring Rain ONEW」 KR 0                               JP  Spring Rain / KAKERA -運命のピース- — ONEW
```

**KR 스토어프론트에는 곡 카탈로그가 없다.** itunes-candidates 는 KR 만 봐서 MV 가 있는 타이틀곡만 잡았다(누적 승급 116,
no_match 718). 수록곡·일본 발매곡은 구조적으로 못 잡는다. 그런데 곡 unmet 상위가 바로 그 모양이었다 —
스프링 레인(요청 13) · 카케라-운메이노피스-(7) · 유라유라 -운메이노하나-(6) · 유니☆스파클!(6) · 회귀LOVE ·
더블 낫 · 헬리베이터 … 전부 K-pop 가수의 **일본 발매곡이나 영문 제목 곡**이고, 기사는 **원제를 괄호로 함께 적는다.**

**재고 썼다.** 곡 candidate 292개 중 기사 문맥에서 원제가 뽑히는 것 37개를 JP 스토어로 흉내 냈다 —
**13곡 적중, 13곡 모두 기사 원문과 맞다.** 가수 관문이 옳게 막은 것: 「신 포도」(미지의 곡인데 스토어 첫머리는
르세라핌 Sour Grapes) · 「전야」(RIIZE 가 팬미팅에서 부른 EXO 곡, 기사엔 RIIZE 만).
흉내에서 놓친 것 중 여럿은 **기존 가수 추정이 틀려서**였다 — 「기사에 나온 가장 긴 이름 하나」가
스프링 레인→이예진 · 카케라→하이라이트 · 디어 마이 크레이지 솔메이트→이지 로 나왔다.

**`05cab74`**: KR 에서 못 찾으면 원제를 뽑아(순수 함수 `itunesOrigTitle`) JP 스토어에 「원제 + 가수 라틴명」으로 묻는다.
가수는 **기사에 나온 active 가수 전부**와 대조하고, 맞는 가수가 둘이면 버린다. 깨진 글자(`ピ?ス`)는 가나 사이의
「?」 만 한 글자로 본다. 원장 사유에 KR 일치 · 원제 JP 일치/불일치 · 가수 모호를 따로 센다.

**운영 결과** — 쿨다운을 푼 곡 candidate 102개 중 레인 한 바퀴(수동 1회 + 스케줄):
```
원제 JP 로 승급 17곡 — 17곡 모두 기사 원문과 맞다
  ONEW      스프링 레인 · 카케라-운메이노피스-(깨진 글자 행) · 카케라 -운명의 조각-
  ZEROBASEONE 유라유라 -운메이노하나- · 회귀LOVE          Stray Kids  더블 낫 · 헬리베이터 · 저스트 어 리틀
  LIGHTSUM  폴린 월드 · 요루오 아루쿠 키미에              청하 스내핑 · izna 핸드 위드 케어 · 인어미닛 환상통
  KISS OF LIFE 스웨트 · BABYMONSTER 위 고 업 · FIFTY FIFTY 투 머치 파트 원 · Mighty Mouth 크리스마스 플랜
가수 모호로 버림 1 · 원제 없음/불일치 85
```

★**승급과 함께 기계값이 나갔다** — `252e41c`. 17곡의 en 이 **전부 gtranslate** 였다(폴린 월드 "Pauline World" ·
핸드 위드 케어 "Hand with Care" · 회귀LOVE "Regression LOVE"). 빈칸일 때만 채우게 해 둔 탓에, candidate 일 때는 안 나가던
값이 active 가 되며 나갔다. 원제 경로는 공식 제목을 쥐고 있으므로 라틴이면 en 의, 일본어면 ja 의 기계값·빈칸을 올리게
했다(itunes > 기계, 기존 순위 그대로). 배포 뒤 옛 값 17행을 `/tmp/itc_before.tsv` 에 저장하고 같은 규칙으로 고쳤다 —
en 12칸(Fallin' World · HANDLE WITH CARE · Painful …) · ja 5칸(ゆらゆら -運命の花- · 回帰LOVE · KAKERA -運命のピース- ×2 · 夜を歩く君へ).
교훈: **승급은 값이 나가기 시작하는 순간이다.** 승급하는 레인은 그 행에 이미 있던 기계값까지 책임져야 한다.

**못 푸는 것도 확인했다.** 한글 제목 곡(살아온 우리에게 · 부디 행복해질 것 · 남아있는 노래처럼 …)은 키 없이 한글 제목으로
대조할 곳이 없다 — iTunes JP/US 는 로마자·영문 제목(임영웅→Lim Young Woong, 「For Us」), Deezer 도 같다
(이 순간을 사랑해요→「Love This Moment」). **Spotify 키가 있어야 풀린다(오너 영역).**

### ⑦ 「정말 모르는 것」 쪽 — 발굴 입구 게이트가 **사람 단서를 09-15 범위로 넓히지 않았다** (인계)

unmet 의 「행 없음」 552낱말을 발굴 큐 상태로 갈랐다: **367낱말(492요청)이 입구 사전검사에서 review 로 빠져 있다.**
자동 검증(intake_autoverify)은 돈다 — 오늘도 시간마다 auto_evidence 승인이 나간다(09-21 441건). 남는 것이 문제다:

```
최근 7일 review 중   소진(3회 근거 없음 → 운영자 몫)   328건 · 요청 1,336회   ← 처리하는 사람이 없다
                     한 번도 시도 안 됨(유형 충돌 등)  199건 · 요청 644회
소진 유형            person 98(374요청) · unknown 69 · event_tour 48 · song_album 39 · show 28 · company 24
```

소진 상위 20건의 **실제 네이버 결과를 운영 판정 함수(DecideIntake) 그대로** 돌렸다(일회용 시험, 커밋 안 함):
- 오늘 결과로는 11건이 통과한다. 그러나 **통과 유형이 틀린 것이 대부분**이다 — 김서준·최선은·조수용·스페셜타임스 →
  government_body, 세계테마기행(show) → person. 요청 유형이 실패하면 **모든 유형을 차례로 대 보는데**(autoVerifyTypesToTry),
  government_body 단서에 **한 글자 「청」** 이 있어 「청년·요청·시청」에 다 걸린다.
- 실제 피해: 09-15 이후 auto 승인된 government_body 145건 표본 25건 중 **「찹쌀떡」이 active/government_body** 로 나가 있다
  (「예산군 청소년…」). 「공주 페스타」(행사)·「도로공사」(일반명사)도 government_body 로 들어갔다.
  유형 힌트가 있으면 promote 가 힌트를 지키지만(07-29 결정), **힌트가 없으면 이 틀린 유형이 그대로 저장된다.**

그리고 **person 단서가 연예 직업뿐이다**(배우·가수·감독·작가·모델·개그맨·방송인·멤버·출연자). 09-15 에 범위를
정치·경제·스포츠로 넓히면서 발굴의 연예직업 필터는 뗐지만(ac93245) 입구 단서는 그대로다. 사람 review 40건으로 쟀다:

```
person 단서                           사람 40건 통과   비사람이 사람으로 잘못 통과(25건 중)
현재(연예 직업만)                           4               2
엄격(+정치인·장관·대표이사·선수·교수 …)     14               5   (재정경제부…위원장 · 보건복지부…장관)
넓게(+의원·시장·회장·대표·기자)            22              11   (삼성전자…회장 · 해시드…대표 · 서울시…의원)
```

**단서만 넓히면 오탐도 같이 는다.** 32자 창 안에 직함이 있으면 통과시키는 방식이 새는 구조다. 제안: 사람은 **직함이
이름에 붙은 경우만**(「김서준 대표」·「배우 정혜인」·「반효진 선수」) 인정하는 규칙 + 한 글자 단서(「청」) 제거 +
구체 힌트가 있으면 **다른 유형으로 떨어지지 않게**(증거는 저장될 유형을 받쳐야 한다). 셋 다 입구 게이트의 **유형 판정
규칙**이라 유형·범위 담당 에이전트에 넘긴다. 이재용·조국처럼 candidate 에 갇힌 인물(cand-evidence 「K-엔터 인물이 아님」)도
같은 뿌리다.

### ⑧ 결핍 원장 17:34 — 오늘 오전 zh 작업의 순효과는 작았다

```
             11:32            17:34
unmet        1,695 / 2,305    1,844 / 2,471   (낱말 / 요청, 7일 창)
zh           835 / 1,209      894 / 1,272
zh_hant      831 / 1,202      889 / 1,264
```

합계는 새 수요로 늘었다. 순효과만 따로 셌다 — **11:32 에 zh 결핍 상위였던 150낱말 중 지금 채워진 것은 7개(4.7%)**
(zh_hant 도 7). TMDb 변형 앵커·wd 번체 라우팅은 맞게 돌지만 zh 결핍을 줄일 만큼의 공급원은 아니다(41회차 결론과 같다).
곡 unmet 의 변화는 17:34 관측이 승급(17:30~17:55)보다 앞서 23:34 관측에서 본다.

(감시: `ci-20260921-252e41c` · panic/fatal 0 · lanes_silent [localfill, zhwiki-title] · lanes_missing [mdl-works] ·
다른 에이전트 새 main 커밋 없음.)

---

# 2026-09-23 — 중국어를 채웠다. 공급원을 다 뒤진 끝의 결론은 «권위 10%, 나머지는 잠정»

오너 지시: «위키데이터 외에 해결 가능한 솔루션을 찾아봐. 지금 중국어가 많이 필요한데, 어떻게든 해결해야지.»
그리고 승인: «제안대로.»

## ① 대상 — 요청이 온 개체의 zh 빈칸 1,029건

최근 14일 요청된 active 개체 기준: 사람 360 · 곡 157 · 방송 96 · 기업 70 · 그룹 61 · 캐릭터 58 · 행사 44 ·
단체 39 · 기획사 34 · 정부기관 19. **785건이 09-16 이후에 생겼다.**

빈칸의 원인은 버그가 아니라 정책이었다 — zh 시도 기록의 대부분이 `ground-strict-skip`(836). 근거가 없으면
LLM 합성(L4)을 일부러 건너뛴다. 그 전에 생긴 개체는 gtranslate·codex 로 zh 를 받았고(전체 active 의 84% 가 zh 보유),
09-16 에 codex 경로가 접히면서 새 개체는 받을 길이 없어졌다.

## ② 공급원을 전부 찔러 봤다 (실측)

```
NetEase 음원 (등록된 중국어 제목)   곡 154 → 제목일치 107 → 가수일치 40 → 중국어 제목 11  (약 10%)
한국어 위키백과 한자 병기           기관 185 → 22 (12%)   ※ 한국식 한자(情報院·海兵隊) · 일반 개념 문서 주의
중국어 위키백과(한글 병기 문서)     그룹·방송·곡 250 → 26, 정확도 60% → 조건을 조이면 약 5%
중국어 위키백과, 사람               사람 120 → 8, 대부분 다른 사람        ✗
NetEase 가수 이름                   사람 150 → 한자 이름 7 (5%)
네이버 지식백과 한자                사람 — 동명이인을 가를 수 없다        ✗
Baidu Baike                         직접 캡차 · 서버 SearXNG 의 baidu 엔진도 「Suspended: CAPTCHA」  ✗
Douban                              subject_suggest 빈 응답(차단)          ✗
```

**결론: 권위 있는 중국어 표기가 세상에 존재하는 대상은 10% 안팎이다.** 무명 한국인의 중국어 이름은 어디에도
정해져 있지 않다 — 중국 매체도 한자를 모르면 추측해서 쓴다.

## ③ LLM 잠정 채움의 품질 — 블라인드 140건

권위 출처(wikidata·tmdb·itunes·위키백과)의 zh 를 **가리고** 운영과 같은 프롬프트·모델(gemma)로 채우게 해
정답과 대조했다. 표본이 권위 출처 보유분이라 비교적 유명하다 — **상한으로 읽어야 한다.**

```
유형     정답과 같음   틀린 값의 성격
기관       71%        허용 가능한 다른 표기 (韩国教育部 vs 大韩民国教育部 · SK电信 vs SK电讯)
그룹       70%        라틴·중문 관례 차이 (Boohwal vs 复活)
사람       60%        ★읽기가 같고 한자가 틀림 — 오답 15건 중 9건 (郑星一→郑成一 · 韩宝凛→韩宝蓝)
곡         33%        영문 제목은 답 안 함(정답도 영문) · 한글 제목은 직역
방송       20%        공식명 아닌 직역 (惊叹的周六 vs 惊人的星期六 · 偶像房间 vs 偶像室)
```

사람 것만 성격이 다르다. **사람이 봐도 틀린 줄 모르는 값**이라 정정도 안 들어온다. 그래서 사람만 문을 따로 뒀다.

## ④ 한 일

```
8ef023d  잠정 채움에 유형 문 (KDB_ENRICH_PROVISIONAL_EXCLUDE_TYPES, 기본 person)
c23bda9  잠정 채움이 **요청 경로**(enrich/orchestrator)에 없었다 — 자동 레인에만 있었다
046e105  NetEase 곡 제목 레인 + 잠정값이 드레인 선택 목록에서 빠져 있던 것
.env     KDB_ENRICH_PROVISIONAL_LOCALES=zh,zh_hant  (09-17 에 만들고 꺼 둔 스위치를 켰다)
```

★**스위치를 켜고도 0건이었다.** 요청 상위 25건을 돌리니 `filled=map[]`. 잠정 채움이 자동 레인
(`agents/enricher`)에만 있고 `enrich/orchestrator` — prepare 의 bgEnrich 와 enrich-test 가 쓰는, **소비자 요청이
부르는 바로 그 길** — 에는 없었다. 수요가 있는 행이 그 경로로 들어오는데 거기서만 빠져 있었다.

★**잠정값이 쌓이면 권위 레인이 닿지 못하는 칸이 된다.** 등급(9)은 맨 아래인데 드레인들이 「덮어도 되는 칸」을
고를 때 쓰는 `MachineFilledSourcesWeakerThan` 목록에 잠정이 없었다. 채운 만큼 막히는 구조였다 — 목록에 넣고
시험으로 못 박았다.

## ⑤ 결과 — 중국어를 얼마나 채웠나

사람을 뺀 요청 대상 686건을 배포된 보강 경로로 한 바퀴 돌렸다(운영 바이너리, 4병렬).

```
잠정 채움     zh 253칸 · zh_hant 265칸   (켜기 전 0칸)
요청 대상(사람 외) 중국어 보유   1,840 → 2,068 / 2,516  (73% → 82%)
아직 빈칸     448 (LLM 이 답을 못 한 것 — 라틴 이름·모호한 대상)
사람          372칸은 그대로 빈칸 — 유형 문으로 막아 뒀다(오너 결정 대기)

유형별 잠정: 곡 45 · 단체 30 · 캐릭터 26 · 행사 25 · 방송 25 · 기업 24 · 정부기관 19 · 그룹 14 · 기획사 10 · 학교 8
```

표본 품질(눈으로 확인): 교촌치킨 校村炸鸡 · 대신증권 大信证券 · 아모레퍼시픽재단 爱茉莉太平洋财团 ·
문태고등학교 文泰高中 · 한국도로공사 韩国道路公社 · 스프링 레인 春雨 · 쩔어 Dope. 섞인 잡음도 실측대로다 —
대한기독교감리회 「韩国基督大理会」(監理会가 맞다) · 카니발(밴드) 「嘉年华」(직역).

소비자 응답에는 값과 함께 `provenance: llm-provisional` 이 **요청하지 않아도** 나간다(09-17 인수인계의
②번 항목은 그 사이 이미 해결돼 있었다). 소비자가 자기 값과 비교해 정정을 보낼 수 있다.

netease-zh 첫 회차: 조회 8 · 승급 0 (사유 no_match 4 · 가수 불일치 2 · 중국어 제목 없음 2). 배선과 기록은
정상이고, 실측 수율(10%)대로면 회차당 1건 안팎이라 시간이 쌓여야 보인다.

## ⑥ 다음

```
· 사람 372칸 — 오너 결정. 열면 `KDB_ENRICH_PROVISIONAL_EXCLUDE_TYPES=none` 한 줄이다.
  (60% 정확 · 25% 는 읽기가 같고 한자가 틀린 값)
· 위키백과 한자 레인(기관 12%) — NetEase 와 같은 «고치는 레인». 다음 차례.
· 중국어 위키백과 경유 앵커 찾기(5%) — 조건을 조인 뒤.
· 잠정값 정정 회수 — 소비자가 보내는 값과 우리 잠정값을 대조하는 4층(09-17 문서 §4)은 여전히 미구현.
```

## ⑦ 이어서 — 일본어까지 열고, 사람도 열었다 (오너: «계속 둘 수는 없잖아»)

### 일본어 블라인드 170건 — 중국어와 같은 방법

```
유형        정답과 같음   틀린 값의 성격
기관          82%        표기 차이
곡            80%        대부분 라틴 제목 그대로
사람          60%        음역 변형 (ジェガル vs チェガル)
그룹          55%        가타카나 vs 라틴 관례 (WINNER vs ウィナー)
드라마·영화·방송 36%      **공식 일본 개봉명과 다른 직역** (약한영웅: 공식 弱いヒーロー vs 弱き英雄)
```

지어낸 값이 아니라 «공식명이 아닌 다른 표기»다. 작품의 공식 일본명은 TMDb 가 가진 것이 전부이고,
빈칸으로 남은 것은 TMDb 가 모르는 롱테일이다.

### 켠 것

```
.env  KDB_ENRICH_PROVISIONAL_LOCALES=zh,zh_hant,ja
      KDB_ENRICH_PROVISIONAL_EXCLUDE_TYPES=none      ← 사람도 연다(오너 지시)
```

사람 중국어는 25%가 「읽기는 같고 한자가 틀린」 값이다. 그래도 연 이유는 09-17 의 판단 그대로다 —
빈칸은 오답을 막는 게 아니라 통제를 넘기는 것이고, 잠정은 **무엇에든 밀리는 등급**이라 권위 값이
오면 자동으로 사라진다. 그래서 같은 날 «고치는 레인» 둘을 먼저 깔았다.

### 결과 — 요청 온 개체 4,412건 기준

```
              켜기 전        지금        전체 active 빈칸
일본어 빈칸     563    →      71         1,607 → 1,115
중국어 빈칸     800    →     521         2,210 → 1,931
번체 빈칸       785    →     461         2,136 → 1,811
잠정 칸        zh 253 → 537 · zh_hant 265 → 593 · ja 0 → 493
```

오늘 채운 칸의 출처: llm-provisional 1,024 · kana-rule 138 · wikidata-label 91 · codex-fallback 67 ·
gtranslate 55 · tmdb 41.

소비자 응답 예(요청 그대로):
```
강은비  ready  ja カン・ウンビ(rule-transliteration) · zh 姜恩妃(llm-provisional) · zh_hant 姜恩妃
```

### 고치는 레인 둘 — 켰고, 첫 회차에서 결함 하나를 잡았다

```
kowiki-hanja  조회 20 · 적용 3   한국 기관·인물의 공식 한자(6등급) → 잠정값을 밀어낸다
netease-zh    조회 20 · 적용 2   플랫폼에 등록된 중국어 곡 제목(4등급)
```

★첫 회차에서 `권성준`(나폴리 맛피아)의 번체가 `南星薰` 으로 들어갔다. **위키데이터 앵커가 남성훈을
가리키고 있었고**, 레인이 그 문서의 한자를 가져왔다(`984495c` 에서 제목 대조를 걸어 막고, 값은 되돌렸다).
같은 행의 `zh 南星薰` 은 **위키데이터 라벨**로 들어온 값이라 내 레인이 만든 것이 아니다 — 앵커 교정은
정체성 영역이므로 넘긴다.

### 남은 것

```
· 중국어 521칸 — LLM 이 답을 안 한 것들(라틴 이름·모호). 라틴 공식명은 중국 매체도 그대로 쓰므로
  「라틴 이름 그대로 통과」 규칙이 다음 후보다(블라인드에서 정답이 실제로 영문 그대로였다).
· 작품의 공식 일본·중국 제목 — TMDb 밖에서는 못 찾는다. 스트리밍 카탈로그가 다음 공급원 후보.
· 잠정값 회수 — 소비자가 보내는 값과 우리 잠정값을 대조하는 4층(09-17 §4)은 여전히 미구현.
```
