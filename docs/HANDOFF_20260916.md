# KDB 이어작업 — 2026-09-16 16:30 기준

## 오늘 배포된 것 (main, 전부 회귀 통과 + CI success)

| SHA | 내용 |
|---|---|
| a023989 | 새 유형 앵커 레인(org_anchor_drain) + occupation_domain 뒤채움 |
| fcf5af9 | 앵커 저장 실패를 "승급"으로 적던 것 |
| 396f576 | 앵커 감사를 person·character → **전 유형**으로 |
| 7374253 | **codex 폐기** (실행경로·CLI·Node·인증마운트·설정) |
| 90dd4e4 | 이름표를 사실대로 (gpt-5.5 → gemma) |
| a3a426b | 메뉴 대기 건수 배지 |
| 34557de | **요청 훅** — 소비자가 물은 낱말의 candidate 행을 그 자리에서 다시 민다 |
| 5b1b198 | **프롬프트 예시 누출 차단 + 인용 대조** (지어낸 근거로 승급 못 하게) |
| 542828c | **판정기가 제목만 읽고 있었다** — 네이버를 본문 유무로 부른다 + 일일 예산 |

## 운영에서 돌고 있는 새 레인 2개

- `OrgAnchorDrain` — 10분 주기, 1회 12건. `KDB_ORG_ANCHOR=0` 으로 끔.
- `OccupationDomainFill` — 5분 주기, 1회 200명. `KDB_OCCUPATION_FILL=0` 으로 끔.
- `demand.Lane`(요청 훅) — **주기 없음, 요청이 걸면 돈다.** `KDB_DEMAND_LANE=0` 으로 끔.
  엔티티당 1시간 선점(last_enriched_at, bgEnrich 와 같은 칸), 동시 3. 15분마다 집계 로그.

성과 (배포 전 → 16:15):
```
occupation_domain   99 → 3,533+     (미상 5,329 → 1,979)
gender             103 → 3,591+
새유형 active        38 → 81
새유형 앵커          17 → 50
government_body  3 → 18 (en/ja/vi 14건씩) · school 1 → 7 (7/7/7)
```

## gemma 판정 품질 — 2026-09-16 저녁 실측 (운영자 물음에 대한 답)

물음: "보통 cli 를 돌릴경우 gemma가 제대로 판단해야되는데 걱정이된다."

**앵커 감사 CLI 는 모델을 안 쓴다.** `AuditPersonAnchors` 는 위키데이터 클라이언트만
받고 P31↔유형 표 대조로 판정한다. gemma 가 실제로 판단하는 곳은 cand-evidence ·
EvidencePass · active_audit · keyword_triage 다.

Sonnet 에이전트로 오늘 승급 40건 전수 판정:

```
근거정합성  OK 26 / 날조인용 12(30%) / 과잉추론 2
사실정확성  맞음 11 / 틀림 1 / 모름 28
```

★원인은 **모델이 아니라 둘**이었다.

1. **프롬프트가 답을 흘렸다.** identity 예시로 «예: SF9 멤버» 를 주고 있었고,
   그 이름이 그대로 답으로 샜다. identity 에 SF9 가 든 활성 행 5건 중 4건의
   근거에 SF9 가 한 줄도 없었다 — 옌안(근거는 중국 도시 延安 기사) · 타쿠야
   (기무라 타쿠야) · 유성영(골프 칼럼니스트) · 윤산하(아스트로).
   → 예시 제거 + 인용 대조(5b1b198). 오염 4건은 강등/정정하고 dataqa_log 에 기록.

2. **판정기가 제목만 읽고 있었다.** 8-16 에 구글뉴스가 1순위가 되면서 네이버가
   사실상 안 불렸는데, 구글뉴스 RSS 는 본문을 주지 않는다(11,574건 실질본문 0건,
   평균 48자). 오늘 승급 285건 중 238건(84%)이 구글 단독.
   → 네이버 호출을 «건수»에서 «본문 유무»로(542828c).

★버린 방식도 적어 둔다. 판정문에서 고유명사를 뽑아 근거와 대조하려 했는데
운영 데이터 2,026건에 걸어 보니 1,251건(62%)을 잡고 대부분 오탐이었다 —
"김재환의"(조사) · "NiziU"(근거는 "니쥬") · "장편영화"(일반어). 한국어 교착과
표기 변형이 그 방식을 못 쓰게 한다. 코드로 옮기기 전에 SQL 로 재 봐서 잡았다.

★Sonnet 을 판정기로 붙이는 건 아직 안 했다. 헤드라인 한 줄만 주면 어떤 모델이든
사전지식으로 메우고 **똑똑할수록 더 그럴듯하게** 메운다. 근거를 두껍게 한 뒤
효과를 다시 재고 판단할 일. 저장소에 anthropic 경로는 아직 없다.

## 남은 작업 순서

1. **brand_place 분리 (3위)** — 재조사 결과 "쪼갤 문제"가 아니었음.
   ★전제가 **저절로 오지 않는다**(2026-09-16 저녁 정정). 앵커 감사는 티커가 아니라
   일회성 CLI(`kdb-app anchor-audit N`, main.go:687)다. 30일 캐시가 막고 있던 게
   아니라 **아무도 부르지 않아서** 안 돈 것이다.
   미감사 **2,174건**: drama 739 · group 425 · show 209 · movie 209 · song_album 121
   · agency 96 · brand_place 75 · channel_outlet 66 · event_tour 46 · person 115 …
   (앵커 보유 active 5,971건 중). 감사 CLI 를 돌려 화면에 띄우는 것이 첫 동작.
   감사를 상시 레인으로 올릴지도 같이 판단할 일.
2. **나머지 유형 분할 (5위)** — occupation_domain 이 다 차면 필요 없을 수 있음.
3. **메뉴 통합 (4위 잔여)** — 배지는 넣었음. ①유입 4화면이 같은 두 표를 보는 건 여전히 통합 후보.

## 발견했으나 손대지 않은 것

- **`testdb` 통합시험 20파일 59개가 전부 조용히 건너뛴다.** `KDB_TEST_DATABASE_URL` 을
  거는 곳이 저장소에 하나도 없다(회귀 스크립트도 CI 도). 회귀가 쓰는 DB 는
  `kdb_platform_migration_test` 인데 harness 는 `kdb_workflow_test` 만 받는다.
  이름을 맞추거나 변수를 걸면 살아난다.
- **정규화 키에 함수 색인이 없다.** `lower(regexp_replace(btrim(canonical_ko),…))` 로
  거는 조회가 `EnqueueResearchDetailed` 한 함수에만 셋이고, 각각 14,569행 순차 스캔
  44ms 다. 함수 색인 하나면 셋이 같이 빨라진다 — 마이그레이션이라 운영자 판단.
- **`이재명` 의 앵커 Q6514101 은 1991년생 축구선수다.** 오늘 20회 요청됐고 candidate
  에 멈춰 있다. 요청 훅이 "레이어가 돌았으면 승급" 규칙을 썼다면 축구선수 표기가
  나갔을 것이라, 그 규칙을 아예 뺐다. 앵커 자체를 고치는 것은 앵커 감사의 몫.

- **근거 대장의 69%가 본문 없는 제목 줄**이다(google-news 11,574건). 542828c 로
  앞으로 들어오는 것은 네이버 본문이 붙지만, **이미 쌓인 것은 그대로다.**
  과거 승급분을 다시 볼지는 판단 대기.
- **트래픽의 68%가 소비자 ID 없이 들어온다.** `write` 등급 키로 인증되지만 소비자 행에
  안 묶여 원장에 `operator` 로 기록됨(1,233/1,807). 그래서 `/v1/my/changes` 와
  소비자별 되물음 알림이 이 트래픽에 안 닿음. read 키 소비자는 3곳(112·64·1건)뿐.
- `canonical_*_source='codex-fallback'` 4,990칸 — 키 이름은 "LLM 이 지어낸 값"이라는
  **의미 키**라 그대로 둠(hideLLMServe·stripLLMOnlyLocales·provenance 'llm-only'·
  absent_reason 이 이 문자열을 봄). 표시명만 고쳤음.
- `anchor-contradicts-type` 불변식 185 (옛 수치). 감사 확대분이 더해지는지 다음 주기 확인 필요.
- 도시가 government_body 로 앉아 있음(부산시·밀양시·동두천시). 표에 city→brand_place 를
  적어 두어 «유형 어긋남»으로 보이게 했음. 옮기는 건 유형 감사의 몫.
- 1,984건 dead-scope 기각에 위키데이터 앵커 없음 — scope-reopen 이 못 닿음.
- 기계번역 4,204칸 — 비울지 운영자 판단 대기.
- 서버 이전 잔여: nginx/TLS 가 아직 옛 서버(114.203.210.38), kdb-shim 이 중계.
  앱은 서버23(atikar)에서 돌고 옛 서버 kdb-app 은 꺼져 있음.

## 작업 환경 (중요)

- 로컬에 Go 없음. 빌드·시험은 전부 서버에서.
- `cd <scratchpad> && ./r3 <타임아웃초> < script.sh` → 서버23(aiin@atikar.com:38383)에서 실행
- `./r1` → 옛 서버(kdb@114.203.210.38:38371)
- 빌드체크: `/home/aiin/kdb/bldcheck` 클론에서 `docker run golang:1.23`
- 회귀: `cd /home/aiin/kdb/repo && KDB_REGRESS_REF=<브랜치> bash scripts/run-isolated-regression.sh`
  (약 6~10분. `tail` 때문에 끝나야 출력이 나옴 — waitreg2.sh 로 기다릴 것)
- 배포: main 에 push → GitHub Actions "Deploy Production" (약 1분 30초)
- 운영 DB: `docker exec -i kdb-db psql -qAtX -F ' | ' -U kdb -d kdb -c "..."`

## 오늘 반복해서 만난 결함 유형 (다음에도 나올 것)

1. **"장치는 있는데 아무도 안 켠"** — 5번. TTL 시계 · 앵커 표 · 관리화면 유형목록 ·
   catchall-retype · NavItem.BadgeCount.
2. **"못 한 것을 없다고 적음"** — 3번. 내가 세 번 다 만들었다.
   (거짓 «검색없음» 120건 · 묶음조회 실패 · 저장 실패를 "승급"으로 로그)
3. **자기 주석이 자기 시험에 걸림** — 3번. 없앤 이름을 주석에 적으면 grep 시험이 잡는다.
4. **원인 단정 전에 대조 실험** — 아침에 «경합»이라 단정했다 틀림. 오후엔 main 으로
   대조해서 내 탓임을 확인했고, 고치니 통과했다.
