# 인수인계 — 2026-09-16 밤

오너가 주무시는 동안 진행. 오전·저녁 인수인계는 `HANDOFF_20260916.md`,
`HANDOFF_20260916_EVENING.md`. 큰 틀 재정리는 **`ROADMAP_20260916.md`** 에 있다.

---

## 0. 오너 지적과 그에 대한 답

> *"큰 틀에 할일을 모두 놓치고 지금 당장만 바라보고 있잖아. 길을 잃은듯해."*

맞다. 그래서 TDB 병합·분류 확장·관리자 메뉴 세 가지를 실측하고 `ROADMAP_20260916.md`
를 썼다. 실측 결과 **세 가지는 따로가 아니라 한 줄기**였다:

```
못 답하는 유형 = 보유가 빈 유형 = TDB 에 원본이 쌓인 유형

  government_body  26/68 미답(38%)   보유 23건    TDB admin_region 249
  sports_team       7/32 미답(22%)   보유  8건
  organization     17/95 미답(18%)   보유 16건    TDB organization 2,557
  person           51/2341 미답(2%)  보유 5,561건
```

보유량이 답변율을 결정한다. 그리고 그 보유량을 채울 원본이 TDB 에 있다.

**★TDB 에 대한 가장 중요한 실측: 다국어 표기가 없다.** `name_ko` 와 `addr_en` 뿐이다.
TDB 는 사전이 아니라 **어휘 목록**이다 — 존재 증명과 유형을 주지 표기를 주지 않는다.
이걸 착각하면 "TDB 흡수했으니 표기가 채워지겠지"라고 잘못 기대하게 된다.

---

## 1. 배포한 것

```
fbf2e43  main · kdb-app:ci-20260916-fbf2e43 · healthy
 ├ 67e0b15  로케일 드레인이 en 기계값을 영영 안 집던 것
 └ 6556d5c  근거를 든 정정신고를 게이트가 기각하던 오거부
7bd3227  docs: 로드맵 (배포 없음)
```

회귀 두 건 모두 전부 통과 (전체 33/0 · race 31/0 · 정적 8/8).

### ① 로케일 드레인 — SELECT 와 UPDATE 가 어긋나 있었다

`UPDATE` 는 8개 로케일을 덮는데 `SELECT` 의 재선택 조건은 ja/zh/zh_hant 세 개의
`_source` 만 봤다. 그래서 `canonical_en = gtranslate` 인 행은 **빈칸도 아니고 감시
대상도 아니라서** 영영 다시 집히지 않았다. 앵커를 새로 붙여도 영문 표기가 기계번역인
채로 남는 이유였다.

```
실측: en 기계값 496건 · 대상 1,932 → 4,631
```

조건을 손으로 적지 않고 `wikidataLocaleTargets` 에서 만들어 내도록 바꾸고, 대칭을
테스트로 잠갔다(`TestRefillClauseCoversEveryLocaleItUpdates`).

### ② 오거부 — 근거를 든 정정신고를 문 앞에서 돌려보냈다

`latin_passthrough` 규칙(수록곡 목록 투척 방어)이 **정정신고까지** 막고 있었다.
게다가 정정 경로는 소비자의 `evidence_url`·`reason` 을 게이트에 넘기지 않고 판정이
끝난 **뒤에** 원장에만 적고 있었다 — 근거는 남는데 판단엔 안 쓰였다.

신고(서버가 정하는 `Origin`, 위조 불가) + 유효한 근거 URL 이면 **기각 대신 심사**로
보낸다. 통과가 아니다.

**실증(같은 용어·같은 근거)**:

```
20:21:47  Blingy + melon.com 앨범 → rejected_precheck   (배포 전)
22:16:41  Blingy + 같은 근거      → preparing            (배포 후)
          precheck_reason = latin_correction_with_evidence
```

---

## 2. 데이터 조치 — 전부 감사 원장에 남아 되돌릴 수 있다

```
kwave_kdb_dataqa_log
  correction-label-corrected      22
  false-rejection-reopened        82
  false-rejection-reopen-retry    70
```

### ① 거짓 «codex 검증» 라벨 610건

`bbbe293`(06-13)이 `"codex 검증: "` 을 **고정 문자열로** 박아 놔서 판정자와 무관하게
610건이 codex 라고 적혀 있었다. `gemma` 라 적힌 행은 **한 건도 없었다.**

원문을 덮지 않고 **덧붙였다**:

```
확정  22건 (09-15 af805c1 클램프 이후 — 코드가 codex 호출을 차단한 상태였다)
        → "[판정자 정정: 실제 판정은 gemma …]"
미상 588건 (그 이전 — 라벨이 하드코딩이라 판정자 정보 자체가 없다)
        → "[판정자 미상: 이 «codex» 는 코드에 박힌 고정 문구이며 …]"
```

**판정자를 지어내지 않았다.** 09-08 이전은 codex 가 살아 있었을 가능성을 배제할 수
없어 «미상»으로 뒀다.

### ② 오거부 회수 82건

`status='rejected'` 인데 같은 이름으로 **근거 URL 을 달고** 정정신고가 들어온 것들.
82건 전부 `operator_locked=false` — 사람이 판단한 게 아니라 자동 게이트가 닫은 것이다.

```
MEOVV(미야오)   group           theblacklabel.com      ← 소속사 공식
SNL KOREA       show            coupangplay.com        ← 공식 플랫폼
공조2           movie           koreanfilm.or.kr       ← 영화진흥위원회
EGO GROUP       agency          egogroup.co.kr         ← 본인 공식
+ 애플뮤직/벅스/지니 링크가 붙은 곡 수십 건
```

**active 로 올리지 않고 candidate 로만 돌렸다** — candidate 는 외부로 나가지 않는다.
승급은 기존 근거 경로(인용 대조 포함)가 하도록 뒀다. 신고는 "다시 봐 달라"이지
"네 값이 맞다"는 증명이 아니다.

### ★그러다 진짜 결함을 하나 더 밟았다 — 회수가 즉시 되돌려졌다

10분 만에 21건이 다시 `rejected` 가 됐다. 사유가 전부 같았다:

```
autopilot: on-demand 3회 무검증 → 자동 기각
```

원인: autopilot 의 재시도 횟수는 **`notes` 문자열에 «무검증» 이 몇 번 나오는지로 센다**
(`internal/kdb/autopilot/sweep.go:1996`).

```sql
SELECT array_length(regexp_split_to_array(COALESCE(notes,''), '무검증'), 1) - 1
```

그래서 과거 이력이 notes 에 남은 채 되살리면 **첫 회차부터 3회를 채운 상태**가 된다.
이력을 감사 원장으로 옮기고 깨끗한 메모로 교체했다(«무검증» 0회 · `[cand-evidence`
없음 → 오거부 가드가 살아난다). `last_enriched_at` 도 NULL 로 돌려 재시도 여지를 줬다.

**최종 상태: candidate 70 · active 12.**

승급 12건은 근거를 갖고 올라갔다 — 지어낸 게 아니다:

```
공조2       → "영화 공조2: 인터내셔날로 명시"
SNL KOREA   → "tvN의 예능 프로그램 'SNL Korea 8'로 명시"
MEOVV       → "신인 그룹이자 엘라, 가원 등의 멤버가 속한 K-pop 그룹"
비던(B:DAWN)→ "버추얼 아이돌 그룹 B:DAWN(비던)으로 명시"
```

---

## 3. 새로 드러난 구조적 결함 (아직 안 고침)

### ★«되살리기» 라는 연산이 코드에 없다

오늘 SQL 로 손수 했다. 그래서 첫 시도가 되돌려졌고, 두 번째에야 제대로 됐다.
필요한 것은 `rejected → candidate` 를 **한 번에 안전하게** 하는 함수다:

```
· status 를 candidate 로
· 재시도 이력(notes 의 «무검증») 을 감사 원장으로 이관
· last_enriched_at 초기화
· operator_locked 인 것은 건드리지 않음
· 전건 dataqa 기록
```

지금은 이게 없어서, 오거부를 발견해도 되살리는 일 자체가 위험한 수작업이다.

### ★재시도 횟수를 자유 텍스트 필드의 부분문자열로 센다

`notes` 에 «무검증» 이 몇 번 나오는지로 3-스트라이크를 판정한다. 누가 notes 에
그 단어를 쓰면 카운터가 움직인다. 전용 정수 컬럼이어야 한다.

### ★candidate 정체 1,220건

로그에 계속 뜬다: `candidate-stuck total=1220 over_21d=183 oldest=115d`.
승급도 기각도 안 된 채 115일 된 것이 있다.

---

## 4. 막혀 있는 것 — 오너 손이 필요하다

### ① `auth.json` (codex)

`/home/aiin/kdb/codex-home/auth.json` — **순서가 중요하다.**

```
① auth.json 넣기
② /home/aiin/kdb/repo/.env:77 을 KDB_LLM_CORRECTION=codex 로
③ kdb-app 재기동
④ 정정신고 하나로 원장에 «codex 검증…» 이 찍히는지 확인
```

②를 ① 없이 하면 안 된다. `RunP` 는 codex 실패 시 **오류를 돌려준다** — gemma 로
폴백하지 않는다(폴백은 일일 상한 소진일 때만). 정정 검증이 전부 `pending` 으로 쌓인다.

없어도 시스템은 멈추지 않는다. gemma 가 답하고, **이제는** 원장에 `gemma 검증` 으로
사실대로 적힌다(f4c8170 이후).

### ② 대기 정정 25건 (최고령 2026-08-15)

판단이 어려워 운영자 심사로 넘어온 것들. 방침만 주시면 처리한다. 22건이 근거 URL 을
갖고 있는데 **검증기가 그 URL 을 읽지 않는다**(신뢰 도메인 지름길에만 쓰고 버린다).
기각 1,038건 중 926건이 URL 을 갖고 있었다.

---

## 5. 다음 순서 (`ROADMAP_20260916.md` 결론)

```
1위  오거부 의심 재심            — 오늘 82건 회수. 되살리기 연산을 코드로 만들어야 재발 방지
2위  앵커 불일치 470건 정리      — 오염이 8개 로케일로 번지는 경로
3위  /admin/health 한 장          — 아래 다섯 숫자만 보이면 된다
4위  TDB person 200건 표본 검수   — 4만 건 넣기 전에 비율부터
5위  TDB organization/education 흡수
6위  government_body 근거 규칙    — 뉴스 대신 공식 사이트를 1차 근거로
```

`/admin/health` 에 들어갈 숫자 (전부 오늘 실측):

```
앵커 불일치            470   유형어긋남 278 · 인물↔캐릭터 139 · 이름항목 33 · …
기계번역 표기       30,807   active 칸의 28% — 빈칸이 아니라서 아무 화면에도 안 뜬다
오거부 의심             82   오늘 회수 → 0. 다시 쌓이는지 봐야 한다
정정 적체               25   최고령 08-15
candidate 정체       1,220   21일 초과 183 · 최고령 115일
TDB 흡수                 0 / 48,000
```

**1~3위를 TDB 앞에 두는 이유**: 지금 규모를 키우면 오염과 오거부도 같이 커진다.
그리고 보는 창이 없으면 커진 걸 모른다. 오늘 하루에만 «장치는 있는데 아무도 안 켠»
경우를 여덟 번 만났는데, 그게 우연이 아니라 **볼 화면이 없어서**다.

---

## 6. 오늘 내가 틀렸던 것

기록해 둔다. 다음 사람이 같은 데서 헛돌지 않게.

1. **"제목만으로 승급된 건 없다"** — snippet 이 비었는지만 쟀지 내용이 제목의 반복인지
   안 쟀다. 다시 재니 11,585/16,864(69%)가 제목 반복이었다. Sonnet 에이전트 진단이
   맞고 내가 틀렸다.
2. **"OO 자리표시자 누출 9건"** — `LIKE '%OO%'` 가 `COOL`·`BOYNEXTDOOR`·`WOODZ` 를
   잡은 것이었다. 철회.
3. **"대상이 100배"** — 적중률을 안 재고 대상 크기만 비교했다. 실제 8배.
4. **정정 4건 중복을 "재시도 폭주"로 의심** — 로케일 4개(en·es·ja·vi)를 각각 보낸
   정상 동작이었다.
5. **21:50 정정 4건이 행을 안 남긴 것을 버그로 의심** — 미보유 용어(김수아)라
   발굴 큐로 간 설계대로의 동작이었다.
6. **오거부 회수를 status 만 바꿔서 했다** — 재시도 카운터를 안 지워 10분 만에
   21건이 되돌려졌다. 2차 조치로 바로잡았다.
7. **기계번역 4,204칸이라고 적었다** — 실측 30,807칸. 로드맵에서 수정했다.

공통점: **재기 전에 말했다.** 재고 나서 말하면 틀리지 않는다.

---

## 7. 밤 늦게 찾은 것 — 내일 첫 번째로 할 일

로케일 드레인 수정(`67e0b15`)을 배포하고 실측하니 `checked=80 filled=0` 이었다.
파 보니 두 가지가 나왔다.

### ① 내가 효과를 과장했다

"대상 1,932 → 4,631" 은 SELECT 자격 기준이고 **즉효가 아니다.** en 기계값 494건 중
**340건은 이미 방문해 «레이블이 없거나 덮어쓰기 대상이 아님»으로 판정된 것**이었다.
새로 열린 실질 대상은 미방문 60건 + 앞으로 앵커가 바뀌는 행이다.

고친 것 자체는 유효하다(구조적 비대칭을 없앴고 테스트로 잠갔다). 다만 **효과는 앞을
향한다** — 앵커를 새로 붙인 행의 en 이 이제 재선택된다. 전에는 영원히 안 됐다.

### ② ★이름 대조 규칙이 두 개고 서로 반대 판정을 낸다

오늘 `active-anchor` 로 앵커를 붙인 20건 중 **18건이 여전히 기계번역 en** 이다.
드레인이 방문은 했는데 7건이 이 사유로 막혔다:

```
냉부해               → QID Q18604987  ko 레이블/별칭이 정본과 불일치
쇼미더머니6           → QID Q31179526  불일치
동상이몽-너는 내 운명  → QID Q33185231  불일치
UBC울산방송 · 만원의 행복 · 전국 TOP10 가요쇼 · 내가 매일 기쁘게
```

| 규칙 | 위치 | 별칭 | 판정 |
|---|---|---|---|
| `findAnchorQID` | `org_anchor_drain.go` | 인정(네 관문 통과 후) | **붙인다** |
| `wikidataAnchorMatches` | `wikidata_locale_drain.go` | **불인정(레이블만)** | **거부한다** |

`냉부해` 는 `냉장고를 부탁해` 의 줄임말이라 위키데이터에 별칭으로만 있다.

**어느 쪽도 틀리지 않았다.** 별칭 불인정 가드는 실제 오염 4건(바이브→네이버 VIBE,
DK→Dplus Kia, 제이→DAY6 제이 박, 라미→김성경)을 겪고 만든 것이다. 문제는 **이미 우리
레인이 네 관문으로 검증해 붙인 앵커를 다른 레인이 처음 보는 것처럼 다시 심사**한다는 것.

**고칠 방향**: 앵커의 출처(provenance)를 기록해 우리 검증 레인이 붙인 앵커는 드레인이
재심하지 않게 한다. 출처 미상에는 엄격한 대조를 그대로. `kwave_entity_external_refs`
에 출처 컬럼이 있는지부터 확인 — 없으면 마이그레이션이 필요하다.

**★이걸 고치기 전에는 `active-anchor` 를 더 돌려도 표기가 안 바뀐다. 앵커만 쌓인다.**

오늘 밤은 여기서 멈춘다 — 배포를 두 번 했고, 지켜볼 수 없는 시간에 세 번째 구조 변경을
넣지 않는다.
