# 이어서 — 2026-09-23 (소비자 신고 3건 대응)

새 세션은 이 문서와 [`CONSUMER_REPORTS_20260923.md`](CONSUMER_REPORTS_20260923.md) 둘만
읽으면 이어받을 수 있다. 전자가 **지금 무엇을 해야 하나**, 후자가 **왜 그렇게 됐나**다.

---

## 1. 지금 상태

```
브랜치   fix/consumer-api-contract-20260923   (origin 에 있음, main 위로 정리됨)
PR       #2  https://github.com/rickyjoo73/kdb/pull/2   OPEN · 머지 안 함
배포     안 했다. main 푸시 = GitHub Actions 자동 배포이고, 오너가 «PR 올리고 대기» 를 골랐다
원장     4건 유형 교정 + 중복 1쌍 병합은 **이미 운영에 적용돼 있다**(아래 3절)
시험     go build ./... · go test ./... 통과
```

커밋 6개(최신순): 원장 실측 답변 · 유형 필터 가림 수정 · 신고 답변 문서 ·
준비건 값 구분 문서 · zh 음차 기준 · 조회 status 계약.

⚠ **PR #2 에는 이 작업만 들어 있다.** 한때 `fix/common-noun-ledger` 커밋 4개가 딸려
있었고 2026-09-23 에 `git rebase --onto origin/main` 으로 떼어 냈다. 그 브랜치는 그대로
살아 있으니 **따로 머지해야 한다**(a7e46f8 tdb 증분 · 5e881fe I13 · 979ad2c 고정DB 병합 ·
cf252b6 앵커 판정 집행).

## 2. 다음에 할 일 — 순서대로

### ① 배포 판단 (오너)
PR #2 를 머지하면 소비자 6곳이 보는 **계약이 바뀐다**. 바뀌는 목록은 PR 본문의 표에 있다.
가장 큰 것: `found` 의 뜻(부분일치는 이제 `related`), 정정신고 기각 422→200.
소비자 세 곳은 이미 자기 쪽을 고쳤다고 신고서에 적었다 — 지금이 바꾸기 좋은 때다.

### ② `wikidata-label` 재라벨 — **오너 판단 먼저, 그다음 내가 레인**
실측(표본 300칸): 위키데이터 라벨과 **불일치 6.5%**, 그 언어 라벨이 **아예 없는데도**
`wikidata-label` 로 나가는 칸 **3.0%**. 뿌리는 0049 의 `DEFAULT 'wikidata-label'` 잔재다.
**값은 대체로 우리 쪽이 낫다** — 틀린 것은 라벨이다.

사실대로 내리면 `verified_only` 통과 칸이 **약 9.5%(23,751 중 2,200여) 줄어든다.**
갈래 둘 중 하나를 오너가 골라야 한다:
- ① 전부 사실대로 내린다 — 신뢰 신호가 정확해지고, 검증 폭은 준다
- ② 유도 경로가 결정적인 것(`opencc` 변환 등)은 검증으로 남기고 나머지만 내린다

고른 뒤 레인은 이렇게 짠다: QID 보유 칸마다 `wbgetentities` 로 라벨을 받아
같으면 유지, 다르면 사실 라벨로 교체, 없으면 `source-unrecorded`(신규, 미검증).
반드시 dry 로 «몇 칸이 verified 에서 빠지는가»를 먼저 찍는다.

재현(표본 뽑기):
```bash
ssh -i ~/.ssh/id_ed25519_atikar -p 38383 aiin@atikar.com \
  'docker exec -i kdb-db psql -U kdb -d kdb -At -F"|"' <<'SQL' > /tmp/wdcells.txt
SELECT r.external_id, v.loc, v.val, e.kid, e.canonical_ko
  FROM kwave_entities e
  JOIN kwave_entity_external_refs r ON r.entity_id=e.id AND r.provider='wikidata'
   AND r.external_id ~ '^Q[0-9]+$',
  LATERAL (VALUES ('ja',e.canonical_ja,e.canonical_ja_source),('vi',e.canonical_vi,e.canonical_vi_source),
                  ('zh',e.canonical_zh,e.canonical_zh_source),('en',e.canonical_en,e.canonical_en_source)) AS v(loc,val,src)
 WHERE e.status='active' AND COALESCE(v.val,'')<>'' AND v.src='wikidata-label'
 ORDER BY md5(e.id::text||v.loc) LIMIT 300;
SQL
```
⚠ 위키데이터 API 는 **User-Agent 가 없으면 403** 이다.

### ③ 소비자 type 힌트 vs 위키데이터 P31 — dry 로 규모부터
`aijudge.ClassifyFromEvidence` 가 소비자 힌트를 **P31 보다 앞**에 둔다
(`internal/kdb/aijudge/classify_evidence.go` ①). 그래서 카카오가 `event_tour` 로,
케이뱅크가 `brand_place` 로 굳었다.

```
활성 중 «소비자 type힌트=» 로 유형을 받은 것   6,625
그중 위키데이터 앵커 보유                      2,106   ← 재판정 가능 모집단
```
전면 역전은 하지 않는다(기사를 본 쪽의 판단이 맞는 경우가 많다).
**«P31 이 명확하고 힌트와 충돌할 때만 P31»** 로 좁히고, dry 로 몇 건이 바뀌는지 찍은 뒤
`type-retrace` 와 같은 모양(`dataqa_log` 스냅샷 + `[retrace:type-fix a→b]` 노트)으로 쓴다.

새로 깔아 둔 신호를 쓰면 대상을 고를 수 있다 — 조회에서 유형 필터가 보유 이름을 가리면
인입 기록에 `type_mismatch:<우리 유형>` 이 남는다(`kwave_kdb_request_terms.item_status`).

### ④ 제안 표기 — 배선했으니 **들어오는지 확인**
`savePrepareSuggestions` 가 만들어진 뒤 호출되지 않아 표가 0행이었다. PR #2 에서 배선했다.
배포 뒤 확인:
```sql
SELECT producer, count(*), max(last_seen_at) FROM kwave_kdb_suggested_names GROUP BY 1;
```
0행이면 배선이 또 끊긴 것이다. 소비자(global.nbntv)가 `suggestion_meta.producer` 를
보내고 있으므로 값이 와야 한다.

## 3. 원장에 이미 적용한 것 (되돌리는 법 포함)

되돌릴 근거는 전부 `kwave_kdb_dataqa_log` 에 있다(`verdict='retrace-type-fix'`,
`model IN ('wikidata-p31','operator-merge')`, `old_value` = 그때의 유형).

| 대상 | 전 | 후 | 근거 |
|---|---|---|---|
| K0000192 삼성전자 | brand_place | company | Wikidata Q20718 P31 |
| K0008647 네이버 | brand_place | company | Q485639 |
| K0014955 카카오 | event_tour | company | Q17684312 |
| K0023012 케이뱅크 | brand_place | company | Q31180393 |
| K0006914 사장님 귀는 당나귀 귀 | drama | show | KBS2 예능 |
| K0003545 사당귀 | active | **rejected(병합)** | 위 행의 별칭이 됐다 |

병합 문구는 기존 경로와 같다(`disambiguator: merged into … [uuid] (abbreviation)`) —
kid 로 물으면 퇴역 행도 답한다.

## 4. 알아 두면 시간 버는 것

- **접속**: `ssh -i ~/.ssh/id_ed25519_atikar -p 38383 aiin@atikar.com` (2026-09-23 재등록).
  psql 은 `docker exec -i kdb-db psql -U kdb -d kdb`.
- **읽기 API 키는 아직 없다.** `kdb-app` 컨테이너의 `KDB_API_KEY` 는 **빈 값**이라
  서버 안에서도 HTTP 로 확인할 수 없다. 서빙 확인은 psql 로 하거나 오너에게 키를 받는다.
- **로컬에 Go 가 없었다** — `brew install go` 로 넣었다(1.27.1). CI 와 같은 게이트를
  로컬에서 돌릴 수 있다: `go build ./... && go test ./...`.
- **다른 에이전트가 같은 저장소에서 일한다.** 커밋 전 `git fetch`, 작업은 브랜치에서.
  오늘도 작업 중 origin/main 이 bbab140 으로 움직였다.
- **소비자가 «없다»고 하면 원장을 먼저 본다.** 오늘 신고 다섯 건 중 «없다»는 주장이
  전부 틀렸다 — 있는데 유형 때문에 안 보였다.

---

## 5. 오후 진행 (2026-09-23) — 배포했고, ③은 닫았고, 더 큰 것이 드러났다

**배포**: PR #2(조회 계약)·#3·#4(hint-p31-retrace)·#5(subtype 버그) 머지 = 운영 반영.

**③ type 힌트 대 P31 — 닫음.** `kdb-app hint-p31-retrace [n] [go]`.
- 첫 dry(2,113건): 옮김 124 중 ~100건이 사람·작품으로 가는 방향이었고 **거의 전부 앵커가 틀린 것**
  (미란·미호→성인배우 QID, 매미·미래·솜사탕→동명 가수, 단발머리(조용필 곡)→걸그룹).
  사람 이름·흔한 낱말은 kowiki 제목 일치로 신원을 못 가른다.
- 그래서 옮겨 갈 곳을 기관·기업 계열로 좁혔다(#4). 다시 dry: **5건**. 검토 후 4건 적용
  (디나미스 원·스마일게이트→company, 서울대학교어린이병원→organization, 필굿뮤직→agency),
  `유니버스`는 보류(앵커가 팬 플랫폼 앱). 되돌리기: `dataqa_log` model='wikidata-p31'.
- 카카오류(힌트가 기관을 잡동사니 칸에 넣은 것)는 드물다 — 오전 4건이 거의 전부였다.

**버그**: `catchall-retype`·`hint-p31-retrace` 가 `kwave_entities` 에 **없는** `subtype` 을 써서
`go` 가 한 건도 못 썼다(#5 로 고침). catchall-retype 은 그동안 dry 만 의미가 있었다.

### ★다음 할 일 1순위 — 틀린 앵커가 «검증됨» 으로 서빙 중
```
활성 행 중 앵커 감사 «어긋남» 판정 + 그 ref 가 아직 붙어 있음   436
  type-mismatch 276 · human-on-character 139 · not-human 19 · …
  human-on-character 139건 중 wikidata-label 표기 칸 보유       114
예) 단발머리(곡) en="Bob Girls"(wikidata-label) · 매미 en="MEMI"(wikidata-label)
```
집행기 `anchor-enforce`(저장된 판정을 읽어 앵커 철회·표기 비움·등급 강등, 기본 dry)는
`fix/common-noun-ledger` 에 있고 **아직 main 에 없다.** 남은 4커밋을 main 위로 옮긴 로컬
브랜치 `fix/common-noun-ledger-rebased` 가 있다(빌드·시험 통과, 푸시 안 함). 여기엔 인입
동작을 바꾸는 I13(이름이 이미 별칭이면 새 UUID 를 안 만든다)이 함께 있다 → 오너 확인 후 머지.
이 건은 ②(`wikidata-label` 재라벨)와 뿌리가 겹친다 — 틀린 앵커의 라벨이 곧 틀린 «검증» 칸이다.
앵커를 먼저 떼고 나서 ②의 표본을 다시 뽑아야 불일치율이 제대로 나온다.

### ④ 제안 표기
배포 뒤 1시간 안쪽 소비자 prepare 3묶음, `kwave_kdb_suggested_names` 여전히 0행, 저장 오류 로그 없음.
그 3묶음이 제안을 실었는지는 원장으로 못 가린다(요청 본문을 안 남긴다). 트래픽이 쌓인 뒤 다시 본다.

## 6. 저녁 진행 — 중국어·일본어가 왜 안 채워졌나 (2026-09-23)

요청 대상(최근 14일 요청어 = canonical_ko, 활성 4,517건) 빈칸: ja 127 · zh 552 · zh_hant 509.
잠정 채움(zh·zh_hant·ja, 사람 포함)은 켜져 있었다. 안 채워진 이유는 둘이었다.

**① 간체 칸만 빈 행 92건 — 순서 결함(#9).** 간체↔번체 변환(L3.5)이 잠정 채움·L4 **앞**에
있어 LLM 이 번체만 답하면 간체가 빈 채 남았다. LLM 층 뒤에 변환을 한 번 더 돌고, 잠정
기준에서 나온 변환값은 `llm-provisional` 을 물려받는다(등급을 올리지 않는다). 기존 84칸 백필
(`dataqa_log` verdict=`zh-variant-backfill`), 이체자가 섞인 8건은 건너뜀.

**② 요청 대상 대부분이 한 번도 시도되지 않았다(#10).** ja 빈칸 127 중 103, zh 552 중 318.
채움 에이전트는 «빈칸이 하나라도 있는 활성 행» 전부에서 30분에 40건을 고르는데 정렬에
수요가 없었다. demand_gaps 는 재기만 했다. 최근 14일 요청어를 맨 앞에 뒀다(EXPLAIN 84ms,
상위 200 전부 요청 대상). 쌓인 것은 `kdb-app drain-enrich 4` 를 40분 제한으로 돌려 비우는 중.

**머지 대기 — PR #11** (`anchor-enforce` · I13 · tdb 증분). 권한 분류기가 운영 배포로 막아
오너 머지가 필요하다. 머지 뒤: `kdb-app anchor-enforce 500` (dry) → 표본 확인 → `go`.

## 7. 2026-09-24 새벽 — 오너 지시로 Claude 가 직접 판정·채움

- **codex CLI 0.146.0 → 0.156.1, 모델 gpt-6-luna**(#13). 낡은 CLI 는 GPT-6 을 400 으로 거부했다.
  GPT 일일 상한 300 → 3000(.env).
- **앵커**: 저장 판정 434건 + 전체 재감사로 새로 드러난 50건(감사 받은 적 없던 앵커 892개가 있었다 —
  `단발머리` 가 그 하나)을 Claude 가 판정. `anchor-apply`(#16)로 집행: 철회 233 · 칸 비움 637(스냅샷
  verdict=`anchor-judge-clear`) · 유형교정 142 · 이름항목 철회 17. 철회 뒤 남은 위키백과 제목 파생 칸
  110개는 한 칸씩 봐서 26 비움 · 85 잠정으로 강등(`anchor-judge-downgrade`).
- **채움**: 요청 대상 ja/zh/zh_hant 빈칸 1,159칸을 Claude 가 표기(`llm-provisional`, dataqa_log
  verdict=`claude-provisional-fill` model=`claude-opus-5.5`). 요청 대상 빈칸 en 3 · ja 3 · zh 12 · zh_hant 13.
- **제안 표기 결함**(#15): `parsePrepareTerm` 이 suggestions 를 버렸다. 고쳤고, 빈 칸은 제안으로
  채운다(#16, `consumer-suggestion`). 배포 뒤 prepare 트래픽이 아직 없어 저장 확인은 대기.
- **교정 12칸 — 완료(오너 허락, 2026-09-24)**: 이미 값이 있는데 명백히 틀린 칸(재정경제부 zh=기획재정부
  이름, 기무사 en/ja/zh=다른 기관, 타잔 ja/zh=동명인, 빛나 zh=떼어 낸 가수 앵커의 본명, 단발머리 ja=걸그룹 표기).
  원값은 dataqa_log verdict=`claude-correct`.
- **남은 판정**: 보류 94건(근거로 못 가름) · 운영자 잠금 16건. `제이` 행은 DAY6 Jae 와 ENHYPEN Jay 가 섞였다.
