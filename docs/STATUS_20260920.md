# 2026-09-20 — 중국어 응답률과 묻혀 있던 답

## 0. 한 줄

**소비자가 물은 낱말의 중국어 응답률은 49.5% 다.** 오늘 그중 절반의 원인이
«답이 없다»가 아니라 **«답을 갖고도 안 내보낸다»** 였다는 것을 찾아 155건을 되돌렸다.

## 1. 먼저 자를 고쳤다 — 종전 숫자는 소비자가 겪는 것이 아니었다

대시보드 `[3]` 은 «리서치 큐»를 모수로 «원장에 행이 있는가»를 셌다. 둘 다 틀린 자다.

| | 종전 `[3]` | 새 `[3-0]` |
|---|---|---|
| 모수 | 리서치 큐 | `kwave_kdb_request_terms` — **소비자가 실제로 물어본 낱말** |
| 붙이기 | `canonical_ko` 만 | **정규화 키 + 별칭** (API 가 붙이는 방식) |
| 판정 | 행이 있는가 | **그 로케일 칸에 값이 있고 `active` 인가** |

별칭을 안 보면 «데이식스» 요청이 `DAY6` 에 안 붙어 구멍으로 잡힌다.
그 차이만으로 중국어 응답률이 **44.9% 로 과소 보고**되고 있었다(실제 49.5%).

### 최근 7일, 요청 낱말 4,655개 기준

| | 요청 낱말 | 요청 건 |
|---|---|---|
| A. 중국어 답이 나감 | **2,330** | 5,008 |
| B. `active` 인데 zh 빈칸 | 634 | 1,028 |
| C. `candidate` — 값이 있는데 안 나감 | 19 | 79 |
| D. `candidate` — 값도 없음 | 964 | 1,363 |
| E. 원장에 행 자체가 없음 | 711 | 1,174 |

`[3-1]` 에 «중국어가 안 나가는 낱말»을 요청 많은 순으로 찍는다 —
번역 쪽이 직접 번역해야 하는 목록이 그대로 그것이다.

## 2. 오늘 찾은 것 — 표시가 자기 사유를 부정하고 있었다

원장에 이런 행이 236개 있었다.

```
[revert-term:reject] wikidata Q211650 직업이 비-엔터: "South Korean
association football player"
```

`[revert-term:reject]` 의 명제는 **«이 레코드에 붙은 QID 가 비-K 다»** 인데,
같은 줄이 그 QID 를 **"South Korean …"** 이라고 적고 있다. 기록이 스스로를 반박한다.

죽은 것은 대상이 아니라 «직업이 연예가 아니다» 라는 옛 범위 명제이고,
그건 0143(정치·경제·시사·스포츠)으로 소멸했다.

`scope_phrases.go` 는 이 명제의 다섯 가지 표현을 이미 한 자리에 모아 두었고
**오세훈을 예시로 적어 두기까지 했다.** 그런데 이 계열만 못 걸렀다 —
문구가 아니라 **표시**로 들어왔고, `NotATombstoneSQL` 이 표시를 먼저 보기 때문이다.

### 무엇이 묻혀 있었나

| 직업(위키데이터 설명) | 건수 |
|---|---|
| politician | 26 |
| footballer / association football player | 32 |
| writer | 9 |
| baseball player | 9 |
| volleyball · Go · pro gamer · archer · academic … | 나머지 |

이재명·정의선·류현진은 최근 7일 요청 상위 20 안에 **셋 다** 있었다.
우리는 값을 갖고 있으면서 아무것도 답하지 않았다. **오거부는 최상위 금칙이다.**

### 되돌린 결과

```
판정 225 · active 승급 155 · candidate 되살림 70
  동명이인 보류 4   (조국 · 오너 · 페이즈 · 천보성)
  동명 active 있어 건너뜀 4
  이름항목 0 · 개념 0 · 해외 0 · 조회실패 0
```

되돌린 155행: 영문 152 · 일본어 155 · **중국어 115** · 번체 115.
`confidence` 를 0.72 로 올렸다 — 강등이 0.000 으로 깎아 놓았고 조회 기본
`min_confidence` 가 0.50 이라, 안 올리면 `active` 로 돌려도 **여전히 안 나간다**.

노트를 믿고 되살리지 않았다. 226건 전부 **지금 위키데이터에 다시 물어서**
한국 대상인지 확인했다(이름항목·개념·해외·앵커 유형 가드는 기존 scope-reopen 과 동일).

## 3. 손대지 않은 것과 그 이유

| 대상 | 건수 | 왜 |
|---|---|---|
| `직업이 비-엔터` 인데 설명이 `South Korean` 이 아닌 것 | 12 | 북한(김정)·조선시대 인물(이순신·김홍도·이상설) — **범위 판단이 따로 필요하다** |
| 2026-07-21 감사 강등분 중 «오링크» 사유 | 105 | 경기·경남·구리·광종처럼 **지명/일반어가 person 으로 잘못 앵커**된 것. 이쪽 명제(«참조가 틀렸을 수 있다»)는 아직 살아 있다 |
| 동명이인 표시가 붙은 4건 | 4 | 실존 확인과 «그 이름으로 서빙해도 된다»는 다르다. 되살리되 `active` 로는 안 올린다 |

## 4. 오늘 내가 만든 회귀와 그 구조적 원인

155행을 `active` 로 올렸더니 **그중 16칸이 오염된 채 서빙 대상이 됐다**
(김병현 `金炳賢` · 오세훈 `吳世勳` · 박종현 `朴钟贤`). 표본을 눈으로 보지 않았으면 그대로 나갔다.

원인은 내 레인이 아니라 구조다.

- QA 자체검사(`qaCharsetOK`)는 **값을 채울 때만** 본다.
- 이미 값을 가진 행이 `candidate`/`rejected` 에서 `active` 로 **올라오는 경로**는
  그 검사를 한 번도 안 거친다. 그런 경로가 여럿이다 — kopis·kmdb·tmdb·kofic·
  on-demand·occup-scope.
- 그리고 `RepairZhVariants` 를 부르는 곳이 **일회성 명령 하나뿐**이었다.
  어제 손으로 만든 «간체 칸 번체 0» 은 **그 순간만 0** 이었다는 뜻이다.

→ 자체 수리를 **30분 주기**로 돌린다(`KDB_ZH_REPAIR_INTERVAL_SECONDS`).
   경로마다 가드를 다는 것보다, 새 경로가 생겨도 안 새는 유일한 방법이다.
   16칸은 수리했고 두 번째 실행은 0건이다(멱등).

또, 수리 도구가 «건너뜀 17» 을 건수만 세고 조용히 넘기고 있었다.
찍어 보니 전부 `昇`(16)·`汎`(1) — 두 자체 모두에서 옳은, 어제 검증해 둔 글자다.
**오염이 아니라 고칠 것이 없는 것**이었지만, 근거를 볼 수 없던 것이 문제다.
이제 사유를 찍는다. 「보류」라고 쓰지 않는다 — 다음에 읽는 사람이
«못 고친 오염 17건»으로 읽는다.

## 4-2. 그리고 표기 자리에 «위키 사정»이 들어와 있었다

되살린 행을 눈으로 보다가 발견했다.

```
김병욱  canonical_zh = "金炳旭 (1965年)"        (wikidata-label)
박태준  canonical_zh = "朴泰俊 (跆拳道运动员)"   (wikidata-label)
임수연  canonical_zh = "林秀妍（音译）"          (opencc)
이새롬  canonical_zh = "李赛纶（独立条目）"      (opencc)
```

«(1965년)»·«(태권도 선수)» 는 위키백과가 **같은 이름이 여럿일 때 문서를 가르려고**
붙인 말이다. 사람 이름이 아니다. «(음역)»·«(독립 항목)» 은 아예 위키 내부 사정이다.
그대로 내보내면 번역 쪽은 그게 공식 표기인 줄 안다 — **빈칸보다 나쁘다.**

실측 active: zh 128 · zh_hant 121 · ja 235 · en 117 = **601칸**.

가르는 기준을 «내 판단»으로 두지 않았다. 관측 둘만 쓴다.

1. **한국어 정본에 괄호가 있는가.** 있으면 제목의 일부다 — 128건 중 84건이 그랬다
   (`Amigo (Feat. 민수)` · `작은 것들을 위한 시(Boy With Luv)`).
2. **괄호 안이 동음이의 주석 어휘인가.** 직업·연도·매체종류·위키 내부용어.

②가 없으면 **하필 버려야 할 쪽을 남기는** 실수가 납니다:

| 값 | 걷으면 | 왜 안 되나 |
|---|---|---|
| `f(x)` | `f` | ★이게 그룹 이름이다 |
| `ALL(H)OURS` | — | 괄호가 끝이 아니라 구조로 이미 빠진다 |
| `Jin（金硕珍）` | `Jin` | 괄호 **안**이 맞는 값이다 |

**못 고치는 것은 그대로 둡니다.** 특히 전소현 `Yuna (演员)` — 걷으면 `Yuna` 가 되는데
주석만 사라질 뿐 **사람 이름의 중국어 표기가 라틴**이라는 사실은 그대로고, 오히려
정상적인 라틴 브랜드명처럼 보여 구멍이 눈에 덜 띕니다. **틀린 값을 그럴듯하게
다듬는 일은 하지 않습니다.**

```
조회 600 · 걷음 55 · 그대로둠 545 · 2회차 0건(멱등)
```

걷어 내자 그동안 «다른 문자 섞임»으로 가려져 있던 자체 오염 2칸이 드러나 같이 고쳤다.

## 4-3. ★오늘 가장 비싼 것 — 판정기 둘이 옛 범위로 판정하고 있었다

요청이 **지금** 들어오는 것을 보다가 걸렸습니다.

```
농협       candidate  zh 없음   "한국 대중문화 엔티티가 아닌…"
롯데마트   candidate  zh 없음   "롯데마트는 한국 대중문화(K-콘텐츠) 엔티티가 아닌 유통…"
이마트     candidate  zh 없음   "이마트는 한국 대중문화(K-콘텐츠) 엔티티가 아닌…"
연세대학교 rejected   type=term "비-K(범위밖): 일반 교육기관"
```

**게이트는 0143 으로 고쳤는데(2026-09-15), 그 뒤에 오는 판정기 둘을 안 고쳤습니다.**
새 유형이 문을 통과해 들어온 뒤 거기서 다시 죽었습니다.

`verify_evidence.go` 의 기각 규칙 원문:

> "해외 인물, 일반 단어/명사, **의약품·스포츠·정치 등 무관 분야**"

**스포츠와 정치는 0143 이 명시적으로 넣은 분야입니다.**

`type_retrace.go` 는 더 나빴습니다. 고를 수 있는 유형 목록이 **11종**이라 0143·0146 으로
늘린 10종(정당·정부기관·기업·단체·프로구단·학교·게임·뮤지컬·웹툰·간행물)이
**하나도 없었습니다.** 판정기가 「국민의힘」을 실존한다고 봐도 적을 칸이 없어 가장
가까운 `brand_place` 로 밉니다 — 기존 노트에 그 흔적이 그대로 있습니다
("사용 가능한 분류 중 brand_place가 가장 가깝습니다").

### 얼마나

```
[cand-evidence:review] 로 막힌 행     1,623
  옛 범위 문구가 사유인 것           1,133  (70%)
  막힌 candidate 502 중 0143 새 유형    147
새로 찍히는 속도   9/16:182 · 9/17:144 · 9/18:118 · 9/20:146   ← 지금도 돌고 있었다
```

### 고친 것

- 판정기 둘의 범위를 0143 으로. 기각 규칙에 **「'연예인이 아니다'는 기각 사유가
  아니다」** 를 명시 — 말로 못 박지 않으면 모델이 옛 틀로 돌아갑니다.
- 유형 목록을 손으로 적지 않고 `kdb.AssignableEntityTypes()` **한 자리**를 보게.
  손목록이 뒤처진 것이 원인이었으므로.
- **시험이 프롬프트 문자열에 죽은 명제가 있으면 실패**합니다. `scope_phrases.go` 가
  원장 노트에서 푼 문제를 프롬프트에서도 막습니다.
- 이미 찍힌 표시를 걷는 레인(dry 실측 **390건**). **제가 대신 판정하지 않습니다** —
  고쳐진 판정기가 다시 보게 열어만 줍니다(`status` 불변).

### 순서가 중요합니다

표시를 먼저 걷으면 **옛 판정기가 다시 찍습니다.** 배포 → 표시 걷기 순으로 합니다.

## 4-4. 같은 모양을 오늘 네 번 봤습니다

| 표시 | 걷은 사람 | 결과 |
|---|---|---|
| `[revert-term:reject]` | 없음 | 236행 영구 차단 |
| `[scope:review]` | 없음 | 80행이 itunes·discogs·enrich 세 레인에서 제외 |
| `[cand-evidence:review]` | 없음 | 1,133행, **지금도 하루 150건씩 증가** |
| `[occup-scope-restore]` | — | **오늘 제가 만들고 오늘 밟았습니다** |

**레인을 고치는 것과 그 레인이 남긴 표시를 걷는 것은 다른 일입니다.**
네 번 다 뒤쪽을 빠뜨렸고, 네 번째는 제 것입니다.
표시를 만들 때는 **그것을 걷는 조건도 같이 정해야 합니다.**

## 5. 남은 갭과 다음 수

### 지금 막힌 곳 (중국어 기준)

- **D. candidate 964건 — 값이 없다.** 발굴은 돌고 있다(`last_enriched_at` 최근, 한 번도
  안 돈 행 0). 외부 근거가 **실제로 없는** 롱테일이다. 오너 지시대로 **빈칸으로 둔다** —
  번역 쪽이 LLM 직번역을 할 수 있어야 한다.
- **E. 행 없음 711건** · **B. active 빈칸 634건.**

### 앵커가 병목이다 (새로 실측)

zh 빈칸 3,371행 중 위키데이터 앵커가 있는 것은 **266행뿐**이다(kowiki 링크 217).
번역이 안 되는 게 아니라 **대상을 특정할 근거가 없다.**

ko.wikipedia 제목 직접 조회는 **안전하지 않다**(실측 50건 표본):
문서는 33건 있는데 zh 링크는 6건이고 그중 절반이 오매칭이다 —
`시소→蹺蹺板`(놀이기구) · `창희→昌豨`(중국 인물) · `판→牧神`(그리스 신) ·
`고세구→异世界女团`(그룹). 짧은 한국 이름은 동음이의가 너무 많다.
→ `kowiki_anchor_drain` 이 이미 가드 셋으로 이 문제를 푼다(적격 1,378행 남음).

### 오너 손이 필요한 것 (둘 다 무료 등록)

- **Spotify Client ID/Secret** — song_album 이 가장 큰 갭이다(빈칸 694 · 요청 993).
- **공공데이터포털(data.go.kr) 인증키** — 학교 공식 영문명, 공공기관 목록.

## 6. 오늘 커밋

```
eec7876  fix(scope): 표시가 자기 사유를 부정하는데 236행이 영구 차단돼 있었다
949ffd2  fix(zh-repair): "건너뜀 17" 이 무엇인지 아무도 몰랐다
f09780e  fix(zh): 승급 경로로 들어온 오염을 아무도 안 봤다 — 자체 수리를 주기로 돌린다
7ace771  fix(scope): 레인은 껐는데 그 레인이 남긴 표시를 아무도 안 걷었다
7e0c4df  fix(scope): 되살림이 두 걸음인데 «이미 봤다» 표시가 두 번째를 막았다 + 앵커 레인 스케줄
03744e3  fix(paren): 위키백과가 문서를 가르려 붙인 말이 표기 자리로 나가고 있었다
```

## 7. 오늘 숫자

```
active           14,565 → 14,788   (+223)
되살린 행           217            중국어 160 · 전부 confidence 0.72
걷어 낸 주석         55칸
자체 오염 수리       24칸          (내가 승급으로 들여온 16 + 주석 뒤에 가려졌던 8)
간체 칸 번체          0            번체 칸 간체 0 · 재실행 0건(멱등)
```

**세 번 같은 모양을 봤다:** 레인을 끄거나 고쳤는데 **그 레인이 남긴 표시를 아무도 안 걷었다.**
`[revert-term:reject]` · `[scope:review]` · `[occup-scope-restore]` — 셋 다 그랬다.
마지막 것은 **오늘 내가 만든 것**이다. 표시를 만들 때는 그것을 걷는 조건도 같이 정해야 한다.

브랜치 `fix/occup-scope-restore`. 다른 에이전트와 동시 작업 중이라
공유 배포 체크아웃(`/home/aiin/kdb/repo`)은 깨끗한 `main` 으로 되돌려 두었다.

---

# 8. 저녁 — 일반명사에 제 구간을 주고, 교정을 스스로 닫게 했다

오전에 고친 것을 오후에 **내가 다시 깼다.** 그 자리에서 구조를 바꿨다.

## 8-1. 오늘 만든 레인이 옳게 거른 것을 걷어 갔다

`DrainDeadScopeFlags` 를 한 번 돌린 결과(396건)를 실측하니:

```
걷힌 396  ·  그중 일반명사 사유 118  ·  해외 사유 24
이미 «걷힘 → 재판정 → 똑같은 결론» 왕복을 마친 것 43
```

원인은 정규식이 아니라 **낱말로 가르려 한 것**이다. 판정기는 살아 있는 명제를
죽은 명제와 같은 말로 쓴다:

```
무지개  "…일반 명사로서의 '무지개'를 다루고 있으며 K-콘텐츠(song_album)와 무관"
        ↑ 살아 있는 판정                            ↑ 죽은 명제의 낱말
```

게다가 노트 **전체**를 봤다. 무지개의 cand-evidence 사유는 "기상 현상" 인데, 같은
노트 다른 자리에 gatekeeper 가 "K-엔터테인먼트 고유명사가 아닌" 이라고 적어 두었다.

그리고 그 레인은 범위 문구를 **자기 파일에 인라인으로** 적었다 — `scope_phrases.go`
가 막으려던 일곱 번째 사본이다. 가드 시험이 있었는데 정규식이 `[~!][~]`(두 글자)라
평범한 `~` 를 못 잡았다. **가드가 못 잡는 가드는 없는 것보다 나쁘다.**

## 8-2. 일반명사 구간 (mig 0150)

「가끔 일반명사도 올라온다」는 오너 지적이 이 혼돈의 정확한 이름이다. 지금까지
일반명사는 **적을 자리가 없어** 자유 문장에 섞였다. 자리를 만들었다.

| | |
|---|---|
| `kwave_kdb_common_nouns` | 낱말 하나가 한 행. 사유·근거·판정자·요청횟수·되돌림 |
| `entity_type='common_noun'` | 원장 쪽 칸. **미상(term)과 구분된다** |
| `CHECK (NOT (common_noun AND active))` | 일반명사는 서빙되지 않는다 — 쿼리마다 조건을 적는 대신 넣는 쪽을 막는다 |

`term` 은 changelog 가 이미 «어느 칸인지 모르겠다»라고 못 박았는데 세 레인이 그것을
«일반어»로 읽어 왔다(rejudge 제외 · cand-evidence 제외 · romanize 제외). 그래서
서울대학교·연세대학교가 term 으로 죽은 채 **어느 레인에도 안 걸렸다.** 칸이 갈라졌다.

판정기에는 `kind` 칸을 새로 받는다(common_noun · foreign · type_mismatch · product).
사유의 종류가 표시로 남으므로(`[rk:common-noun]`) 다음 레인은 문장을 해석하지 않는다.

## 8-3. 기각 더미에 처음으로 손이 닿는다

```
죽은 범위로 rejected     2,157
  위키데이터 앵커 없음     2,030  ← DrainScopeReopen 이 JOIN 때문에 못 봄
  term·unknown            624   ← RejudgeRejects 가 제외
  status='rejected'              ← DrainDeadScopeFlags·type_retrace 가 안 봄
```

네 레인이 있는데 아무도 못 보는 자리였다. 근거는 **노트가 스스로 적은 종류**다 —
"일반 교육기관(**대학교**)" · "반도체 제조 **기업**" · "한국의 **정당**" · "KBO **프로야구단**".
`[revert-term:reject]` 가 오세훈에게 한 것과 같은 자기반박이다.

dry 실측 **315건**, 요청 많은 순서가 그대로 나온다:

```
더불어민주당 15 · SK하이닉스 14 · 이재용 14 · 연세대학교 14 · 한국사회복지저널 12
국민의힘 11 · 두산 베어스 11 · 가톨릭대 · 서울특별시청년활동지원센터 · 교육부 …
```

미상 칸은 노트가 이름 붙인 종류로 **재유형화**한다(dataqa_log 로 되돌릴 수 있다).
안 하면 되살려도 cand-evidence 가 term 을 선정에서 빼기 때문에 판정이 안 일어난다.
되살림은 `candidate` 까지다 — 판정은 고쳐진 판정기가 뉴스 근거로 한다.

## 8-4. 교정은 우리가 닫는다

| 무엇이 | 어떻게 |
|---|---|
| `verdict=other` 가 클라 확인을 기다리다 48h 소모 (확인 실적 **0건**) | 근거가 명확하면(Wikidata 일치 · 빈칸+신뢰출처) **즉시 반영** |
| 우리 수정안이 우리 문자셋 규칙을 어기면 `continue` — 로그도 사유도 없음 | 제 자체로 되돌려 보고(`我獨自生活`→`我独自生活`), 안 되면 **사유를 남기고** 운영자에게 |
| 7일 뒤 «클라이언트 미응답» 이라는 **사실과 다른 사유**로 강등 | 우리 잘못을 클라 탓으로 적지 않는다(시험이 막는다) |
| 같은 신고 757건 재제출, 매번 LLM 재판정 | 30일 내 같은 판정은 **재사용**. 새 신뢰출처가 오면 재판정(탈출구) |
| `LIKE '%codex 검증 불확실%'` — gemma 가 답한 건은 영영 pending | 이름표가 아니라 판정 내용으로 고른다 |

#3867(「나 혼자 산다」 zh)이 3일 17시간 멈춰 있던 이유가 두 번째 줄이다. 클라는
응답했고 간체로 올바르게(`我独自生活`) 제안까지 했다.

## 8-5. 부수적으로 드러난 것

오전의 type_retrace 수정이 **절반만** 적용돼 있었다. 프롬프트는 `AssignableEntityTypes()`
를 보게 고쳤는데 **스키마 enum 과 받아들임 표는 11종 그대로**였다 — 판별기에게
"school 을 골라도 된다"고 말해 놓고 스키마가 그 값을 막고 있었다. 셋 다 한 자리를 본다.

## 8-6. 새 명령

```
kdb-app scope-rejected [n] [go]            기각 더미 회수 (dry 315)
kdb-app common-nouns [n]                   일반명사 구간 보기
kdb-app common-nouns backfill [n] [go]     원장 문장에 있던 판정 옮기기
kdb-app common-nouns revoke <낱말> <사유>   «실은 고유명사였다» 되돌림
```

주기 레인: `KDB_SCOPE_REJECTED_INTERVAL_SECONDS`(기본 1시간) · `_BATCH`(25) ·
`KDB_SCOPE_REJECTED_ENABLED=0` 으로 끈다.

## 8-7. 검증

```
go test ./...        전체 통과(34 패키지)
mig 0150             복원 DB 적용 확인 · CHECK 가 common_noun+active 를 실제로 거부
새 선정 조건 dry     오늘 잘못 걷힌 13건 전부 «안걷음(정상)» · 구간 추출 실패 0/177
```

---

# 9. 밤 — 채우는 쪽으로 돌아왔다

오너 지시로 축이 바뀌었다. *"못 채운 것은 방법을 찾아 채워야지. 우리는 채워서 제공해
주는 시스템이야. 공신력 있는 소스 확보하고, 채워야지."*

## 9-1. 요청 상위가 «값이 없다»가 아니라 «행이 안 살아 있다» 였다

7일 요청 상위 20을 보니 대부분이 `행없음` — 원장에 있는데 active 가 아니다.
더불어민주당·이재명·SK하이닉스·국민의힘·조국·이재용. 채우기 전에 **회수**가 먼저다.

## 9-2. 출처는 있었다. 길이 없었을 뿐이다

요청 상위 15개를 위키데이터에 직접 물었다 — **14/15가 en·ja·zh 라벨을 갖고 있고 전부
kowiki 문서가 있다.** 그런데 0143 으로 들어온 유형(정당·부처·대학·구단·기업)에는
**앵커를 붙여 주는 레인이 하나도 없었다.** person 엔 musicbrainz·tmdb·kofic 이,
작품엔 discogs·itunes 가 있는데 기관엔 아무것도 없다.

## 9-3. 이름으로 찾으면 안 된다 — 같은 표본에서 나온 함정 셋

    조국   → Q642555   "nation of one's 'fathers'"     ← 일반명사 祖國
    교육부 → Q861556   "United States Department of …" ← 미국 교육부
    FC서울 → Q27951528 "reserve team of FC Seoul"      ← 2군(검색 1위는 **곤충 속**)

셋 다 이름은 정확히 일치한다. 그래서 **세 근거가 다 맞을 때만** 앵커를 붙인다:
①종류(P31) ②한국(P17, 타국 명시는 배제) ③한국어 문서 제목이 우리 정본과 일치.

## 9-4. 막힌 길을 다섯 번 뚫었다 — 셋은 내가 만든 결함이다

| # | 무엇이 막았나 | 결과 |
|---|---|---|
| 1 | `SearchAndFetch` 의 K-웨이브 설명 필터가 기관을 전부 걸러냄 | 0 → 7 |
| 2 | 위키데이터 검색 순위가 한국 기관명에 약함 → ko.wikipedia 제목 경로 | 7 → 8 (FC서울 확보) |
| 3 | 소비자 이름 ≠ 위키 정식명 → 괄호 뗌 + 약칭 리다이렉트 | 8 → 10 |
| 4 | **내 이름 가드가 내 확인을 부정** (괄호 떼고 찾은 뒤 원래 이름으로 검증) | 티빙·iNKODE 회수 |
| 5 | **dry 가 쓰기를 검증 안 함** — 컬럼명 오타로 18건 찾아 놓고 전부 실패 | 11건 실제 적재 |

5번이 가장 값싸게 배울 수 있었던 것이다. dry 는 «판정이 맞는가»만 봤고 «쓸 수 있는가»는
안 봤다. 이제 dry 도 트랜잭션 안에서 실제로 넣어 보고 되돌린다. 예행과 본 실행이
**같은 SQL 문장**을 쓰게 한 자리에 뒀다.

★약칭 리다이렉트는 **두 출처가 같은 말을 할 때만** 받는다. 「농협」은 「농업협동조합」
(일반 개념)으로 리다이렉트되고 그 영문이 "Agricultural cooperative" 다 — 위키데이터
별칭에 우리 이름이 있어야 받는다(심평원·가톨릭대는 있고, 일반 개념엔 없다).

## 9-5. 결과

    앵커 11건 적재 → wd-locale 로 6칸 채움 → 11행 전부 active · en 10 · zh 9

    FC서울    en=FC SEOUL   ja=FCソウル    zh=首尔足球俱乐部  hant=首爾足球俱樂部
    롯데마트  en=Lotte Mart ja=ロッテマート  zh=乐天玛特       hant=樂天瑪特
    한국해양과학기술원 · 한림대춘천성심병원 · LoL 챔피언스 코리아 · 티빙 · 치지직 …

## 9-6. 남은 것

- **위키 문서가 없는 40/60** — 소니뮤직엔터…코리아·국민연금공단 지사·소형 기획사.
  위키 계열로는 못 채운다. **DART OpenAPI**(법인 영문명)·**공공데이터포털**(학교·공공기관)
  키가 있어야 한다. 둘 다 무료 등록, 오너 손이 필요하다.
- **`external ID reserved by another common Entity` 4건** — 한국도로공사·화계사·
  국립전주박물관·아트센터 나비. kdb 중복이 아니라 공통 플랫폼(kentity)이 그 QID 를
  선점했다. 그쪽 영역이라 기록만 남긴다.
- **거제시의회**는 위키데이터 항목에 라벨이 없다. 정직한 빈칸.
- **시간당 주기 레인이 하나도 안 돈다** — 배포가 20~30분마다 들어와 `time.NewTicker(1h)`
  가 매번 0으로 돌아간다. scope-rejected·paren-annot 이 여태 한 번도 못 돌았다.
  «시작 직후 한 번»이 없으면 배포가 잦은 날엔 시간당 레인이 없는 것과 같다.

## 9-7. 그리고 오늘 운영을 5분 세웠다

`ALTER TABLE ... ADD CONSTRAINT ... NOT VALID` 가 ACCESS EXCLUSIVE 를 잡는다.
`NOT VALID` 가 건너뛰는 것은 **전체 스캔**이지 잠금이 아니다 — 첫 판 주석이 그 둘을
혼동했다. 7분짜리 SELECT 뒤에서 기다리기 시작하자 FIFO 잠금 큐 때문에 **그 뒤의 평범한
읽기까지 전부 막혀** API 가 503 이 됐다. 취소하니 즉시 회복(health 17ms).

교훈은 «이 제약을 걸지 말자»가 아니다 — **바쁜 테이블의 DDL 은 기다리면 안 된다.**
lock_timeout 2초로 짧게 시도하고 못 얻으면 물러난다. 다섯 번 실패하면 제약 없이 간다.
복원 DB 에서 60초 잠금을 걸어 두고 검증했다(30초 만에 물러나고 읽기 19,754행 정상).
