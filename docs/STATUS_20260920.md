# 2026-09-20 — 중국어 응답률과 묻혀 있던 답

## 0. 한 줄

**소비자가 물은 낱말의 중국어 응답률은 49.5% 다.** 오늘 그중 절반의 원인이
«답이 없다»가 아니라 **«답을 갖고도 안 내보낸다»** 였다는 것을 찾아 155건을 되돌렸다.

## 1. 먼저 자를 고쳤다 — 종전 숫자는 소비자가 겪는 것이 아니었다

대시보드 `[3]` 은 «리서치 큐»를 모수로 «원장에 행이 있는가»를 셌다. 둘 다 틀린 자다.

| | 종전 `[3]` | 새 `[3-0]` |
|---|---|---|
| 모수 | 리서치 큐 | `kwave_kdb_request_terms` — **소비자가 실제로 물어본 낱말** |
| 붙이기 | `canonical_ko` 만 | **정규화 키 + 별칭** (API 가 붙이는 방식) |
| 판정 | 행이 있는가 | **그 로케일 칸에 값이 있고 `active` 인가** |

별칭을 안 보면 «데이식스» 요청이 `DAY6` 에 안 붙어 구멍으로 잡힌다.
그 차이만으로 중국어 응답률이 **44.9% 로 과소 보고**되고 있었다(실제 49.5%).

### 최근 7일, 요청 낱말 4,655개 기준

| | 요청 낱말 | 요청 건 |
|---|---|---|
| A. 중국어 답이 나감 | **2,330** | 5,008 |
| B. `active` 인데 zh 빈칸 | 634 | 1,028 |
| C. `candidate` — 값이 있는데 안 나감 | 19 | 79 |
| D. `candidate` — 값도 없음 | 964 | 1,363 |
| E. 원장에 행 자체가 없음 | 711 | 1,174 |

`[3-1]` 에 «중국어가 안 나가는 낱말»을 요청 많은 순으로 찍는다 —
번역 쪽이 직접 번역해야 하는 목록이 그대로 그것이다.

## 2. 오늘 찾은 것 — 표시가 자기 사유를 부정하고 있었다

원장에 이런 행이 236개 있었다.

```
[revert-term:reject] wikidata Q211650 직업이 비-엔터: "South Korean
association football player"
```

`[revert-term:reject]` 의 명제는 **«이 레코드에 붙은 QID 가 비-K 다»** 인데,
같은 줄이 그 QID 를 **"South Korean …"** 이라고 적고 있다. 기록이 스스로를 반박한다.

죽은 것은 대상이 아니라 «직업이 연예가 아니다» 라는 옛 범위 명제이고,
그건 0143(정치·경제·시사·스포츠)으로 소멸했다.

`scope_phrases.go` 는 이 명제의 다섯 가지 표현을 이미 한 자리에 모아 두었고
**오세훈을 예시로 적어 두기까지 했다.** 그런데 이 계열만 못 걸렀다 —
문구가 아니라 **표시**로 들어왔고, `NotATombstoneSQL` 이 표시를 먼저 보기 때문이다.

### 무엇이 묻혀 있었나

| 직업(위키데이터 설명) | 건수 |
|---|---|
| politician | 26 |
| footballer / association football player | 32 |
| writer | 9 |
| baseball player | 9 |
| volleyball · Go · pro gamer · archer · academic … | 나머지 |

이재명·정의선·류현진은 최근 7일 요청 상위 20 안에 **셋 다** 있었다.
우리는 값을 갖고 있으면서 아무것도 답하지 않았다. **오거부는 최상위 금칙이다.**

### 되돌린 결과

```
판정 225 · active 승급 155 · candidate 되살림 70
  동명이인 보류 4   (조국 · 오너 · 페이즈 · 천보성)
  동명 active 있어 건너뜀 4
  이름항목 0 · 개념 0 · 해외 0 · 조회실패 0
```

되돌린 155행: 영문 152 · 일본어 155 · **중국어 115** · 번체 115.
`confidence` 를 0.72 로 올렸다 — 강등이 0.000 으로 깎아 놓았고 조회 기본
`min_confidence` 가 0.50 이라, 안 올리면 `active` 로 돌려도 **여전히 안 나간다**.

노트를 믿고 되살리지 않았다. 226건 전부 **지금 위키데이터에 다시 물어서**
한국 대상인지 확인했다(이름항목·개념·해외·앵커 유형 가드는 기존 scope-reopen 과 동일).

## 3. 손대지 않은 것과 그 이유

| 대상 | 건수 | 왜 |
|---|---|---|
| `직업이 비-엔터` 인데 설명이 `South Korean` 이 아닌 것 | 12 | 북한(김정)·조선시대 인물(이순신·김홍도·이상설) — **범위 판단이 따로 필요하다** |
| 2026-07-21 감사 강등분 중 «오링크» 사유 | 105 | 경기·경남·구리·광종처럼 **지명/일반어가 person 으로 잘못 앵커**된 것. 이쪽 명제(«참조가 틀렸을 수 있다»)는 아직 살아 있다 |
| 동명이인 표시가 붙은 4건 | 4 | 실존 확인과 «그 이름으로 서빙해도 된다»는 다르다. 되살리되 `active` 로는 안 올린다 |

## 4. 오늘 내가 만든 회귀와 그 구조적 원인

155행을 `active` 로 올렸더니 **그중 16칸이 오염된 채 서빙 대상이 됐다**
(김병현 `金炳賢` · 오세훈 `吳世勳` · 박종현 `朴钟贤`). 표본을 눈으로 보지 않았으면 그대로 나갔다.

원인은 내 레인이 아니라 구조다.

- QA 자체검사(`qaCharsetOK`)는 **값을 채울 때만** 본다.
- 이미 값을 가진 행이 `candidate`/`rejected` 에서 `active` 로 **올라오는 경로**는
  그 검사를 한 번도 안 거친다. 그런 경로가 여럿이다 — kopis·kmdb·tmdb·kofic·
  on-demand·occup-scope.
- 그리고 `RepairZhVariants` 를 부르는 곳이 **일회성 명령 하나뿐**이었다.
  어제 손으로 만든 «간체 칸 번체 0» 은 **그 순간만 0** 이었다는 뜻이다.

→ 자체 수리를 **30분 주기**로 돌린다(`KDB_ZH_REPAIR_INTERVAL_SECONDS`).
   경로마다 가드를 다는 것보다, 새 경로가 생겨도 안 새는 유일한 방법이다.
   16칸은 수리했고 두 번째 실행은 0건이다(멱등).

또, 수리 도구가 «건너뜀 17» 을 건수만 세고 조용히 넘기고 있었다.
찍어 보니 전부 `昇`(16)·`汎`(1) — 두 자체 모두에서 옳은, 어제 검증해 둔 글자다.
**오염이 아니라 고칠 것이 없는 것**이었지만, 근거를 볼 수 없던 것이 문제다.
이제 사유를 찍는다. 「보류」라고 쓰지 않는다 — 다음에 읽는 사람이
«못 고친 오염 17건»으로 읽는다.

## 5. 남은 갭과 다음 수

### 지금 막힌 곳 (중국어 기준)

- **D. candidate 964건 — 값이 없다.** 발굴은 돌고 있다(`last_enriched_at` 최근, 한 번도
  안 돈 행 0). 외부 근거가 **실제로 없는** 롱테일이다. 오너 지시대로 **빈칸으로 둔다** —
  번역 쪽이 LLM 직번역을 할 수 있어야 한다.
- **E. 행 없음 711건** · **B. active 빈칸 634건.**

### 앵커가 병목이다 (새로 실측)

zh 빈칸 3,371행 중 위키데이터 앵커가 있는 것은 **266행뿐**이다(kowiki 링크 217).
번역이 안 되는 게 아니라 **대상을 특정할 근거가 없다.**

ko.wikipedia 제목 직접 조회는 **안전하지 않다**(실측 50건 표본):
문서는 33건 있는데 zh 링크는 6건이고 그중 절반이 오매칭이다 —
`시소→蹺蹺板`(놀이기구) · `창희→昌豨`(중국 인물) · `판→牧神`(그리스 신) ·
`고세구→异世界女团`(그룹). 짧은 한국 이름은 동음이의가 너무 많다.
→ `kowiki_anchor_drain` 이 이미 가드 셋으로 이 문제를 푼다(적격 1,378행 남음).

### 오너 손이 필요한 것 (둘 다 무료 등록)

- **Spotify Client ID/Secret** — song_album 이 가장 큰 갭이다(빈칸 694 · 요청 993).
- **공공데이터포털(data.go.kr) 인증키** — 학교 공식 영문명, 공공기관 목록.

## 6. 오늘 커밋

```
eec7876  fix(scope): 표시가 자기 사유를 부정하는데 236행이 영구 차단돼 있었다
949ffd2  fix(zh-repair): "건너뜀 17" 이 무엇인지 아무도 몰랐다
f09780e  fix(zh): 승급 경로로 들어온 오염을 아무도 안 봤다 — 자체 수리를 주기로 돌린다
```

브랜치 `fix/occup-scope-restore`. 다른 에이전트와 동시 작업 중이라
공유 배포 체크아웃(`/home/aiin/kdb/repo`)은 깨끗한 `main` 으로 되돌려 두었다.
