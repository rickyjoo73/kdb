# 고유명사 운영센터 — UI 우선 수직 구현

요청: 첫 화면에서 신규 등록과 분야별 활용 현황을 확인할 수 있어야 한다.
UI 모형만 배포하지 않고 실제 DB 집계·필터·상세·기존 검수 기능까지 연결한다.

**18:46 KST 운영 반영 완료.** 코드 `a70797848f8ad23b505c31b07cf1c1592d9ecb8e`,
이미지 `kdb-app:entity-center-20260912-1` (KDB 소유자 commit/push/build/deploy).
아래 인수 범위만 완료한 것이며 분야별 전체 수집/분류/표기 승인 완료가 아니다.

## 이번 인수 범위

- [x] 첫 화면에 공통 고유명사 운영센터, 분야 카드, 신규 등록 목록과 작업 진입점 제공.
- [x] 공통 목록에 유형·분야·상태·원출처 필터, 등록/정체성 변경순 정렬, 페이지 탐색 제공.
- [x] 등록 폼을 직접 여는 링크와 상품·개념·미확인 유형까지 제공.
- [x] 미분류 수량/목록을 명시한다. 기존 KDB 출처만으로 연예 분야를 추정해 일괄 분류하지 않는다.
- [x] 등록 시각과 정체성 변경 시각을 구분한다. 레거시 표기 변경 전체 이력으로 과장하지 않는다.
- [x] 실제 SQL 집계/필터·오류·권한 회귀·모바일/PC·운영 로그인 검증 후 배포한다.

## 화면 계약

`/admin/` → 운영센터 검색 / 등록 진입 / 신규 24h / 미검증 후보 / 분야 미지정 /
8개 분야 카드 / 최근 등록 8건 / 기존 상세 운영 지표.

`/admin/kentity` → 전체 집계 / 조합 필터 / 등록·정체성 변경 시각 / 50개 페이지 /
`?create=1#candidate-create` 등록 폼. 등록 후 기존 동일 UUID 상세와 자동 조사로 이동한다.

모든 카드 수는 공통 Entity 원장 기준이며 승인된 언어 수나 전체 수집 목표 달성률이 아니다.
다중 분야는 중복 집계될 수 있다. 분야 0건은 삭제가 아니라 분류 연결 부재일 수 있다.
신규 24h는 원장에 보존된 created_at 기준이며 이관한 날을 신규로 부풀리지 않는다.
공통 기능 비활성 시 기존 대시보드 유지. 조회 실패를 0으로 표시하지 않는다.

## 후속 (이번 배포 완료와 구분)

기존 데이터 분야 분류와 근거·검수 경로, 공식 정치/경제/스포츠 출처 adapter,
누락 자동 발견 및 실제 요청별 보충 효과, Gemma 작업별 비용/수율, TDB 원본 교정/이관,
기사 문맥 linking/Glossary/PressLocale/CMS 종단 간 연동은 별도 완료 게이트가 필요하다.
시간 예상: 본 UI/실데이터 연결 1~2시간의 초기 추정. 분야별 데이터 품질 작업은 수일 이상이며
외부 출처·권리·실측 검증에 따라 달라진다. 전체 플랫폼 완료 시각을 약속하는 문서가 아니다.

## 검증 기록과 한계

- 전체 `go test ./... -count=1` (격리 PostgreSQL 포함), `go build ./...`,
  `go test -race ./internal/kentity ./internal/kdbadmin -count=1` 통과.
- 이전 공유 캐시/합성 HTML 파일에 root 소유 파일이 있어 첫 전체 실행은 환경 권한으로 실패했다.
  기존 파일 권한 변경 없이 별도 개인 캐시/산출물 경로에서 전부 재실행해 통과했다.
- 기존 운영 복원본 READ ONLY 화면 점검: 첫 화면 1.01초, 공통 목록 150ms,
  정치 후보 필터 46ms, 미분류 KDB 2페이지 214ms. 해당 시점 관측이며 SLA가 아니다.
- 390px/1440px 합성 브라우저: 전체 화면 상태/모바일 탐색/검색/복합 필터/등록 폼/보호 폼 통과.
  산출물 `/tmp/kdb-entity-center-ui.I7y4Nv`, 운영 데이터가 아닌 합성 예시.
- 실제 정상 로그인: 첫 화면의 새 영역, 기존 workflow/preparations/common/mapping/TDB 상세,
  `?create=1` 폼 열림, 정치 후보/스포츠 인물/미분류 KDB 페이지 이동 모두 200 및 오류 표시 없음.
- 앱 18:46:14 KST 시작, healthy, API/admin 200, 기존 4개 네트워크/IP 유지.
- 스키마 migration·운영 이름/분야 대량 변경·후보 승인·Gemma 스케줄 변경 없음.
- 페이지는 50건 단위, 최대 offset 100000. 큰 이관 뒤에는 cursor pagination 전환이 필요하다.
- 분야 미지정은 모든 상태를 포함한다. 미지정을 일괄 연예로 분류하지 않으며 분류 검수는 후속이다.
- 최근 정체성 변경은 common entity.updated_at 기준이다. 모든 레거시 언어 변경 이력/자동 조사 이벤트 피드가 아니다.
- 원출처는 최초 등록 계통이고 현재 쓰기 책임과 구분된다. 검수 상태 active는 언어별 ready와 다르다.

Rollback: `.env`의 KDB_APP_IMAGE/KDB_BUILD_VERSION 두 키만 직전
`kdb-app:tdb-class-guard-20260912-1` / `tdb-class-guard-20260912-1`로 되돌려
기존 두 compose 파일로 앱만 재생성한다. DB 복원이나 보호 플래그 해제는 필요 없다.
